make sure to read the parent CLAUDE.md one level up.

see CODING_STANDARDS.md for the rules distilled from this project's
own shipped-and-fixed bugs; the CI gates in .github/workflows/ci.yml
(sanitizers, valgrind, fuzz, cppcheck, multi-compiler -Werror) are a
standing requirement, not optional.
