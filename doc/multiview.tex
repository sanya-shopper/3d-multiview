\documentclass[11pt]{article}

\usepackage[margin=1.1in]{geometry}
\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{positioning,fit,calc}
\usepackage[colorlinks=true,linkcolor=blue!60!black,citecolor=green!40!black,urlcolor=blue!60!black,pagebackref=true]{hyperref}
% bibliography back-references: each entry lists (hyperlinked) the pages
% where it is cited
\renewcommand*{\backref}[1]{}
\renewcommand*{\backrefalt}[4]{%
  \ifcase #1 \or (cited on p.~#2)\else (cited on pp.~#2)\fi}

\theoremstyle{definition}
\newtheorem{example}{Example}
\providecommand*{\exampleautorefname}{Example}

\newcommand{\mat}[1]{\mathbf{#1}}
\newcommand{\vect}[1]{\mathbf{#1}}
\newcommand{\R}{\mat{R}}
\newcommand{\K}{\mat{K}}
\newcommand{\F}{\mat{F}}
\newcommand{\E}{\mat{E}}
\newcommand{\B}{\mat{B}}
\newcommand{\PP}{\mat{P}}
\newcommand{\Hm}{\mat{H}}
\newcommand{\X}{\vect{X}}
\newcommand{\x}{\vect{x}}
\newcommand{\tv}{\vect{t}}
\newcommand{\Cv}{\vect{C}}
\newcommand{\code}[1]{\texttt{#1}}

\title{\textsc{multiview}: A Portable C99 System for Fixed-Camera\\
Scene Analysis and 3D Model Extraction\\[4pt]
\large A self-contained overview of the system and its mathematics}
\author{Project documentation --- v0.6.0}
\date{August 4, 2026}

\begin{document}
\maketitle

\begin{abstract}
\textsc{multiview} is a small, dependency-free C99 library for metric scene
analysis and 3D model extraction from the synchronized video streams of
\emph{two stationary cameras} observing an overlapping volume, optionally
augmented by a collection of still photographs and, in a later stage,
range-finder measurements. Calibration is part of the system: the rig is
calibrated from views of a printed letter-page checkerboard of known square
size, or from a computer display under our control showing synchronized
Gray-code patterns. This document doubles as a self-study overview of the
underlying mathematics: each concept is developed from first principles,
cross-linked to the modules that implement it, illustrated by a
\emph{hand-computable} worked example, and sourced in the bibliography.
All quantitative claims (\autoref{sec:results}) are reproducible from the
deterministic test programs in the repository.
\end{abstract}

\tableofcontents

%----------------------------------------------------------------------
\section{Introduction and capabilities}
\label{sec:capabilities}

A camera pointed at a three-dimensional scene produces a grid of pixels,
each holding a brightness or color sample. In principle that grid encodes
a great deal about the scene's geometry; the difficulty is what a single
image withholds. Every point along one pixel's viewing ray lands on the
same pixel, so depth is lost outright --- and the grid carries no
intrinsic unit of measurement: nothing in the pixels distinguishes a
small scene photographed up close from a large one photographed from
afar. Both losses are repairable. A second camera viewing the same
volume restores depth, because a point seen by both cameras is pinned to
the intersection of two viewing rays (\autoref{sec:triangulation});
and placing an object of known physical dimensions in the volume anchors
the unit, because its image tells the pixel grid what a metre looks like
--- the heart of calibration (\autoref{sec:calib}). Time has the same
character: each camera samples light at its own rate, on its own private
clock, so no pair of frames arrives certified as simultaneous --- but by
comparing \emph{when} objects appear, move, and disappear in each view,
the clocks themselves can be measured and brought into a common frame
(\autoref{sec:timemodel}), making ``the same instant'' as recoverable as
``the same point.'' From two views and
one known object, it becomes possible to calibrate the physical meaning
of every pixel --- in space and in time --- and then to extract
progressively richer descriptions of
what the cameras see: distances, surfaces, models of the scene, and
events unfolding in it. This document develops that program, engineered
for one deliberately favorable setting.

The target deployment is deliberately narrow and fully under our control:
\emph{fixed} cameras --- two in the reference rig --- are rigidly mounted
and observe the same working volume. ``Fixed'' is the load-bearing word:
the contrast is with mobile-camera systems (handheld, vehicle- or
drone-mounted), which must solve ego-motion before they can measure
anything. Here the cameras never move, so every rig-dependent quantity is
computed once --- and a \emph{mobile} camera entering the volume is not a
rival architecture but just another tracked mover, one that happens to
carry a sensor (\autoref{sec:photos}). The system
first \emph{calibrates itself} from views of a known planar target
(\autoref{sec:calib}); thereafter video is processed frame by frame. The
result of that first step is best stated at the level of pixels: each
camera of the rig ends up with a known intrinsic matrix, lens-distortion
model, and pose in one shared metric world frame, so every pixel of every
camera is promoted from a bare brightness sample to a measurement along a
\emph{known ray} --- pixel $(u,v)$ of camera $i$ observes exactly the
points on one fixed, computable line in space (back-projection,
\autoref{sec:camera}), and conversely any 3D point in the working volume
projects to a predictable pixel in each camera. Because
the cameras do not move, every geometric quantity that depends only on the
rig --- the projection matrices (\autoref{sec:camera}), the fundamental
matrix (\autoref{sec:epipolar}), the rectifying homographies
(\autoref{sec:stereo}) --- is computed \emph{once} and reused for every
frame, which is
the central efficiency win of the stationary-rig assumption.

The implemented core (v0.4.0) provides:
\begin{enumerate}
  \item plane-based calibration of intrinsics, distortion, and pose from a
        printed letter-page checkerboard or from a controlled display
        showing Gray-code patterns (\autoref{sec:calib});
  \item a calibrated pinhole camera model with Brown radial--tangential lens
        distortion, forward projection and ray back-projection
        (\autoref{sec:camera});
  \item exact two-view epipolar geometry from the calibration, and the
        normalized 8-point algorithm to estimate or verify it from image
        correspondences (\autoref{sec:epipolar});
  \item $N$-view linear triangulation of corresponding image points into
        metric 3D, with reprojection-error reporting
        (\autoref{sec:triangulation});
  \item planar stereo rectification of the pair, and dense correspondence by
        block matching with sub-pixel refinement, giving per-pixel depth
        (\autoref{sec:stereo});
  \item the calibration-subsystem front half: the spec-v1 composite
        screen generator with its shipped M-array, a synthetic renderer,
        and the blind pattern reader --- corners, identities,
        orientation, and frame counter from a single frame
        (\autoref{sec:calibspec});
  \item TSDF fusion over enriched samples with marching-tetrahedra mesh
        extraction --- the first model representation beyond point
        clouds (\autoref{sec:tsdf});
  \item point-cloud assembly and PLY
        export\footnote{PLY, the \emph{polygon file format} (also
        ``Stanford triangle format''), is the de-facto interchange
        format for point clouds and meshes. Its \emph{ASCII} variant is
        plain text: a short human-readable header declaring the
        elements and their properties, then one line per vertex ---
        trivially writable from C99, diffable, and loadable by every
        common mesh tool. See \autoref{sec:models} for a complete
        example file.} for inspection in standard mesh tools, plus
        minimal PGM image I/O (\autoref{sec:models}).
\end{enumerate}

Planned extensions --- structure-from-motion over an auxiliary photo
collection and fusion of range-finder data --- are designed but not yet
implemented; their theory and integration points are described in
\autoref{sec:extensions} so that the code can grow into them without
rearchitecting.

\paragraph{How to read this document.} \autoref{sec:toolkit} collects the
small amount of linear algebra everything else stands on, and closes with
two short orientation notes that locate the system's design choices in the
wider reconstruction literature.
\autoref{sec:camera}--\autoref{sec:stereo} then follow the data through the
system: camera model, calibration, two-view geometry, triangulation, dense
stereo. Every section ends in a worked example with numbers chosen so the
computation fits on paper; doing them by hand \emph{is} the intended
self-study exercise, and each can then be checked against the library
--- or watched live in the interactive companion
(\autoref{sec:webcompanion}).

\subsection{The interactive companion}
\label{sec:webcompanion}

The repository ships a static web page, \code{web/index.html}, that
animates the core of this document: a stylized molecule floats in the
working volume, watched by two freely positionable cameras whose coarse
$64\times48$ displays are rendered at the pixel level. As the cameras
or the molecule move, the page recomputes live exactly what the
following sections derive --- projection (\autoref{sec:camera}),
plane-homography view transfer and its per-pixel difference from the
actual view (\autoref{sec:parallax}), epipolar lines
(\autoref{sec:epipolar}), DLT triangulation with its millimetre error
(\autoref{sec:triangulation}), and the depth-error prediction
(\autoref{sec:stereo}). Scenes are addressable by URL, so the text can
point at a configuration and mean it: \code{?scene=default} is the
wide-baseline reference rig, \code{?scene=narrow} moves the cameras
nearly together (plane transfer becomes exact while triangulation
degrades --- the two faces of \eqref{eq:parallax} and
\eqref{eq:deptherr}), and \code{?scene=roll} rolls camera 2 to slant
its epipolar lines; any hand-arranged scene can be copied as a link
from the page and pasted into notes. The page is dependency-free
(hand-written HTML/JS; the math lives in \code{web/model.js} and is
unit-tested against this document's worked examples by
\code{make check}).

\paragraph{Design constraints.} The library is strict C99 with no
dependencies beyond \code{libm}, row-major \code{double} arrays throughout,
and fully deterministic (no clocks, no ambient randomness; the demos use
fixed-seed generators). This keeps it portable from desktop to embedded
targets, and keeps every experiment repeatable --- a prerequisite for the
controlled-environment, learning-oriented use the project is intended for.

%----------------------------------------------------------------------
\section{System overview}
\label{sec:overview}

\begin{table}[ht]
\centering
\begin{tabular}{lll}
\toprule
Module & Header & Responsibility \\
\midrule
\code{mat}         & \code{mv/mat.h}         & small dense linear algebra, Jacobi SVD, null vectors \\
\code{cam}         & \code{mv/cam.h}         & camera model, projection, rays, distortion \\
\code{target}      & \code{mv/target.h}      & printed checkerboard model + printable rendering \\
\code{graycode}    & \code{mv/graycode.h}    & display-based active target (Gray-code patterns) \\
\code{calib}       & \code{mv/calib.h}       & homographies; Zhang calibration; radial estimate \\
\code{pattern}     & \code{mv/pattern.h}     & spec-v1 calibration screen, shipped M-array \\
\code{render}      & \code{mv/render.h}      & synthetic camera views of the display plane \\
\code{reader}      & \code{mv/reader.h}      & blind pattern decoding: corners, ids, counter \\
\code{epipolar}    & \code{mv/epipolar.h}    & $\F$ from calibration; normalized 8-point; $\E$ \\
\code{triangulate} & \code{mv/triangulate.h} & $N$-view DLT triangulation, reprojection RMS \\
\code{rectify}     & \code{mv/rectify.h}     & Fusiello rectification, disparity$\to$depth \\
\code{stereo}      & \code{mv/stereo.h}      & dense block matching (SAD, sub-pixel) \\
\code{img}         & \code{mv/img.h}         & PGM (P5) image I/O \\
\code{cloud}       & \code{mv/cloud.h}       & point cloud container, PLY export \\
\code{tsdf}        & \code{mv/tsdf.h}        & TSDF fusion over enriched samples; meshing \\
\bottomrule
\end{tabular}
\caption{Module layout. \code{mv/mv.h} is the umbrella header.}
\label{tab:modules}
\end{table}

Processing has a one-time phase and a per-frame phase:
\[
\underbrace{\text{target views}\rightarrow\text{calibrate}}_{\text{once, \code{target}/\code{graycode}/\code{calib}}}
\qquad
\underbrace{\text{2 frames}\rightarrow\text{undistort}\rightarrow\text{rectify}\rightarrow\text{match}\rightarrow\text{depth}\rightarrow\text{cloud}}_{\text{per frame, \code{cam}/\code{rectify}/\code{stereo}/\code{triangulate}/\code{cloud}}}
\]
Sparse operation (feature correspondences instead of dense matching) uses the
same chain with \code{epipolar} providing the correspondence-validation gate.
Because the rig is static, temporal accumulation over video frames is a
simple union of per-frame clouds in the fixed world frame; moving objects
appear as temporally inconsistent points and can be segmented by comparing
frames --- the entry point for scene \emph{analysis} beyond reconstruction.

%----------------------------------------------------------------------
\section{Mathematical toolkit}
\label{sec:toolkit}

Four ideas carry the whole system; everything later is an application.
The section closes with two subsections that are orientation rather than
machinery: the two dual ways of integrating measurements into a volume
(\autoref{sec:gatherscatter}), and the standard estimation problems ---
bundle adjustment and SLAM --- relative to which the whole design is best
located (\autoref{sec:slamba}).

\subsection{Homogeneous coordinates}
A 2D point $(u,v)$ is represented as any triple $(wu, wv, w)$, $w\neq0$; a
3D point $(X,Y,Z)$ as $(X,Y,Z,1)$ up to scale. The payoff: projection,
homographies and rigid motions all become \emph{linear} maps, and points at
infinity ($w=0$) need no special case. We write $\simeq$ for equality up to
a nonzero scale. \autoref{fig:homogpixels} shows what the convention means
physically for our cameras: a homogeneous triple is best read as the name
of a \emph{viewing ray}, and a pixel on a camera's display is exactly one
such ray --- which is why every rescaling of the triple addresses the same
pixel. (The companion page, \autoref{sec:webcompanion}, renders this
figure live: its camera displays are exactly such coarse pixel grids.)

\begin{figure}[t]
\centering
\begin{tikzpicture}[font=\scriptsize]
  % ---- left: the working volume seen by the two rig cameras
  \draw[gray!70] (2.3,0.4) rectangle (5.3,3.2);
  \node[gray!90!black] at (3.8,3.45) {working volume};
  \fill (4.0,1.6) circle (2.5pt) node[below right] {object at $\X$};
  \fill (0,0.6) circle (1.5pt) node[below] {$\Cv_1$};
  \fill (0.4,3.3) circle (1.5pt) node[above] {$\Cv_2$};
  \draw[->] (0,0.6) -- (4.0,1.6);
  \draw[->] (0.4,3.3) -- (4.0,1.6);
  \draw[thick] (0.99,1.41) -- (1.25,0.35) node[below] {$\pi_1$};
  \draw[thick] (1.25,2.29) -- (1.71,3.29) node[above] {$\pi_2$};
  \fill[blue!70] (1.12,0.88) +(-0.06,-0.06) rectangle +(0.06,0.06);
  \fill[blue!70] (1.48,2.79) +(-0.06,-0.06) rectangle +(0.06,0.06);
  % ---- right: camera 1's display, coarse pixels
  \draw[->,gray!90!black] (7.0,3.0) -- (7.7,3.0) node[right] {$u$};
  \draw[->,gray!90!black] (6.8,2.7) -- (6.8,2.0) node[below] {$v$};
  \draw[gray!50] (7.0,0.3) grid[step=4mm] (10.2,2.7);
  \fill[blue!20] (8.6,1.5) rectangle (9.0,1.9);
  \fill[blue!20] (8.2,1.1) rectangle (8.6,1.5);
  \fill[blue!20] (8.6,1.1) rectangle (9.0,1.5);
  \fill[blue!50] (8.2,1.5) rectangle (8.6,1.9);
  \node at (8.6,3.3) {display of camera 1};
  \draw[->,shorten >=2pt] (8.0,-0.15) -- (8.35,1.6);
  \node at (8.6,-0.35)
    {$(60,70,1)\simeq(120,140,2)$: the object's pixel};
\end{tikzpicture}
\caption{What a homogeneous triple names. Left: each camera of the rig
observes the working volume through its image plane ($\pi_1,\pi_2$,
thick); the object at $\X$ determines one ray per camera (arrows), and
each ray punches through its image plane at one pixel (blue squares).
Right: camera 1's display as a coarse pixel grid --- the object lights
up a small patch of pixels (light blue), one of which (dark blue) is
the pixel of $\X$. Every point along the left-hand ray projects to
this same pixel, and correspondingly every rescaling
$(wu,wv,w)$ of its homogeneous coordinates, e.g.\ the two triples of
\autoref{ex:homog}, dehomogenizes to the same display position
$(u,v)=(60,70)$: the triple names the ray, the ray names the pixel.}
\label{fig:homogpixels}
\end{figure}

\begin{example}[dehomogenization]
\label{ex:homog}
$(120,\,140,\,2) \simeq (60,\,70,\,1)$: both name the pixel $(60,70)$.
Conversely the direction ``straight down the $u$ axis'' is $(1,0,0)$, which
no finite pixel represents.
\end{example}

\subsection{Rotations}
A rotation matrix $\R\in SO(3)$ satisfies $\R^{\!\top}\R=\mat{I}$,
$\det\R=1$; its rows are the axes of the new frame expressed in the old.
Consequently $\R^{-1}=\R^{\!\top}$ --- inverting a rigid transform never
requires a matrix inversion.

\begin{example}[rotation about $y$]
\label{ex:rot}
$\R_y(90^\circ)=\bigl(\begin{smallmatrix}0&0&-1\\0&1&0\\1&0&0\end{smallmatrix}\bigr)$
sends the world $z$ axis $(0,0,1)$ to $(-1,0,0)$: a camera yawed $90^\circ$
sees the world's $-x$ direction ahead. Check $\R^{\!\top}\R=\mat I$ by
inspection: the rows are orthonormal.
\end{example}

\subsection{The cross-product matrix}
For $\vect v=(v_1,v_2,v_3)$,
\begin{equation}
[\vect v]_\times=
\begin{pmatrix}0&-v_3&v_2\\ v_3&0&-v_1\\ -v_2&v_1&0\end{pmatrix},
\qquad [\vect v]_\times\vect w=\vect v\times\vect w .
\label{eq:skew}
\end{equation}
It is rank 2 with null vector $\vect v$ itself --- the algebraic reason
epipoles exist (\autoref{sec:epipolar}).

\begin{example}[skew matrix]
\label{ex:skew}
$\vect v=(1,0,0)$:
$[\vect v]_\times=\bigl(\begin{smallmatrix}0&0&0\\0&0&-1\\0&1&0\end{smallmatrix}\bigr)$,
and $[\vect v]_\times(0,1,0)^{\!\top}=(0,0,1)^{\!\top}=\vect v\times\vect w$. \qedhere
\end{example}

\subsection{Homogeneous least squares and the SVD}
Every estimation problem in this system reduces to: given $\mat A$
($m\times n$), find $\vect x\neq0$ minimizing $\|\mat A\vect x\|$ subject to
$\|\vect x\|=1$. The minimizer is the right singular vector of the smallest
singular value of $\mat A=\mat U\,\mathrm{diag}(\sigma_1\ge\dots\ge\sigma_n)\,\mat V^{\!\top}$
\cite{GolubVanLoan2013}. The library computes this with a one-sided Jacobi
SVD (\autoref{sec:numerics}) via \code{mv\_nullvec}.

\begin{example}[null vector by hand]
\label{ex:nullvec}
$\mat A=\bigl(\begin{smallmatrix}1&2\\2&4\end{smallmatrix}\bigr)$ has rank 1
(row 2 $=2\times$ row 1), so the smallest singular value is exactly $0$ and
the null vector solves $x_1+2x_2=0$: $\vect x=\tfrac1{\sqrt5}(2,-1)$.
Check: $\mat A\vect x=\vect 0$.
\end{example}

\subsection{Integrating measurements into a volume: gather versus scatter}
\label{sec:gatherscatter}

A recurring computational choice later in the document
(\autoref{sec:models}) deserves to be named here in its general form.
The measurements this system produces are \emph{ray-shaped}: each depth
pixel says ``along this known ray, the first surface is at distance
$Z$'' --- which simultaneously asserts a surface \emph{near} the
endpoint and emptiness \emph{along} the segment before it. To build a
persistent model, a stream of such measurements must update a
volumetric state, in our case a voxel grid. There are exactly two
iteration orders, dual to one another:

\begin{itemize}
  \item \emph{Scatter} (measurement-major): for each measurement, visit
        the voxels it touches. \emph{Ray marching} steps each ray
        through the grid cell by cell --- an incremental traversal
        costing a few additions per voxel crossed \cite{Amanatides1987}
        --- updating the cells it passes through (measured empty) and
        the cells around its endpoint (measured near-surface).
        \emph{Voxel accumulation} is the degenerate scatter:
        back-project each point and increment the one voxel containing
        it, as in classical occupancy grids \cite{Elfes1989}; emptiness
        is then either ignored or restored by ray marching, which is
        exactly OctoMap's \code{insertPointCloud} \cite{Hornung2013}.
  \item \emph{Gather} (state-major): for each voxel, look up the one
        measurement that constrains it --- project the voxel center
        into the camera and read the depth map at that pixel. This
        demands \emph{dense} measurements (every lookup must find an
        answer); a depth map is precisely such a dense bundle of rays.
\end{itemize}

The costs are proportional to different things: scatter costs
$O(\text{rays}\times\text{voxels crossed per ray})$ and touches only
observed cells; gather costs $O(\text{voxels}\times\text{cameras})$ per
frame no matter how many measurements arrived. Fidelity differs too:
plain voxel accumulation records only \emph{presence}, so a surface
extracted from it is a density ridge whose accuracy is capped at one
voxel, whereas ray-marched and gathered updates can both store
\emph{signed distance to the surface}, whose zero crossing interpolates
to sub-voxel accuracy (\autoref{sec:tsdf}).

Where this system sits: its TSDF fusion (\autoref{sec:tsdf}) is the
gather form --- the right choice there because the depth maps are dense
and the cameras never move, so each voxel's projection is precomputed
once and the per-frame update collapses to a table lookup. Scatter is
not rejected, only deferred: it is the natural update for \emph{sparse}
measurements, which is how the planned range-finder returns
(\autoref{sec:range}) would enter the same TSDF through its
enriched-sample interface. Ray marching also returns on the output
side: marching a ray of a \emph{virtual} camera through the fused grid
until the stored distance changes sign renders a synthetic depth map
from any viewpoint (\autoref{sec:tsdf}).

\begin{example}[gather or scatter for our rig]
\label{ex:gatherscatter}
Numbers from \autoref{ex:tsdf}: $5.5$M voxels at $1$\,cm, two cameras,
so a gather sweep is $11$M lookups per frame regardless of input. A
dense $640\times480$ depth map is $307$k rays per camera; marching each
ray only through the $\pm\tau=7$\,cm truncation band ($\sim$14 voxels)
costs $4.3$M steps --- comparable --- but also carving the $\sim4$\,m of
free space before each hit ($\sim$400 voxels per ray) costs $123$M,
an order worse. For $10^4$ sparse laser returns the same two figures
are $0.14$M and $4$M --- there scatter wins by orders of magnitude.
Measurement density decides the traversal order.
\end{example}

\subsection{Placing the system: bundle adjustment and SLAM}
\label{sec:slamba}

Two terms dominate the neighboring literature; defining each by its
inputs and outputs is enough to see where this system stands.

\emph{Bundle adjustment} \cite{Triggs2000}. \emph{Input}: a set of 2D
feature correspondences across images (point $j$ seen in image $i$ at
pixel $\x_{ij}$), together with an initial estimate of every camera's
parameters and every 3D point. \emph{Output}: refined cameras and
points that \emph{jointly} minimize the total squared reprojection
error $\sum_{ij}\|\x_{ij}-\pi(\text{camera}_i,\X_j)\|^2$, with
$\pi$ the projection of \autoref{sec:camera}. It is a large sparse
nonlinear least-squares problem --- the gold-standard \emph{final
polish} of a reconstruction, not a way of obtaining one from nothing:
it needs the initialization.

\emph{SLAM}, simultaneous localization and mapping \cite{Cadena2016}.
\emph{Input}: a time-ordered measurement stream from a \emph{moving}
sensor (images, range scans, often inertial readings), with no prior
poses. \emph{Output}: the sensor's trajectory --- a pose per time step
--- \emph{and} a map of the environment, estimated jointly, usually
online. The signature circularity is that the map is needed to
localize the sensor while the poses are needed to build the map;
modern visual SLAM systems run bundle adjustment internally as their
refinement back-end.

Where this system sits: the stationary rig cuts the circularity.
Poses come once from calibration against a known target
(\autoref{sec:calib}) and never change, so there is no localization
problem, and ``mapping'' reduces to the model building of
\autoref{sec:models}; frame-by-frame operation involves no estimation
loop at all. Bundle adjustment appears only in its classical polishing
role: jointly refining the rig calibration itself (\code{mv/bundle.h},
\autoref{sec:results}) and registering auxiliary photo collections
(\autoref{sec:photos}) --- where a moving camera does finally enter,
and each photo's pose is estimated against an already-built map: a
miniature, offline SLAM with the hard half already solved.

%----------------------------------------------------------------------
\section{Camera model}
\label{sec:camera}

A camera is a pinhole with intrinsic matrix
\begin{equation}
\K = \begin{pmatrix} f_x & s & c_x \\ 0 & f_y & c_y \\ 0 & 0 & 1 \end{pmatrix},
\label{eq:K}
\end{equation}
rotation $\R$ and translation $\tv$ mapping world points into the camera
frame, $\X_c = \R\X + \tv$; the camera center is $\Cv = -\R^{\!\top}\tv$. A
world point projects to the homogeneous pixel
\begin{equation}
\x \;\simeq\; \PP \begin{pmatrix}\X\\1\end{pmatrix},
\qquad
\PP = \K\,[\,\R \mid \tv\,]
\label{eq:pinhole}
\end{equation}
\cite{HartleyZisserman2004}. Real lenses deviate from \eqref{eq:pinhole}; on
normalized coordinates $(x,y)=(X_c/Z_c,\,Y_c/Z_c)$ with $r^2=x^2{+}y^2$ the
Brown model gives the distorted
\begin{equation}
\begin{aligned}
x_d &= x\,(1 + k_1 r^2 + k_2 r^4 + k_3 r^6) + 2p_1 xy + p_2(r^2 + 2x^2),\\
y_d &= y\,(1 + k_1 r^2 + k_2 r^4 + k_3 r^6) + p_1(r^2 + 2y^2) + 2p_2 xy,
\end{aligned}
\label{eq:distortion}
\end{equation}
and the pixel is $\K(x_d,y_d,1)^{\!\top}$. Back-projection inverts
\eqref{eq:distortion} by fixed-point iteration and returns the ray
$\Cv + s\,\vect d$, $s>0$. \autoref{fig:pinholerig} draws
\eqref{eq:pinhole} for both cameras of the rig at once --- each with its
own frame $(\R_i,\tv_i)$ --- and previews how a pixel in one view
constrains its partner in the other.

\begin{figure}[t]
\centering
\begin{tikzpicture}[font=\scriptsize]
  % ---- working volume in the world frame
  \draw[gray!70] (2.6,2.2) rectangle (5.6,4.6);
  \node[gray!90!black] at (4.1,4.85) {working volume};
  \fill (4.1,3.3) circle (2.5pt) node[above right] {$\X$};
  \draw[->,gray!90!black] (2.0,1.6) -- (2.6,1.6) node[right] {$X_w$};
  \draw[->,gray!90!black] (2.0,1.6) -- (2.0,2.2) node[above] {$Z_w$};
  % ---- camera 1 with its frame
  \fill (0.4,0.5) circle (1.5pt) node[below] {$\Cv_1$};
  \draw[->,gray!90!black] (0.4,0.5) -- (0.96,0.92) node[below right,xshift=-1pt] {$\vect z_1$};
  \draw[->,gray!90!black] (0.4,0.5) -- (0.73,0.06) node[right] {$\vect x_1$};
  \draw[->] (0.4,0.5) -- (4.1,3.3);
  \draw[thick] (1.15,1.82) node[above left,yshift=-2pt] {$\pi_1$} -- (1.87,0.86);
  \fill[blue!70] (1.51,1.34) +(-0.06,-0.06) rectangle +(0.06,0.06);
  \node[blue!70!black] at (1.15,1.15) {$\x_1$};
  % ---- camera 2 with its frame
  \fill (6.9,0.7) circle (1.5pt) node[below] {$\Cv_2$};
  \draw[->,gray!90!black] (6.9,0.7) -- (6.39,1.18) node[below left,xshift=2pt] {$\vect z_2$};
  \draw[->,gray!90!black] (6.9,0.7) -- (6.42,0.19) node[left] {$\vect x_2$};
  \draw[->] (6.9,0.7) -- (4.1,3.3);
  \draw[thick] (6.39,2.00) node[above right,yshift=-2pt] {$\pi_2$} -- (5.57,1.12);
  \fill[blue!70] (5.98,1.56) +(-0.06,-0.06) rectangle +(0.06,0.06);
  \node[blue!70!black] at (6.35,1.35) {$\x_2$};
  % ---- the induced pixel-to-pixel relation
  \draw[<->,dashed] (1.75,1.28) .. controls (3.7,0.35) .. (5.72,1.48);
  \node at (3.75,0.30) {$\x_2^{\!\top}\F\,\x_1=0$ \ (\autoref{sec:epipolar})};
\end{tikzpicture}
\caption{The camera model, drawn for the rig. The volume and the scene
point $\X$ live in the world frame (gray axes $X_w,Z_w$; $Y_w$ out of
the page). Each camera $i$ carries its own frame --- optical axis
$\vect z_i$, image axis $\vect x_i$, $\vect y_i$ out of the page ---
related to the world by $(\R_i,\tv_i)$, and its intrinsic matrix
$\K_i$ turns the intersection of a viewing ray (solid arrow) with the
image plane $\pi_i$ (thick) into a pixel $\x_i$ (blue). Because both
cameras watch the same volume, the two pixels of one scene point are
not independent: knowing $\x_1$ confines $\x_2$ to the image of
$\x_1$'s back-projected ray (dashed relation), which
\autoref{sec:epipolar} makes exact as the fundamental-matrix
constraint.}
\label{fig:pinholerig}
\end{figure}

\begin{example}[projection]
\label{ex:project}
Let $f_x{=}f_y{=}100$, $c_x{=}c_y{=}50$, $s{=}0$, $\R=\mat I$, $\tv=\vect0$,
no distortion, and $\X=(1,2,10)$. Normalized coordinates:
$(x,y)=(1/10,\,2/10)=(0.1,0.2)$. Pixel:
$u=100\cdot0.1+50=60$, $v=100\cdot0.2+50=70$ --- the point lands at
$(60,70)$, cf.~\autoref{ex:homog}.
\end{example}

\begin{example}[camera center]
\label{ex:center}
Let $\R=\R_y(90^\circ)$ from \autoref{ex:rot} and $\tv=(3,0,0)$. Then
$\R^{\!\top}\tv=(0,0,-3)$ and $\Cv=-\R^{\!\top}\tv=(0,0,3)$: the camera sits
three units down the world $z$ axis. Check forward:
$\R\Cv+\tv=(0\!\cdot\!0+0\!\cdot\!0-1\!\cdot\!3,\,0,\,0\!\cdot\!3)+(3,0,0)
=(-3,0,0)+(3,0,0)=\vect0$ --- the center maps to the camera origin, as it
must.
\end{example}

\subsection{The temporal dimension: a camera is also a clock}
\label{sec:timemodel}

Equations \eqref{eq:pinhole}--\eqref{eq:distortion} describe \emph{where}
a camera maps a world point; a complete model must also say \emph{when}.
Each camera runs on its own oscillator, so frame $k$ (and, for a
rolling-shutter sensor, row $r$ within it) is captured at the world time
\begin{equation}
t_i(k, r) \;=\; \phi_i \;+\; k\,T_i \;+\; r\,\tau_i \;+\; \tfrac12 t_e,
\label{eq:clock}
\end{equation}
where $\phi_i$ is the camera's clock offset, $T_i$ its true frame period
(consumer crystals deviate from nominal by 50--100\,ppm, so $T_i$ differs
per camera and $\phi_i$ drifts), $\tau_i$ the row-readout time (zero for
global shutter), and $t_e$ the exposure whose midpoint is the effective
sampling instant. The full camera state is therefore
$(\K,\R,\tv,\,k_{1..5};\;\phi,T,\tau,t_e)$ --- geometry \emph{and}
clock --- and a measurement is not a pixel but a \emph{timestamped}
pixel: a ray with a time attached.

The multi-view machinery of
\autoref{sec:epipolar}--\autoref{sec:stereo} silently assumes
simultaneity: the epipolar constraint \eqref{eq:epiconstraint} and the
triangulation stack \eqref{eq:dlt} relate observations \emph{of the same
world state}. For static scenes the assumption is vacuous; for a point
moving at velocity $\vect v$, a timestamp mismatch $\Delta t$ between the
paired observations displaces one of them by $\vect v\,\Delta t$ ---
producing epipolar residual and phantom depth in exactly the way
quantified in \autoref{ex:sync}. Simultaneity, like zero distortion, is
thus an idealization to be \emph{calibrated}: the clock parameters of
\eqref{eq:clock} are estimable state, not assumptions --- the display
fixture measures them directly through a dedicated temporal calibration
signal (\autoref{sec:tempcalib}) and moving scene content re-estimates
them at run time, with the measurement and correction practice developed
in \autoref{sec:softsync}. When all cameras share a hardware trigger,
$\phi$, $T$ collapse to common values and the temporal model disappears
back into the geometric one --- the trigger is, in this language, a way
of buying \eqref{eq:clock} exactly rather than estimating it.

%----------------------------------------------------------------------
\section{Calibration from planar targets}
\label{sec:calib}

Calibration is part of the system, not an external input. Both supported
targets are \emph{planes with known metric structure}: a printed
checkerboard page (\autoref{sec:printed}) and a controlled computer display
(\autoref{sec:display}). The mathematics is the same for both: several views
of a known plane determine the intrinsics, the distortion, and each view's
pose, by Zhang's method \cite{Zhang2000,SturmMaybank1999} --- the
plane-based approach that displaced earlier calibration against machined
3D targets \cite{Tsai1987}, paper and displays being the targets anyone
can make.

\subsection{The homography of a plane}
\label{sec:homography}

Put the target plane at $Z=0$ in its own frame. Then \eqref{eq:pinhole}
collapses: writing $\R=[\vect r_1\,\vect r_2\,\vect r_3]$ (columns),
\begin{equation}
\x \simeq \K\,[\vect r_1\;\vect r_2\;\tv]\,(X,Y,1)^{\!\top}
\;=\; \Hm\,(X,Y,1)^{\!\top},
\label{eq:planehom}
\end{equation}
a $3\times3$ \emph{homography} with 8 degrees of freedom. Each point
correspondence gives two linear equations in the entries of $\Hm$, so
$n\ge4$ points determine it as a homogeneous least-squares problem
(\autoref{ex:nullvec} at scale $2n\times9$), solved by normalized DLT
(\code{mv\_homography\_dlt}; normalization as in \autoref{sec:numerics}).

\begin{example}[plane homography]
\label{ex:homography}
Frontal camera: $\K$ as in \autoref{ex:project}, $\R=\mat I$,
$\tv=(0,0,2)$. Then $[\vect r_1\,\vect r_2\,\tv]=
\bigl(\begin{smallmatrix}1&0&0\\0&1&0\\0&0&2\end{smallmatrix}\bigr)$ and
\[
\Hm=\K\begin{pmatrix}1&0&0\\0&1&0\\0&0&2\end{pmatrix}
=\begin{pmatrix}100&0&100\\0&100&100\\0&0&2\end{pmatrix}
\simeq\begin{pmatrix}50&0&50\\0&50&50\\0&0&1\end{pmatrix}.
\]
The plane point $(X,Y)=(0.1,0.2)$ maps to
$(50\cdot0.1+50,\;50\cdot0.2+50)=(55,60)$. Check via
\autoref{ex:project}: the 3D point is $(0.1,0.2,2)$, normalized
$(0.05,0.1)$, pixel $(100\cdot0.05+50,\,100\cdot0.1+50)=(55,60)$. \qedhere
\end{example}

\subsection{Zhang's constraints and the closed-form intrinsics}
\label{sec:zhang}

Write $\Hm=[\vect h_1\,\vect h_2\,\vect h_3]$ (columns). From
\eqref{eq:planehom}, $\vect h_1\simeq\K\vect r_1$ and
$\vect h_2\simeq\K\vect r_2$ with the \emph{same} scale. Since
$\vect r_1,\vect r_2$ are orthonormal,
\begin{equation}
\vect h_1^{\!\top}\B\,\vect h_2=0,
\qquad
\vect h_1^{\!\top}\B\,\vect h_1=\vect h_2^{\!\top}\B\,\vect h_2,
\qquad
\B \;\coloneqq\; \K^{-\!\top}\K^{-1},
\label{eq:zhangconstraints}
\end{equation}
i.e.\ each view of the plane contributes two linear equations in the six
independent entries of the symmetric matrix $\B$ (the \emph{image of the
absolute conic}). Three views generically determine $\B$ up to scale; two
suffice if zero skew ($s{=}0 \Leftrightarrow B_{12}{=}0$) is imposed. The
solution is again a null vector (\autoref{ex:nullvec}); $\K$ then follows
from $\B$ in closed form \cite{Zhang2000}, and the pose of each view is
recovered as
\begin{equation}
\lambda=\frac1{\|\K^{-1}\vect h_1\|},\quad
\vect r_1=\lambda\K^{-1}\vect h_1,\quad
\vect r_2=\lambda\K^{-1}\vect h_2,\quad
\vect r_3=\vect r_1\times\vect r_2,\quad
\tv=\lambda\K^{-1}\vect h_3,
\label{eq:extrinsics}
\end{equation}
with $[\vect r_1\,\vect r_2\,\vect r_3]$ snapped to the nearest true
rotation via SVD (noise makes it only approximately orthonormal).

\begin{example}[pose from a homography, by hand]
\label{ex:zhang}
Take the (unscaled) $\Hm$ of \autoref{ex:homography} and the known
$\K$. Then
\[
\K^{-1}=\begin{pmatrix}0.01&0&-0.5\\0&0.01&-0.5\\0&0&1\end{pmatrix},\quad
\K^{-1}\vect h_1=\begin{pmatrix}1\\0\\0\end{pmatrix},\quad
\K^{-1}\vect h_2=\begin{pmatrix}0\\1\\0\end{pmatrix},\quad
\K^{-1}\vect h_3=\begin{pmatrix}0\\0\\2\end{pmatrix}.
\]
Both constraint equations of \eqref{eq:zhangconstraints} hold:
$\vect r_1\cdot\vect r_2 = 0$ and $\|\vect r_1\|=\|\vect r_2\|$. The scale
is $\lambda=1/\|(1,0,0)\|=1$, so \eqref{eq:extrinsics} gives
$\vect r_1=(1,0,0)$, $\vect r_2=(0,1,0)$,
$\vect r_3=\vect r_1\times\vect r_2=(0,0,1)$, $\tv=(0,0,2)$ --- exactly the
frontal pose two metres from the target that we started from.
\end{example}

\subsection{Radial distortion and alternation}
\label{sec:radial}

With $\K$ and poses in hand, the ideal (undistorted) projection $(u,v)$ of
every target point is known, and \eqref{eq:distortion} predicts the observed
$(u_d,v_d)$ linearly in $(k_1,k_2)$:
\begin{equation}
u_d-u=(u-c_x)(k_1r^2+k_2r^4),\qquad
v_d-v=(v-c_y)(k_1r^2+k_2r^4),
\label{eq:radiallin}
\end{equation}
a least-squares problem with two unknowns. Since the homographies were fit
to \emph{distorted} points, the library alternates: fit geometry, estimate
$(k_1,k_2)$, undistort the observations, refit --- a few rounds converge for
moderate distortion \cite{Zhang2000}.

An identifiability caveat worth internalizing: over the small radius range a
letter-page target covers, $r^2$ and $r^4$ are nearly proportional, so
$k_1$ and $k_2$ are individually ill-determined even when their combined
\emph{curve} $k_1r^2{+}k_2r^4$ --- the thing that actually corrects pixels
--- is fit to well below noise (measured in \autoref{sec:results}). Wide
targets, or the full-field display target of \autoref{sec:display}, are the
cure when the individual coefficients matter.

\subsection{Robust fitting from video: view selection}
\label{sec:calibrobust}

Filming the target instead of photographing it changes the statistics of
the input in a way the estimator must respect. A one-minute video yields a
hundred-plus decoded views, but they are not a hundred independent,
well-chosen poses: most frames sit within a degree or two of wherever the
camera happened to dwell, and near-fronto-parallel views are exactly the
singular configuration of plane-based calibration
\cite{SturmMaybank1999}. The linear system of \autoref{sec:zhang} weighs
every view equally, so two hundred rows of noise-dominated near-degenerate
constraints can outvote the handful of tilted frames that actually carry
the focal length --- and a single corrupted decode (a mislabeled corner
lattice) poisons the fit outright. Both failure modes were first observed
on real handheld videos (\autoref{sec:results}), not in simulation:
the full-video fit returned $f_x\approx458$ at 35\,px RMS while a
hand-picked six-frame subset of the \emph{same} video returned
$f_x\approx1599$ at 0.39\,px.

The cure is classical: random sample consensus over \emph{views}
\cite{FischlerBolles1981}. \code{mv\_calib\_planar\_robust}
(\code{mv/calib.h}) fits Zhang's closed form on 64 deterministic random
six-view subsets; each candidate $\K$ is scored by the \emph{median},
across all views, of the per-view reprojection RMS with that view's pose
recovered from its own homography under the candidate $\K$. The median
makes the score indifferent to a minority of corrupted views, while a
degenerate subset produces a garbage $\K$ that fits almost \emph{no} view
and scores itself out. The winner defines inliers (per-view RMS within
$3\times$ the best median); a final full fit with radial alternation runs
on those inliers and is accepted only if it does not worsen the median
score --- so the answer is never worse than the best subset found. The
seed is fixed: the same frames give the same calibration, byte for byte.

The unit test for this estimator (\code{test\_calib\_robust},
\code{tests/test\_mv.c}) reproduces the field failure in simulation ---
36 near-fronto-parallel views, four tilted ones, one corrupted, noise
$\sigma=0.3$\,px --- and demands the robust fit recover the focal length
within 2\% while plain Zhang, on the same data, fails outright. Building
that test surfaced a trap worth recording: ``corrupting'' a view by
\emph{reversing} its point order corrupts nothing, because a checkerboard
lattice is centro-symmetric and reversal is precisely a $180^\circ$
rotation of the plane --- still a perfectly valid homography. It is the
same rotation ambiguity the pattern's M-array exists to resolve
(\autoref{sec:calibspec}). A genuine corruption must be a non-projective
scramble (the test swaps adjacent point pairs).

\subsection{The printed target}
\label{sec:printed}

The standard passive target is a checkerboard printed on a letter/A4 page:
$8\times10$ squares of nominally 24\,mm ($192\times240$\,mm), i.e.\
$7\times9$ inner corners (\code{mv/target.h};
\code{mv\_target\_render\_pgm} emits the printable image --- at 10\,px/mm a
24\,mm square is 240\,px). Printers rescale, so the procedure is
\emph{print, then measure one square} and pass the measured size; only that
measurement makes the calibration metric. Corner \emph{positions} in the
image are currently supplied by the caller (an external detector or manual
marking); a saddle-point corner detector is on the roadmap
(\autoref{sec:roadmap}). Ten to twenty views with strong tilt diversity
($\pm30$--$40^\circ$) condition the focal length well --- near-frontal views
leave it weakly observable, a property visible in the experiment of
\autoref{sec:results}.

\subsection{The display as an active target}
\label{sec:display}

A computer display we control is a better target than paper wherever one is
available: its pixel grid is a plane of exactly known geometry (pixel pitch
from the panel datasheet, or measured once), and because we control what it
shows \emph{when}, correspondence needs no detector at all. The display
plays, synchronized with capture, a sequence of binary stripe patterns that
encode each display pixel's coordinate in \emph{Gray code}
\cite{Inokuchi1984,Geng2011}: bit $b$ of the code is shown as stripes,
followed by its inverse, for $b=0..\lceil\log_2 W\rceil{-}1$, then the same
for the $y$ axis. A camera pixel observing the display reads its bit as
``brighter in the pattern or in the inverse?'' --- a per-pixel comparison
immune to illumination, display brightness and gamma --- and after all
frames has decoded \emph{which display pixel it sees}
(\code{mv/graycode.h}). This yields thousands of dense, uniformly spread
correspondences per view; the same pipeline
(\autoref{sec:homography}--\autoref{sec:radial}) consumes them, with plane
coordinates $=$ display pixel $\times$ pitch. Gray code rather than plain
binary because adjacent stripes differ in exactly one bit: a threshold error
at a stripe boundary displaces the decoded coordinate by at most one pixel
instead of, e.g., $2^{b}$.

This static sweep is operating mode M1 of the calibration subsystem
(\autoref{sec:calibspec}).

\begin{example}[Gray code by hand]
\label{ex:gray}
Encode $v=6$: binary $110$; Gray $=110\oplus011=101$. Decode $g=101$ by
prefix-XOR: $b_2=1$, $b_1=1\oplus0=1$, $b_0=1\oplus0\oplus1=0$ ---
binary $110=6$. Now flip the last Gray bit (a boundary
misread): $g'=100\rightarrow$ binary $111=7$, an error of exactly one
position. In plain binary, flipping the top bit of $110$ gives $010=2$, an
error of four.
\end{example}

\subsection{Design: the rotating display}
\label{sec:rotating}

For an $N$-camera rig (\autoref{sec:scaling}) the planned calibration
fixture is the display of \autoref{sec:display} mounted on a plate rotating
at constant angular velocity $\omega$, so that the screen sweeps past every
camera each revolution. This subsection records the design; implementation
is a roadmap item (\autoref{sec:roadmap}).

\paragraph{What the rotation provides.} Three things, all substantial.
(i) \emph{Tilt diversity per camera}: as the screen sweeps past, every
frame shows the known plane at a different orientation --- exactly the
varied-tilt view set Zhang's method needs, whose absence measurably
degrades focal recovery (\autoref{sec:results}). (ii) \emph{One shared
orbit}: every camera observes the same one-parameter family of poses ---
screen pose $=$ mount offset composed with rotation about a fixed axis at
constant $\omega$. Fitting that orbit (axis, $\omega$, phase, mount;
$\sim$10 parameters) jointly across cameras links \emph{all} of them into
one world frame even if no two ever see the screen simultaneously: the
orbit itself is the common reference object, replacing pairwise shared
views. (iii) \emph{The display is the clock}: the screen broadcasts its
frame index, so every captured image self-reports display time and
cameras synchronize to the rotation model without a hardware trigger.

\paragraph{What the rotation forbids.} The static Gray-code stack of
\autoref{sec:display} requires one camera pixel to observe one display
point across $\sim$20 frames --- impossible on a moving target. The
pattern must therefore be \emph{single-shot self-identifying}: every frame
decodable alone. The design is layered:
\begin{itemize}
  \item a checkerboard-type \emph{saddle-point grid} as the metric layer
        --- saddle locations are unbiased under perspective and under
        (moderate, symmetric) blur, unlike dot centroids, which both
        foreshortening and blur displace;
  \item \emph{identity coding} embedded in the grid (marker bits in
        alternate squares, or an M-array whose every $k\times k$ window is
        unique), so partial and oblique views self-locate --- most frames
        of a sweep are partial;
  \item a \emph{frame-counter strip} along the border, Gray-coded
        \emph{in time} (successive indices differ in one bit,
        cf.~\autoref{ex:gray}), providing the timestamp for the orbit
        model;
  \item a \emph{temporal-calibration layer}: the counter timestamps are
        quantized to the display refresh, so a few dedicated
        \emph{phase patches} toggle black/white every display frame ---
        a camera whose exposure straddles a toggle records an
        intermediate gray level whose value is the sub-refresh phase of
        its shutter (and, as a by-product, its true exposure duration).
        Counter $+$ phase samples give every camera a stream of
        display-clock timestamps, from which each camera's clock
        \emph{offset and rate} are fit --- the display is not only the
        geometric reference but the rig's common clock
        (\autoref{sec:softsync});
  \item \emph{multi-scale structure} (a coarse grid nested with a fine
        one), since one display serves cameras at different distances and
        obliquities; strokes must span ${\ge}3$--$4$ pixels in the worst
        camera at the most oblique usable pose;
  \item \emph{binary black/white only}: LCD gamma and contrast shift
        off-axis, so gray-level codes degrade with viewing angle while
        binary features with per-frame identity do not.
\end{itemize}
Across frames, only the identity layer changes (counter strip; optionally
alternating coarse/fine emphasis) --- pose variation is already supplied by
the rotation. At low $\omega$ the screen moves ${<}1$ pixel between
consecutive frames, so pattern-plus-inverse in consecutive frames remains
usable and restores the per-pixel contrast robustness of
\autoref{sec:display}. The concrete arrangement of all layers on screen
is specified once, in \autoref{fig:layout} (\autoref{sec:calibspec}).

\begin{example}[motion-blur budget]
\label{ex:blur}
Image-space blur during exposure $t_e$ is
$\omega\,r\,f\,t_e/Z$ pixels, with $r$ the lever arm from axis to screen
edge. For the experimental rig's numbers ($f=800$\,px, $Z=4.7$\,m) with
$r=0.25$\,m and $\omega=6$\,rpm $=0.63$\,rad/s: the screen edge moves
$0.63\cdot0.25\cdot800/4.7\approx27$\,px/s, so a $5$\,ms exposure blurs
$0.13$\,px --- comfortably sub-pixel, and between frames at 30\,fps the
screen shifts $0.9$\,px, validating the consecutive-inverse-frame option.
The formula is the design knob linking $\omega$, exposure, and screen
brightness.
\end{example}

\paragraph{One mechanical trap: cant the screen.} A screen standing
parallel to the rotation axis presents orientations that all differ by
rotation about a \emph{single} axis --- a weak/singular family for
plane-based calibration \cite{SturmMaybank1999}, the same disease as the
measured near-frontal weakness. Mounting the screen tilted back
$20$--$30^\circ$ makes its normal sweep a cone instead of a circle,
exercising two rotation axes; this one bracket angle is the most important
mechanical detail of the fixture.

\paragraph{Pipeline.} Gate frames by obliquity (roughly $15$--$55^\circ$
off-normal: frontal frames are uninformative, grazing frames unmatchable);
per-frame homographies from the self-identifying grid; per-camera Zhang
intrinsics from the gated set; joint orbit fit for all extrinsics $+$
axis $+$ $\omega$ $+$ phase --- the natural first customer for the planned
Levenberg--Marquardt stage.

\subsection{Temporal calibration: the display as a clock signal}
\label{sec:tempcalib}

Geometric calibration shows the cameras a known \emph{spatial} structure;
the temporal camera model \eqref{eq:clock} is calibrated the same way,
by showing a known \emph{temporal} structure --- and the requirements
mirror \autoref{sec:display} exactly: the signal must be
\emph{unambiguous} (coded, not periodic-only, or time aliases the way an
uncoded stripe pattern aliases in space), \emph{fine} (containing
transitions faster than the precision sought), and \emph{covering}
(transitions must actually land inside camera exposures often enough to
be sampled). Each clock parameter of \eqref{eq:clock} gets its own layer
of the signal:

\begin{itemize}
  \item \emph{Offset $\phi$, coarse:} the Gray-coded frame counter
        (\autoref{sec:rotating}) --- an absolute, self-delimiting time
        word, readable in any single frame, quantized to the display
        refresh $T_d$.
  \item \emph{Offset $\phi$, fine:} phase patches toggling every display
        frame. A camera exposure of length $t_e$ that straddles a toggle
        integrates part old, part new value: the recorded gray level
        $g\in(0,1)$ \emph{is} the toggle's position inside the exposure,
        refining the timestamp below $T_d$. A display cannot stagger
        toggles \emph{within} a refresh --- every change lands on the
        vsync --- but the panel's own \emph{scanout} staggers them for
        free: rows are updated top-to-bottom across the refresh, so a
        patch at row $y$ toggles at
        $\text{vsync}+ (y/H)\,T_d$. Patches at several vertical
        positions therefore sample several sub-refresh phases (panel
        response time smears the edge; it is itself calibratable from
        the $g$ statistics). Each patch straddles with probability
        $t_e/T_d$ per frame; full coverage is not needed --- the
        camera--display beat sweeps the phase, and a one-minute fit
        collects hundreds of straddle samples. One additional patch
        toggling at half rate disambiguates exposures longer than
        $T_d$.
  \item \emph{Period $T$:} no extra signal needed --- a linear fit of
        decoded display time against camera frame index over seconds
        (\autoref{ex:clockfit}); the inevitable beat between camera and
        display rates is a \emph{feature}, sweeping the sampling phase
        through the display cycle so all phases get observed without any
        genlock.
  \item \emph{Row time $\tau$ (rolling shutter):} a full-screen toggle
        turns the rolling readout into its own measuring instrument ---
        rows exposed before the toggle record one value, rows after it
        the other, so the captured frame shows horizontal \emph{bands}
        whose pitch is the number of rows read per display period:
        $\tau = T_d / (\text{rows per band cycle})$, plus the readout
        direction for free.
  \item \emph{Exposure $t_e$:} recovered jointly with the fine phase ---
        the fraction of frames whose patches straddle, and the
        distribution of recorded gray levels, both depend on $t_e$ alone
        once $\phi, T$ are known.
\end{itemize}

\begin{example}[row time from banding]
\label{ex:banding}
A rolling-shutter camera photographs the display running a full-screen
toggle at $T_d=16.7$\,ms (bright/dark half-periods of $8.33$\,ms). The
captured frame shows bands $289$ rows tall. Then
$\tau = 8.33\,\text{ms}/289 \approx 28.8\,\mu$s per row --- and the full
$1080$-row readout takes $1080\cdot28.8\,\mu\text{s}\approx31$\,ms,
nearly the whole $33$\,ms frame period, which is typical: rolling
sensors read continuously. One captured frame of a blinking screen
measures the parameter that \autoref{ex:rolling} showed corrupting the
moving-target calibration --- and once $\tau$ is known, every row's
timestamp is known, turning the rolling-shutter \emph{bias} into just
more timestamped measurements per \autoref{sec:timemodel}.
\end{example}

\paragraph{What the display cannot fake: pose diversity.} A tempting
idea --- render the pattern pre-warped, as if the plane were rotating,
so nothing need physically move --- fails for a fundamental reason
worth recording. The camera observes the \emph{physical screen plane},
whose pose is fixed: if the displayed pattern is warped by a chosen
homography $\mat{W}_k$, the observed pattern-to-image map is
$\Hm_k = \Hm_s \mat{W}_k$ with $\Hm_s$ the screen's one fixed
physical homography. Since every $\mat{W}_k$ is known, the whole
session carries exactly the information of the single view $\Hm_s$
--- one homography, two Zhang constraints, $\K$ underdetermined
(\autoref{sec:zhang}); and feeding the $\Hm_k$ to the estimator as if
they were real poses forces a solution unrelated to the camera's
$\K$. Calibration information lives in the \emph{physical}
foreshortening of the light-emitting surface, and no amount of
rendering synthesizes it. The practical consolations: (i) Zhang does
not need \emph{pure} rotation --- any pose change serves, so
inadvertent translation while repositioning is harmless; (ii) a laptop
is its own rotation fixture: tilting the \emph{lid} rotates the screen
about the hinge axis, and swiveling the base on the table provides the
second axis --- ten paused lid/swivel combinations are a complete
Zhang set with no careful handling at all.

The same session that calibrates geometry therefore calibrates every
clock parameter: one revolution of the fixture, read by all cameras,
yields $(\K,\R,\tv,k)$ from the corner grid and
$(\phi,T,\tau,t_e)$ from the temporal layers --- the display is the
rig's ruler and its metronome. Run-time maintenance of the clock model
without the display (movers, LED beacon, golden pairs) is the practice
material of \autoref{sec:softsync}; the concrete screen layout, operating
modes, and data contract are concentrated in \autoref{sec:calibspec}.

\subsection{The calibration subsystem: layout, modes, and data contract}
\label{sec:calibspec}

This subsection is the \emph{specification} that
\autoref{sec:homography}--\autoref{sec:tempcalib} justify: the single
contract against which the two calibration programs are written --- the
\emph{display app} (renders the signal) and the \emph{reader module}
(consumes camera frames, emits records). Rationale lives in the sections
cited; nothing here introduces a new idea. Implementation status: the
pattern generator (\code{mv/pattern.h}), a synthetic renderer
(\code{mv/render.h}), and the blind reader (\code{mv/reader.h}) are
built and validated end-to-end in simulation (\autoref{sec:results});
the display app exists as a self-contained web page
(\code{tools/pattern.html}: modes M0/M2/M3, vsync-driven counter with
missed-frame recovery, Retina-aware 1:1 device-pixel rendering,
downloadable \code{DSP} log --- any MacBook or iPhone becomes the
fixture display; its embedded M-array is checked identical to the C
constant), leaving the record-emitting wrapper, orbit fit, and clock
fit.

\paragraph{Screen layout (\autoref{fig:layout}).} One composite layout
serves all single-shot work. Regions, sized by the rules already
derived: white quiet border; a \emph{version block} (static Gray-coded
integer) naming the pattern-spec revision, so a reader never parses a
layout it was not built for; the \emph{frame-counter strip}
(\autoref{sec:rotating}), Gray-coded, changing every display frame;
\emph{phase patches} at several vertical positions (sub-refresh stagger
via scanout, \autoref{sec:tempcalib}); the central \emph{saddle grid}
with M-array identity marks in the squares (\autoref{sec:rotating}),
strokes $\ge3$--$4$\,px in the worst camera at the worst usable
obliquity (\autoref{sec:resolution}); and one \emph{nested fine grid}
region for near/high-resolution cameras (\autoref{sec:rotating},
multi-scale).

\paragraph{Pattern specification v1 (reference display
$1920\times1080$).} All coordinates in display pixels, origin top-left;
the metric scale is the panel pixel pitch from the session manifest.
Scaling to another resolution scales all entries proportionally, and
bumps the version.

\begin{table}[ht]
\centering
\scriptsize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{l l l}
\toprule
Region & Geometry (px) & Content \\
\midrule
quiet border & 32 all around & white \\
version block & origin (32,32), 10 cells of $48\times48$ & marker $[1,0]$ $+$ 8 Gray bits, static \\
nested fine grid & origin (560,32), $3\times3$ cells of 40 & plain checkerboard \\
phase patches P1--P4 & $96\times96$ at (912,32), (1792,32), (32,472), (1792,472) & toggle every frame; scanout phases \\
phase patch P5 & $96\times96$ at (32,860) & toggles every 2nd frame \\
saddle grid & origin (192,160), $19\times10$ cells of 80 & checkerboard $+$ mark dots \\
M-array marks & $24\times24$ dot at each cell center & bit $=$ dot present (contrast-inverted) \\
corner lattice & $18\times9=162$ corners & id $= 18j+i$, origin nearest version block \\
counter strip & origin (192,984), 24 cells of $64\times64$ & $[1,0]$ $+$ 20 Gray bits $+$ parity, $\overline{\text{parity}}$ \\
\bottomrule
\end{tabular}
\caption{Pattern spec v1. A 20-bit counter at 60\,Hz wraps in
$\approx4.9$\,h.}
\label{tab:patternspec}
\end{table}

\paragraph{M-array construction.} One bit per grid square
($19\times10=190$ bits): a black center dot on white squares, a white
dot on black squares, so every square carries a bit and no dot
approaches a saddle corner ($\ge28$\,px clearance). The window size
must be $4\times4$: with $3\times3$ windows the $136$ windows
$\times\,4$ rotations demand $544$ distinct 9-bit codes and only $512$
exist --- pigeonhole forbids it. At $4\times4$, the $112$ windows
$\times\,4$ rotations need $448$ of $2^{16}$ codes, ample slack. The
array is found once by seeded search with greedy repair (seed 20260803,
solved in 30 steps) and shipped as a constant table in \code{libmv}
(\code{src/pattern.c}), with the exhaustively verified properties: all
$448$ rotated window codes are pairwise distinct with Hamming distance
$\ge2$. The rotation clause is what resolves the checkerboard's 4-fold
orientation ambiguity: any single decoded window fixes the global
orientation, after which corner ids are assigned in the pattern frame.

\paragraph{Reader algorithms.} Fixed choices, so two implementations
decode identically:
\emph{binarization} --- local mean over a $31\times31$ box (integral
image) with $\pm$hysteresis; black/white radiometric references taken
per region from the quiet border and the nearest grid cells.
\emph{Saddle detection} --- correlation with a 4-phase saddle template
bank ($r=5$\,px), candidates by non-maximum suppression, sub-pixel by
2D quadratic fit on the $3\times3$ response neighborhood (the same
parabola rule as the stereo matcher).
\emph{Grid assembly} --- seed at the strongest saddle, estimate a local
homography from its four lattice neighbors, grow breadth-first,
predicting each next corner through the local homography and accepting
within $0.3$ cell.
\emph{Decoding} --- sample mark presence at cell centers through the
local homography against the local black/white references; look up each
$4\times4$ window (and its rotations) in the shipped table; majority
vote fixes position and orientation.
\emph{Counter/patches} --- sample cell and patch centers the same way;
counter valid iff markers and both parity cells check; its
\code{conf} field is the minimum sampling margin
$\min_i |g_i - \tfrac12| \cdot 2 \in [0,1]$. \code{PHS} records are
emitted only for $0.15<g<0.85$ (certain straddle).
\emph{Banding} (mode M3) --- column-median intensity profile per frame;
transition rows at gradient maxima; \code{BND} pitch $=$ median
inter-transition spacing.

\begin{figure}[ht]
\centering
\begin{tikzpicture}[x=9mm, y=9mm, font=\scriptsize]
  % display outline and quiet border
  \draw[very thick] (0,0) rectangle (12,6.75);
  \draw[thin] (0.4,0.4) rectangle (11.6,6.35);
  % version block
  \fill[black!75] (0.6,5.6) rectangle (1.8,6.15);
  \node[anchor=west, font=\tiny] at (0.5,6.55) {version block (static Gray code)};
  % phase patches
  \fill[black!45] (10.9,5.6) rectangle (11.45,6.15);
  \fill[black!45] (0.6,2.9) rectangle (1.15,3.45);
  \fill[black!45] (10.9,2.9) rectangle (11.45,3.45);
  \fill[black!45] (5.7,5.75) rectangle (6.25,6.3);
  \node[anchor=east, font=\tiny] at (11.5,6.55) {phase patches (scanout-staggered $+$ one half-rate)};
  % counter strip
  \foreach \i in {0,...,21} {
    \pgfmathtruncatemacro{\flag}{(mod(\i,3)==0 || mod(\i,7)==0) ? 1 : 0}
    \ifnum\flag>0
      \fill[black] (0.6+\i*0.5,0.55) rectangle (1.1+\i*0.5,1.0);
    \else
      \draw[thin] (0.6+\i*0.5,0.55) rectangle (1.1+\i*0.5,1.0);
    \fi
  }
  \node[anchor=west, font=\tiny] at (0.5,0.25) {frame-counter strip (Gray, changes every display frame)};
  % saddle grid
  \foreach \r in {0,...,3} \foreach \c in {0,...,7} {
    \pgfmathtruncatemacro{\flag}{mod(\r+\c,2)}
    \ifnum\flag=0
      \fill[black!85] (2.0+\c,1.5+\r) rectangle (3.0+\c,2.5+\r);
    \fi
  }
  % m-array marks centered in white squares
  \foreach \x/\y in {3.5/2.0, 5.5/2.0, 4.5/3.0, 8.5/3.0, 7.5/4.0, 6.5/5.0} {
    \fill[black!85] (\x-0.14,\y-0.14) rectangle (\x+0.14,\y+0.14);
  }
  \node[font=\tiny] at (6,1.2) {saddle grid $+$ M-array marks (static)};
  % nested fine grid (top band, clear of the coarse grid)
  \foreach \r in {0,...,3} \foreach \c in {0,...,3} {
    \pgfmathtruncatemacro{\flag}{mod(\r+\c,2)}
    \ifnum\flag=0
      \fill[black!85] (2.6+\c*0.18,5.55+\r*0.18) rectangle (2.78+\c*0.18,5.73+\r*0.18);
    \fi
  }
  \draw[thin] (2.6,5.55) rectangle (3.32,6.27);
  \node[anchor=west, font=\tiny] at (3.45,5.9) {nested fine grid};
\end{tikzpicture}
\caption{Composite screen layout (mode M2; schematic, not to scale).
Geometric layers are static; only the counter strip and phase patches
change per display frame.}
\label{fig:layout}
\end{figure}

\paragraph{Display-app modes.} The app is a mode machine; a calibration
session is a scripted sequence of modes.

\begin{table}[ht]
\centering
\scriptsize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{l l l l l}
\toprule
Mode & Screen shows & Plate & Calibrates & Rationale \\
\midrule
M0 & flat gray ladder (0--100\%) & any & radiometric transfer, flat field & \S\ref{sec:radiometry} \\
M1 & Gray-code stripe sweep $+$ inverses & parked & dense correspondences $\to$ $\K$, pose & \S\ref{sec:display} \\
M2 & composite layout (\autoref{fig:layout}) & $\omega$ const. & $\K,\R,\tv,k$; orbit; $\phi,T$ & \S\ref{sec:rotating}, \S\ref{sec:tempcalib} \\
M3 & full-screen toggle at $T_d$ & parked & $\tau$ (banding), $t_e$ & Ex.~\ref{ex:banding} \\
\bottomrule
\end{tabular}
\caption{Display-app operating modes.}
\label{tab:modes}
\end{table}

\paragraph{Reader output: the record schema.} The reader consumes raw
frames per camera and emits ASCII records, one per line --- the same
recordable/replayable oracle pattern as the inference sidecar
(\autoref{sec:sidecar}); recorded sessions are the test fixtures.

\begin{table}[ht]
\centering
\footnotesize
\begin{tabular}{l l l l}
\toprule
Record & Fields & Emitted & Consumed by \\
\midrule
\code{VER} & spec version, display px, pitch mm, $T_d$ & session start & reader/estimator config check \\
\code{FRM} & cam, frame $k$, arrival time & every frame & bookkeeping, drift diagnostics \\
\code{CTR} & cam, $k$, counter value, confidence & when strip decodes & clock fit ($\phi$, $T$); orbit phase \\
\code{PHS} & cam, $k$, patch id, gray level & straddled patches & clock fit (fine $\phi$); $t_e$ \\
\code{CRN} & cam, $k$, corner id, $u$, $v$ & per detected corner & homographies $\to$ Zhang, orbit fit \\
\code{BND} & cam, $k$, band pitch, direction & mode M3 & row time $\tau$ \\
\code{DSP} & display frame $k$, host time & display app, per frame & clock cross-check \\
\bottomrule
\end{tabular}
\caption{Calibration record schema (all ASCII, one record per line).}
\label{tab:records}
\end{table}

Field-level grammar: whitespace-separated \code{key=value} after the
record tag; times are seconds as decimal doubles on the host
\code{CLOCK\_MONOTONIC}; image coordinates are \emph{raw distorted}
pixels (undistortion is the estimators' job); corner \code{id} is the
pattern-frame lattice index of \autoref{tab:patternspec}. Example
lines:
\begin{quote}\ttfamily\footnotesize
VER spec=1 w=1920 h=1080 pitch\_mm=0.2745 Td\_ms=16.667\\
FRM cam=0 k=1234 t=5182.30412\\
CTR cam=0 k=1234 count=48213 conf=0.94\\
PHS cam=0 k=1234 patch=2 g=0.371\\
CRN cam=0 k=1234 id=57 u=812.431 v=402.117\\
BND cam=0 k=87 pitch=289.3 dir=down\\
DSP k=309882 t=5182.29561
\end{quote}

\paragraph{Session manifest and capture interface.} Each session
directory holds a manifest (\code{key=value} text): rig id; per camera
--- id, serial, driver, nominal fps, lens focal; display --- resolution,
pixel pitch, $T_d$; plate --- nominal $\omega$, cant angle; all RNG
seeds. The capture layer, shared verbatim with the inference sidecar
(\autoref{sec:sidecar}), is a three-call C contract:
\code{open(device)}, blocking \code{get(frame)}, \code{close()}, where a
frame is 8-bit mono pixels $+$ width/height/stride $+$ camera id $+$
driver sequence number $+$ arrival time (\code{CLOCK\_MONOTONIC} at
driver dequeue --- the timestamp whose limitations
\autoref{sec:softsync} exists to overcome). Recorded streams are PGM
sequences plus an index of \code{FRM} lines; a recorded session replays
bit-identically through reader and estimators.

\paragraph{Estimator definitions.} \emph{Orbit fit}: unknowns are the
plate axis (unit vector, 2 dof), a point on the axis (2 dof), $\omega$,
phase $\phi_0$, the fixed display-to-plate mount (6 dof), and each
camera's pose (6 dof each); the display pose at time $t$ is the mount
composed with rotation about the axis by $\omega t+\phi_0$, and the
cost is total corner reprojection error over all cameras and gated
frames, Huber-robustified at $1$\,px. Initialization: per-frame Zhang
extrinsics (\autoref{sec:zhang}) give a display-center trajectory per
camera; its plane normal initializes the axis, its circle fit
(\autoref{sec:occupancy}'s known machinery) the center, and the
\code{CTR} timeline $\omega,\phi_0$. \emph{Clock fit}: per camera,
Theil--Sen (deterministic median-of-slopes) on
(\code{CTR.count}$\cdot T_d$) vs frame index rejects counter
misdecodes, a 3-MAD gate then admits samples to least squares for
$(\phi_i, T_i)$, and \code{PHS} straddle samples jointly refine
$\phi_i$ and $t_e$ against the scanout-phase model of
\autoref{sec:tempcalib}. Acceptance bands for both are the
\autoref{sec:statrules} machinery evaluated at the rig's \emph{measured}
noise (\autoref{sec:radiometry}), not at the synthetic
$\sigma=0.3$\,px.

\paragraph{Display-app guarantees.} Fullscreen exclusive; compositor
scaling disabled and verified optically (a single-pixel checker in the
quiet border must alias to gray in any camera --- if it resolves, the
path is not 1:1); all emitted values pure 0 or 255 (gamma-immune);
counter incremented in the vsync callback, and after a missed vsync
advanced by the number of elapsed refreshes; the app logs its own
\code{DSP} record per displayed frame as the display-side ground truth.
The pattern generator itself lives in \code{libmv} (emitting frames the
app only blits), so every layer of \autoref{tab:patternspec} is
unit-testable against the reader without a display.

\paragraph{Session choreography and acceptance.} A session is:
M0 ladder $\to$ M3 ($\sim$30 frames) $\to$ M2 for $N$ revolutions
($\to$ M1 per camera only if the plate can park facing it); then the
estimator chain runs: per-camera Zhang on obliquity-gated M2 frames
$\to$ joint orbit fit $\to$ clock fit $\to$ radial alternation. The
session is \emph{accepted} only if the standing statistics land in
their bands (\autoref{sec:statrules}): reprojection T5 ratio within its
documented band; clock-fit residuals within the \autoref{ex:clockfit}
budget; banding-derived $\tau$ consistent across repeats; and the
resulting analytic $\F$ stored as the baseline for the run-time drift
monitor (\autoref{sec:epipolar}). Roadmap items 1--2
(\autoref{sec:roadmap}) are precisely the implementation of this
subsection.

\subsection{The coarse tier (pattern spec v2)}
\label{sec:coarsetier}

The fine layout above assumes the camera can resolve a 24\,px identity
dot; the round-four field experiment (\autoref{sec:results}) measured
where that assumption dies --- about $0.6$\,m for a 720p webcam ---
and deployment cameras watch rooms, not desktops. The coarse tier is
the same display speaking more slowly: a $6\times3$ checkerboard of
$270$\,px cells (10 inner corners), an asymmetric L of three $90$\,px
orientation marks whose cell set maps to a disjoint set under every
non-identity rotation, and a 12-cell counter strip ([1,0] sync, 8 Gray
bits, parity, inverted parity) rendered as \emph{isolated} $90$\,px
squares. Every feature is $\sim$$3.4\times$ the fine tier's, so
\eqref{eq:patrange} stretches by the same factor: $\sim$$2$\,m for the
webcam, $\sim$$3.7$\,m for a phone. The price is density (10
correspondences per frame instead of 162) and counter width (8 bits
wraps every $256$ refreshes $\approx4.3$\,s; consumers disambiguate by
capture-order proximity, as \code{tools/rigcalib.c} does, or by
rig-rigidity consistency). Generator: \code{mv\_pattern2\_render};
reader: \code{mv\_read\_coarse} (escalates over $2\times$/$4\times$
downsampling for close views); display app: mode M4.

Two implementation lessons, both found by the sim harness before any
field time was spent. First, \emph{gutter saddles}: two dark squares
separated by a thin light gap form a genuine saddle point between them
--- black left and right, white above and below --- indistinguishable
locally from a checkerboard corner; a counter strip of adjacent solid
cells decorates the image with a full row of them. Isolating the
squares only moves the saddles into the gutters; the working cure is
geometric placement (the strip sits $1.5$ lattice steps below the
bottom corner row, so integer-step lattice growth cannot reach it)
\emph{plus} tolerance in the reader. Second, \emph{keystone defeats
layout spacing}: under a $20^\circ$ tilt the projective foreshortening
moves the strip from $1.5$ to $\sim$$1.35$ image-steps --- back inside
the capture gate --- so no static layout margin survives all poses.
The reader therefore does not demand a clean $5\times2$ bounding box:
it grows generously, enumerates candidate $5\times2$ windows inside
the grown lattice, and lets the exact validator (all 18 cell colors,
all 18 mark states, sync and double parity) decide. The validation
round-trip test (\code{test\_pattern\_coarse}) runs at exactly the
geometry that defeated the fine tier in the field --- a 720p camera
with the pattern $160$\,px wide --- plus a $180^\circ$-rolled,
$20^\circ$-tilted, noisy view, and demands all ten corners, correct
ids, sub-pixel RMS, and an exact counter.

%----------------------------------------------------------------------
\section{Two-view epipolar geometry}
\label{sec:epipolar}

\begin{figure}[ht]
\centering
\begin{tikzpicture}[scale=1.0]
  \coordinate (C1) at (0,0);
  \coordinate (C2) at (6,0);
  \coordinate (X)  at (3,3.2);
  \fill[blue!8] (C1) -- (X) -- (C2) -- cycle;
  \draw[thick] (0.6,0.2) -- (1.6,1.4) node[midway,below right,font=\small] {$\pi_1$};
  \draw[thick] (5.4,0.2) -- (4.4,1.4) node[midway,below left,font=\small] {$\pi_2$};
  \draw[->] (C1) -- (X) node[above,font=\small] {$\X$};
  \draw[->] (C2) -- (X);
  \draw[dashed] (C1) -- (C2) node[midway,below,font=\small] {baseline};
  \fill (C1) circle (1.5pt) node[below left,font=\small] {$\Cv_1$};
  \fill (C2) circle (1.5pt) node[below right,font=\small] {$\Cv_2$};
  \fill (X) circle (1.5pt);
  \fill (1.07,0.76) circle (1.2pt) node[above left,font=\small] {$\x_1$};
  \fill (4.93,0.76) circle (1.2pt) node[above right,font=\small] {$\x_2$};
\end{tikzpicture}
\caption{Epipolar geometry: the camera centers $\Cv_1,\Cv_2$ and the scene
point $\X$ span the epipolar plane (shaded); its intersections with the image
planes $\pi_1,\pi_2$ are the corresponding epipolar lines on which $\x_1$ and
$\x_2$ must lie.}
\label{fig:epipolar}
\end{figure}

Corresponding pixels of the two views are linked by the fundamental matrix
$\F$ through the epipolar constraint
\begin{equation}
\x_2^{\!\top}\,\F\,\x_1 = 0,
\label{eq:epiconstraint}
\end{equation}
with $\F$ of rank 2; the line $\boldsymbol{\ell}_2 = \F\x_1$ is the locus in
image 2 of possible matches of $\x_1$ (\autoref{fig:epipolar}). For
\emph{calibrated} views the essential matrix
$\E = \K_2^{\!\top}\F\,\K_1 = [\tv]_\times\R$ carries the same constraint in
normalized coordinates \cite{LonguetHiggins1981}; note the reappearance of
\eqref{eq:skew}.

Because our rig is calibrated, $\F$ is available in closed form,
\begin{equation}
\F = [\,e_2\,]_\times\, \PP_2\, \PP_1^{+},
\qquad e_2 = \PP_2\begin{pmatrix}\Cv_1\\1\end{pmatrix},
\label{eq:fundamental}
\end{equation}
where $\PP_1^{+}$ is the pseudo-inverse of $\PP_1$
\cite{HartleyZisserman2004}. The system computes \eqref{eq:fundamental} once
at start-up and uses it for \emph{correspondence gating}: any candidate match
whose symmetric epipolar distance exceeds a threshold is rejected before
triangulation.

Independently, $\F$ can be \emph{estimated} from $n\ge 8$ correspondences by
the normalized 8-point algorithm \cite{Hartley1997}: each pair turns
\eqref{eq:epiconstraint} into one linear equation in the nine entries of
$\F$ --- again the homogeneous least-squares pattern of
\autoref{ex:nullvec} --- followed by rank-2 enforcement via SVD. In this
system the estimator serves two purposes: (i) \emph{health monitoring} ---
if the estimate from live correspondences drifts from the calibrated
\eqref{eq:fundamental}, the rig has been bumped or the calibration is stale;
(ii) bootstrapping geometry for \emph{uncalibrated} auxiliary photos
(\autoref{sec:extensions}). On contaminated correspondence sets the
estimator is wrapped in a RANSAC loop \cite{FischlerBolles1981} (planned
with the feature layer).

\begin{example}[essential matrix of a rectified rig]
\label{ex:essential}
Two identical cameras, the second displaced one unit along $x$:
$\R=\mat I$, $\Cv_2=(1,0,0)$, so $\tv=-\R\Cv_2=(-1,0,0)$. By
\autoref{ex:skew},
\[
\E=[\tv]_\times\R=
\begin{pmatrix}0&0&0\\0&0&1\\0&-1&0\end{pmatrix},
\qquad
\x_2^{\!\top}\E\,\x_1
=(x_2,y_2,1)\begin{pmatrix}0\\1\\-y_1\end{pmatrix}
=y_2-y_1 .
\]
The epipolar constraint degenerates to $y_2=y_1$: corresponding points lie
on the \emph{same image row}. This is precisely the situation rectification
(\autoref{sec:stereo}) manufactures for arbitrary rigs.
\end{example}

\subsection{Plane transfer and parallax: why one matrix cannot map view
to view}
\label{sec:parallax}

The epipolar constraint answers ``where \emph{could} the match be'' (a
line); it is natural to ask for more --- a formula sending each pixel of
view 1 to its partner in view 2. No such 2D map exists, and the way it
fails is worth quantifying, because the failure \emph{is} the depth
signal.

The best available single matrix is the plane-induced homography of
\eqref{eq:planehom}: pick a reference plane --- say the fronto-parallel
plane at depth $d$ in camera 1 --- and
\begin{equation}
\mat H \;=\; \K_2\!\left(\R_{21} + \tv_{21}\,\vect n^{\!\top}\!/d\right)\K_1^{-1}
\label{eq:planetransfer}
\end{equation}
transfers \emph{exactly} the pixels of scene points lying on that plane.
A point at true depth $Z\neq d$ instead lands displaced from
$\mat H\x_1$ --- along the epipolar line of $\x_1$, because both the
transferred and the true pixel must lie on it --- by the \emph{parallax}
\begin{equation}
\delta \;\approx\; f\,B\,\Bigl|\frac1Z-\frac1d\Bigr|,
\label{eq:parallax}
\end{equation}
with $B$ the baseline and $f$ in pixels (the small-angle form; it is the
disparity law \eqref{eq:depth} measured relative to the plane instead of
relative to infinity \cite{HartleyZisserman2004}). Two readings of
\eqref{eq:parallax}: transfer error grows with the \emph{depth relief}
of the scene about the chosen plane; and shrinking the baseline drives
the two views toward a single viewpoint, where the homography becomes
exact and, by the same token, depth becomes unrecoverable ---
\eqref{eq:parallax} and the depth-error law \eqref{eq:deptherr} are the
two faces of one trade.

\begin{example}[why the companion's diff panel glows]
\label{ex:parallax}
The interactive companion (\autoref{sec:webcompanion}) warps camera 1's
display into camera 2's frame by \eqref{eq:planetransfer} and shows the
per-pixel $|$actual$-$predicted$|$; in its default scene
(\code{?scene=default}) the difference is strikingly large, and
\eqref{eq:parallax} says it must be. The default rig is strongly
converged: $B\approx3.6$\,m at $d\approx4.2$\,m with $f=52$\,px on a
64-pixel-wide display. A molecule atom $0.7$\,m in front of the
reference plane sits at $Z=3.5$\,m, so
$\delta \approx 52\cdot3.6\cdot(1/3.5-1/4.2)\approx9$\,px --- about
$14\%$ of the display width. Two amplifiers make the \emph{panel} read
even larger than the geometry: the scene is drawn in thin strokes
(1--2\,px bonds and small discs), and a thin stroke displaced by more
than its own width overlaps its counterpart nowhere, so the absolute
difference saturates rather than growing gracefully; and the warp
resamples an already-coarse $64\times48$ grid by nearest neighbor,
adding up to a pixel of edge misregistration everywhere. Watching the
panel while shrinking the baseline (\code{?scene=narrow}) shows the
whole effect collapse, exactly as \eqref{eq:parallax} predicts.
\end{example}

%----------------------------------------------------------------------
\section{Triangulation}
\label{sec:triangulation}

\paragraph{Input and output.} The input is $N\ge2$ pixel observations of
\emph{one} physical point --- one pixel per viewing camera, already
matched to each other (by the calibration pattern reader, by feature
matching, or one disparity of the dense stereo of \autoref{sec:stereo})
--- together with the calibrated projection matrices of those cameras.
The output is a single 3D point $\X$, in metres in the world frame,
that best explains the observed pixels, accompanied by its RMS
reprojection error as a per-point quality report. This is the moment 2D
becomes 3D: everything before this section produces matched pixels,
everything after consumes 3D points. (The companion,
\autoref{sec:webcompanion}, runs this section's DLT on the selected
atom's quantized pixels and reports the millimetre error live.)

Given observations $\x^{(v)} = (u_v, w_v)$ of the same point in $N\ge2$ views
with projection matrices $\PP^{(v)}$, each view contributes two linear
equations obtained by eliminating the projective scale from
\eqref{eq:pinhole}:
\begin{equation}
\begin{pmatrix}
u_v\,\PP^{(v)}_{3,:} - \PP^{(v)}_{1,:}\\[2pt]
w_v\,\PP^{(v)}_{3,:} - \PP^{(v)}_{2,:}
\end{pmatrix}
\begin{pmatrix}\X\\1\end{pmatrix} = \vect{0},
\label{eq:dlt}
\end{equation}
and stacking all views gives a $2N\times4$ homogeneous system solved, once
more, as in \autoref{ex:nullvec} --- the DLT triangulation
\cite{HartleyZisserman2004}. The implementation accepts any $N\ge2$, so
still photographs registered into the rig frame (\autoref{sec:extensions})
simply extend the same call; what additional views and additional pixels
buy, quantitatively, is analyzed in \autoref{sec:scaling}. DLT minimizes an algebraic residual; the
reprojection-optimal two-view method of Hartley--Sturm
\cite{HartleySturm1997} is a planned refinement, worthwhile mainly for
wide-baseline auxiliary views. The library reports the RMS reprojection
error of every triangulated point --- the quantity an eventual bundle
adjustment would minimize \cite{Triggs2000}.

\begin{example}[triangulation by similar triangles]
\label{ex:triangulate}
Rig of \autoref{ex:essential} with $\K$ of \autoref{ex:project}
($f=100$, $c=50$) and baseline $B=0.2$: camera 2 at $(0.2,0,0)$. Scene
point $\X=(0.1,0,2)$.
View 1: $(x,y)=(0.05,0)$, pixel $u_1=100\cdot0.05+50=55$.
View 2: camera coordinates $(0.1-0.2,\,0,\,2)=(-0.1,0,2)$, so
$u_2=100\cdot(-0.05)+50=45$.
Disparity $d=u_1-u_2=10$, and \eqref{eq:depth} gives
$Z = fB/d = 100\cdot0.2/10 = 2$~\checkmark. The lateral coordinate follows
from view 1: $X = Z\,(u_1-c_x)/f = 2\cdot5/100 = 0.1$~\checkmark --- the
full point recovered by hand.
\end{example}

%----------------------------------------------------------------------
\section{Rectification and dense stereo}
\label{sec:stereo}

\paragraph{Input and output.} The input is a synchronized pair of raw
frames, one per camera, plus the fixed rig calibration. The output is a
\emph{depth map}: for every pixel of the rectified reference image
(minus the pixels where matching fails), the metric distance to the
surface seen there --- a dense grid of 3D samples where
\autoref{sec:triangulation} gave one point per matched feature. The
section reaches it in two steps, both visible in
\autoref{fig:rectify} and \autoref{fig:blockmatch}: \emph{rectification}
warps both images so that corresponding pixels are guaranteed to share
an image row (computed once per rig), and \emph{block matching} then
searches, for each left pixel, only along that one right row; the column
offset of the best match (the \emph{disparity}) converts to metric depth
via \eqref{eq:depth}.

For dense per-pixel depth the pair is first \emph{rectified}: both cameras
are rotated (synthetically, about their centers) onto a common orientation
whose $x$ axis is the baseline, making the geometry of
\autoref{ex:essential} true by construction --- epipolar lines become
identical image rows and correspondence search collapses to 1D. We use the
compact algorithm of Fusiello, Trucco and Verri \cite{Fusiello2000}: the new
rotation has rows $(\vect{x},\vect{y},\vect{z})$ with $\vect{x}$ the unit
baseline, $\vect{y}=\vect{k}\times\vect{x}$ ($\vect{k}$ the old optical axis
of camera 1) and $\vect{z}=\vect{x}\times\vect{y}$; both views share the
averaged intrinsics with zero skew, and the pixel maps are the homographies
$\mat{H}_i = \K_{\mathrm{new}}\R_{\mathrm{new}}\R_i^{\!\top}\K_i^{-1}$
(homographies again --- cf.~\eqref{eq:planehom}, here induced by pure
rotation instead of a scene plane). Since the rig is stationary,
$\mat{H}_1,\mat{H}_2$ are computed once and each video frame is warped by
table lookup. \autoref{fig:rectify} shows the effect: the fans of
slanted epipolar lines become one shared set of horizontal rows.

\begin{figure}[t]
\centering
\begin{tikzpicture}[font=\scriptsize]
  % ---- raw pair: epipolar lines slanted, fanning toward the epipoles
  \node at (1.5,2.5) {raw left};
  \node at (6.1,2.5) {raw right};
  \draw (0,0) rectangle (3,2.2);
  \draw (4.6,0) rectangle (7.6,2.2);
  \draw[blue!60] (0,0.03) -- (3,0.50);
  \draw[blue!60] (0,0.74) -- (3,1.00);
  \draw[blue!60] (0,1.46) -- (3,1.50);
  \draw[blue!60] (4.6,0.50) -- (7.6,0.03);
  \draw[blue!60] (4.6,1.00) -- (7.6,0.74);
  \draw[blue!60] (4.6,1.50) -- (7.6,1.46);
  \fill (1.9,0.90) circle (1.5pt) node[above,yshift=1pt] {$\x_1$};
  \fill (5.6,0.91) circle (1.5pt) node[above,yshift=1pt] {$\x_2$};
  % ---- warps
  \draw[->,dashed] (1.5,-0.25) -- (1.5,-0.95) node[midway,right] {$\mat H_1$};
  \draw[->,dashed] (6.1,-0.25) -- (6.1,-0.95) node[midway,right] {$\mat H_2$};
  % ---- rectified pair: rows aligned across both images
  \draw (0,-3.4) rectangle (3,-1.2);
  \draw (4.6,-3.4) rectangle (7.6,-1.2);
  \foreach \yy in {-2.9,-2.3,-1.7}{
    \draw[blue!60] (0,\yy) -- (3,\yy);
    \draw[blue!60] (4.6,\yy) -- (7.6,\yy);}
  \draw[densely dotted] (3,-2.3) -- (4.6,-2.3) node[midway,above] {same row};
  \fill (1.9,-2.3) circle (1.5pt) node[above,yshift=1pt] {$\x_1'$};
  \fill (6.0,-2.3) circle (1.5pt) node[above,yshift=1pt] {$\x_2'$};
  \draw[densely dotted] (6.5,-3.4) -- (6.5,-2.3);
  \draw[->] (6.5,-3.1) -- (6.0,-3.1) node[midway,above] {$d$};
  \node at (1.5,-3.8) {rectified left};
  \node at (6.1,-3.8) {rectified right};
\end{tikzpicture}
\caption{Rectification. Top: in the raw pair the epipolar lines (blue)
of \autoref{fig:epipolar} are slanted and unaligned, so the match of a
pixel $\x_1$ must be sought along a slanted line. Bottom: the
precomputed homographies $\mat H_1,\mat H_2$ warp each image so that
epipolar lines become horizontal \emph{and} row-aligned between the
views; the same scene point now lands on the same row in both images
(dotted), displaced only horizontally by the disparity $d$ (dotted
vertical: the column $\x_1'$ would occupy at infinite depth).}
\label{fig:rectify}
\end{figure}

On the rectified pair, dense correspondence is found by block matching
(\autoref{fig:blockmatch}): for
each left pixel the sum of absolute differences (SAD) over a $(2w{+}1)^2$
window is minimized along the corresponding right row over a disparity
range, followed by sub-pixel refinement by parabolic interpolation of the
cost around the minimum.

\begin{figure}[t]
\centering
\begin{tikzpicture}[font=\scriptsize]
  % ---- rectified pair with reference and candidate windows
  \node at (1.5,2.5) {rectified left};
  \node at (6.1,2.5) {rectified right};
  \draw (0,0) rectangle (3,2.2);
  \draw (4.6,0) rectangle (7.6,2.2);
  \draw[blue!60] (4.6,1.2) -- (7.6,1.2);
  \draw[fill=blue!8] (1.7,1.0) rectangle (2.1,1.4);
  \fill (1.9,1.2) circle (1.5pt);
  \node[align=center] at (1.35,0.5) {reference\\window};
  \draw[->,shorten >=2pt] (1.35,0.75) -- (1.8,1.02);
  \draw[blue!40] (5.0,1.0) rectangle (5.4,1.4);
  \draw[blue!40] (6.6,1.0) rectangle (7.0,1.4);
  \draw[fill=blue!8] (5.8,1.0) rectangle (6.2,1.4);
  \fill (6.0,1.2) circle (1.5pt);
  \draw[densely dotted] (6.5,0) -- (6.5,1.2);
  \draw[->] (7.3,1.7) -- (4.9,1.7) node[midway,above] {search along the row};
  % ---- SAD cost as a function of candidate column, aligned under the row
  \draw[->] (4.6,-3.0) -- (7.75,-3.0) node[right] {column};
  \draw[->] (4.6,-3.0) -- (4.6,-0.7) node[above] {SAD};
  \draw[blue!60,smooth] plot coordinates
    {(4.8,-1.15) (5.2,-1.75) (5.5,-2.25) (5.8,-2.62) (6.1,-2.25)
     (6.5,-1.65) (7.0,-1.30) (7.4,-1.10)};
  \fill (5.5,-2.25) circle (1.5pt);
  \fill (5.8,-2.62) circle (1.5pt);
  \fill (6.1,-2.25) circle (1.5pt);
  \draw[densely dotted] (5.85,-3.0) -- (5.85,-2.60);
  \node[below] at (5.85,-3.05) {$\hat d$ (sub-pixel)};
\end{tikzpicture}
\caption{Block matching on the rectified pair. Top: the
$(2w{+}1)^2$-pixel window around a left pixel is compared against
same-size windows sliding along the \emph{single} corresponding right
row (blue); light squares are candidates, the shaded one is the best
match, and the dotted vertical repeats the reference column so the
displacement is visible. Bottom: SAD cost of each candidate column;
the three dots are the integer samples around the minimum, and a
parabola through them places the sub-pixel disparity $\hat d$.}
\label{fig:blockmatch}
\end{figure} This is the classic local baseline method in the
taxonomy of Scharstein and Szeliski \cite{ScharsteinSzeliski2002} --- chosen
deliberately: it is simple, portable, trivially parallel, and its failure
modes (textureless regions, occlusions, repetitive patterns) are well
understood, giving a controlled baseline against which future matchers
(rank/census transforms, semi-global aggregation) can be measured within the
same harness.

Matched disparity $d$ converts to metric depth by
\begin{equation}
Z = \frac{f\,B}{d},
\label{eq:depth}
\end{equation}
with $f$ the shared rectified focal length (pixels) and $B$ the baseline.
Differentiating \eqref{eq:depth} gives the expected depth uncertainty
\begin{equation}
\sigma_Z \;=\; \frac{Z^2}{f\,B}\,\sigma_d
\label{eq:deptherr}
\end{equation}
for disparity noise $\sigma_d$ --- the quadratic growth with $Z$ is the
fundamental accuracy law of the rig.

\begin{example}[depth error budget]
\label{ex:deptherr}
The experimental rig of \autoref{sec:results}: $f=800$\,px, $B=0.5$\,m,
mean depth $Z=4.7$\,m, per-coordinate noise $\sigma=0.3$\,px so
$\sigma_d=\sqrt2\,\sigma\approx0.42$\,px. Then
$\sigma_Z = 4.7^2\cdot 0.42/(800\cdot0.5) \approx 0.023$\,m: about
2.3\,cm of depth uncertainty --- compare the measured 2.65\,cm RMS
3D error in \autoref{tab:results}.
\end{example}

The companion's \code{?scene=narrow} configuration
(\autoref{sec:webcompanion}) makes \eqref{eq:deptherr} tangible: with
the cameras moved nearly together the triangulation error explodes
while the plane-transfer prediction of \autoref{sec:parallax} becomes
nearly perfect --- small baseline trades depth information for
transferability.

%----------------------------------------------------------------------
\section{From depth to models}
\label{sec:models}

\paragraph{Input and output --- where this is going.} The input is what
the previous sections produce: per frame and per camera, a dense depth
map (\autoref{sec:stereo}) and, where wanted, sparse triangulated points
(\autoref{sec:triangulation}) --- all back-projectable to 3D points in
the one fixed world frame. The intended output is a persistent
\emph{model} of the working volume that outlives any single frame:
today, an accumulated point cloud exported as PLY; as the accumulation
matures (the argument of this section), a fused TSDF volume from which
a triangulated mesh of the static scene, a queryable background model,
and per-frame foreground ``movers'' are all derived. In one sentence:
the sections before this one turn pixels into per-frame 3D, and this
one turns per-frame 3D into a durable scene.

Every depth estimate back-projects to a 3D point in the fixed world frame;
points are accumulated in a growable cloud with optional per-point color and
exported as ASCII PLY --- the text variant of the polygon file format: a
self-describing header followed by one line per vertex, directly loadable
in MeshLab, CloudCompare or Blender. A complete two-point colored cloud,
exactly as \code{mv\_cloud\_write\_ply} emits it:
\begin{quote}\ttfamily\footnotesize
ply\\
format ascii 1.0\\
element vertex 2\\
property float x\\
property float y\\
property float z\\
property uchar red\\
property uchar green\\
property uchar blue\\
end\_header\\
0.1 0.25 4.7 40 200 40\\
0.12 0.25 4.71 220 50 50
\end{quote}
The header names each element type and its properties in order; every
vertex line then lists those property values --- trivially writable from
C99, diffable, and universally read. This section maps out what can be
built \emph{on top} of
that cloud: the representations worth targeting, how robust each is in the
literature, and the background/foreground architecture the stationary rig
makes natural.

\subsection{The representation ladder}
\label{sec:ladder}

Model types form a ladder of increasing geometric commitment; each rung is
derived from the one below, and robustness generally \emph{decreases}
upward:

\begin{enumerate}
  \item \emph{Point clouds} (implemented, this section): no topology, no
        commitments;
        ideal for measurement and inspection; never wrong, never finished.
  \item \emph{Occupancy / signed-distance volumes}: the truncated signed
        distance function (TSDF) fuses many noisy depth maps by running
        per-voxel averages \cite{CurlessLevoy1996}; incremental,
        noise-averaging, topology-agnostic --- the workhorse of the
        KinectFusion lineage \cite{Newcombe2011} and the natural
        accumulation structure for our per-frame depth
        (elaborated in \autoref{sec:tsdf}; the traversal-order choice
        behind it in \autoref{sec:gatherscatter}).
  \item \emph{Triangulated meshes}: the explicit surface representation
        --- a list of vertices (3D points) and a list of triangles
        (vertex index triples), adjacent triangles sharing edges so that
        together they tile a continuous surface with a well-defined
        inside and outside. This is the native language of renderers,
        3D printers, and CAD interchange, and the first rung that can
        answer ``what does the surface look like \emph{between} the
        samples?'' Extracted from a TSDF by marching
        cubes \cite{Lorensen1987}, or from oriented points by Poisson
        reconstruction \cite{Kazhdan2006}. Both are mature and reliable
        \emph{given} rung 2; meshing raw noisy stereo clouds directly
        (Delaunay-based, ball-pivoting) is fragile and best avoided.
        (Extraction for the static scene: \autoref{sec:background}.)
  \item \emph{Geometric primitives} (``something more geometric''):
        RANSAC plane extraction \cite{FischlerBolles1981} is the single
        most robust operation in the entire reconstruction literature ---
        floors, walls, table tops fall out almost for free; cylinders,
        spheres and cones follow with efficient RANSAC variants
        \cite{Schnabel2007} at the cost of tuning; box/cuboid fits give
        furniture-level abstraction. Beyond quadrics, parametric fitting
        becomes brittle. (Slotted into the background pipeline of
        \autoref{sec:background}.)
  \item \emph{Object/semantic models} (chair, table, person): robust today
        only via learned detectors --- outside the dependency-free core,
        but the geometry below is exactly what such an external module
        would consume (the mover statistics of \autoref{sec:movers} are
        the first step in this direction).
\end{enumerate}
Modern learned implicit representations (neural radiance fields, Gaussian
splatting\footnote{Gaussian splatting represents a scene as a few million
translucent ellipsoidal blobs --- each a 3D Gaussian with a position,
shape, opacity, and view-dependent color --- whose parameters are
optimized by gradient descent until images rendered from them match the
input photographs. Rendering ``splats'' each blob onto the screen in
depth order, which is fast enough for real-time view synthesis. The
result is a soft cloud of overlapping blobs tuned to reproduce
\emph{appearance}: excellent for generating new views, but with no
explicit surface to measure against.}) excel at appearance and view
synthesis but are not metrically
superior for measurement, and sit outside this system's portable-C99,
controlled-environment charter.

\subsection{The TSDF, in detail}
\label{sec:tsdf}

Since rung 2 is where this system's model extraction will live, it
deserves more than a name --- starting with its plumbing.

\paragraph{Inputs, state, outputs.} The TSDF's \emph{input} is a stream
of calibrated depth maps: per camera and frame, a depth value per pixel
(from the dense stereo of \autoref{sec:stereo}, disparity $\to$ depth
via \eqref{eq:depth}) together with that camera's fixed calibration.
Its \emph{state} --- the TSDF itself --- is a voxel grid over the
working volume holding two numbers per voxel. Its \emph{outputs} are
three: a triangulated mesh (the ``proper model,'' extracted on demand,
exported as PLY); a queryable distance field (how far is any point from
the nearest surface --- \autoref{sec:background}'s foreground test is
exactly ``does this new depth sample disagree with $D$?''); and, by
raycasting the field (ray marching again, \autoref{sec:gatherscatter}),
synthetic depth/normal maps from any viewpoint
\cite{Newcombe2011}. Build-side and use-side data flow:
\[
\underbrace{\text{depth maps} + \text{calibration}}_{\text{per frame}}
\;\rightarrow\;
\underbrace{\text{voxel sweep \eqref{eq:tsdf}}}_{\text{fusion}}
\;\rightarrow\;
\underbrace{(D,W)\ \text{grid}}_{\text{state}}
\;\rightarrow\;
\begin{cases}
\text{march.\ cubes} \rightarrow \text{mesh}\\
\text{queries} \rightarrow \text{foreground}\\
\text{raycast} \rightarrow \text{depth}
\end{cases}
\]
Fusion is \emph{incremental and order-insensitive} --- the update
\eqref{eq:tsdf} below is a running average, so frames can arrive in any
order, streams from the two cameras interleave freely, and there is
never a batch step; the mesh is extracted whenever wanted without
interrupting accumulation.

\paragraph{The fusion interface: enriched samples.} A design decision
worth recording: the TSDF is \emph{not} a function of a bare point
cloud. Two of its essential ingredients are properties of the observing
\emph{ray}, not of the point --- the \emph{sign} of distance (which
side of the surface a voxel is on) and \emph{carving} (the measured
emptiness of the space between camera and surface). A cloud that has
discarded viewpoints forces the fragile normals-estimation path
(estimate normals by local fits, then propagate a consistent
orientation globally \cite{Hoppe1992} --- machinery that exists
precisely because provenance was thrown away). The fix is one field,
not a parallel pipeline: the fusion interchange type is the
\emph{enriched sample}
\[
(\text{point},\ \text{origin},\ \text{weight},\ \text{time}),
\]
and the TSDF is a pure, commutative fold over a stream of such samples
--- origin reconstructs the ray, sign, carving segment, and the
inverse-variance weight; \emph{time} is the same per-measurement
timestamp the temporal camera model (\autoref{sec:timemodel}) already
demands. On a stationary rig the origin compresses to a camera id.
This is standard practice, in three forms across the literature:
per-scan viewpoints attached to clouds (the PCD \code{VIEWPOINT}
field, E57 scan poses, and OctoMap's
\code{insertPointCloud(cloud, origin)} API, whose free-space carving is
exactly ours \cite{Hornung2013}); per-point attributes in scanning
formats; and, at the fully-enriched limit, \emph{surfel maps}
\cite{Keller2013}, where the enriched point is itself the fused model.
The canonical TSDF papers \cite{CurlessLevoy1996,Newcombe2011} are
instead depth-map-native --- equivalent content, dense packaging ---
and this system takes both: the sample fold defines the semantics
(so sparse feature points, dense depth pixels, and future laser
returns, \autoref{sec:range}, all fuse through one interface), while
the precomputed voxel-major sweep below is the fast path when the
producer happens to be a dense static-camera depth map.

\paragraph{The state and its update.} A \emph{truncated signed distance
function} discretizes the working volume into voxels; each voxel $v$
stores two numbers, a distance estimate $D(v)$ and a weight $W(v)$. When a depth
map arrives, every voxel looks up the depth $Z_{\text{obs}}$ at the
pixel it projects to, compares it with its own depth $Z_v$ along that
camera ray, and forms the signed offset
$\eta = Z_{\text{obs}} - Z_v$ --- positive if the voxel lies in front
of the observed surface, negative behind it. The observation is
\emph{truncated}, $d = \max(-\tau, \min(\tau, \eta))$, and fused by a
weighted running average \cite{CurlessLevoy1996}:
\begin{equation}
D \leftarrow \frac{W\,D + w\,d}{W + w}, \qquad W \leftarrow W + w,
\label{eq:tsdf}
\end{equation}
with voxels far \emph{behind} the surface ($\eta < -\tau$) left
untouched --- a voxel is only carved by rays that pass near or through
it. The reconstructed surface is the zero level set of $D$, extracted
as a mesh by marching cubes \cite{Lorensen1987} with linear
interpolation along voxel edges for sub-voxel placement.
\autoref{fig:tsdfslice} lays the pieces out along a single camera ray.

\begin{figure}[t]
\centering
\begin{tikzpicture}[font=\scriptsize]
  % ---- one slice of the voxel grid; the row pierced by one ray is colored
  \draw[gray!50] (0,0) grid[step=4mm] (4,2.8);
  \foreach \xx in {0,0.4,0.8,1.2,1.6}
    \fill[blue!8]  (\xx,1.2) rectangle +(0.4,0.4);
  \foreach \xx in {2.0,2.4,2.8,3.2}
    \fill[blue!30] (\xx,1.2) rectangle +(0.4,0.4);
  \fill[gray!30] (3.6,1.2) rectangle +(0.4,0.4);
  \node at (2.2,1.4) {$+$}; \node at (2.6,1.4) {$+$};
  \node at (3.0,1.4) {$-$}; \node at (3.4,1.4) {$-$};
  % ray and surface
  \fill (-1.3,1.4) circle (1.5pt) node[below] {$\Cv$};
  \draw[->] (-1.3,1.4) -- (2.9,1.4);
  \draw[densely dotted] (2.9,1.4) -- (4.1,1.4);
  \fill (2.9,1.4) circle (1.5pt) node[above left,xshift=2pt,yshift=3pt] {$Z_{\text{obs}}$};
  \draw[thick] plot [smooth] coordinates
    {(3.15,0) (2.95,0.7) (2.90,1.4) (3.00,2.1) (3.20,2.8)};
  \node at (3.75,3.0) {surface};
  % extent markers below the grid
  \draw (0,-0.25) -- (0,-0.4) (2.0,-0.25) -- (2.0,-0.4)
        (3.6,-0.25) -- (3.6,-0.4) (4.0,-0.25) -- (4.0,-0.4);
  \draw (0,-0.325) -- (2.0,-0.325) node[midway,below] {carved: $d=+\tau$};
  \draw (2.0,-0.325) -- (3.6,-0.325) node[midway,below,yshift=-9pt] {band: $d=\eta$};
  \draw (3.6,-0.325) -- (4.0,-0.325);
  \node[below] at (4.45,-0.35) {untouched};
\end{tikzpicture}\\[14pt]
\begin{tikzpicture}[font=\scriptsize]
  % ---- the stored update as a function of voxel depth along that ray
  \draw[->] (0,0) -- (4.4,0) node[right] {$Z_v$};
  \draw[->] (0,-0.95) -- (0,0.95) node[above] {$d$};
  \draw[densely dotted] (0,0.55) -- (4.1,0.55)
    node[pos=0.5,above] {$+\tau$};
  \draw[densely dotted] (0,-0.55) -- (4.1,-0.55)
    node[pos=0.25,below] {$-\tau$};
  \draw[blue!60,thick] (0,0.55) -- (2.35,0.55) -- (3.45,-0.55);
  \draw[blue!60,thick,densely dotted] (3.45,-0.55) -- (4.1,-0.55);
  \node[blue!60] at (4.6,-0.75) {(no update)};
  \fill (2.9,0) circle (1.5pt);
  \draw[densely dotted] (2.9,0.55) -- (2.9,-0.55);
  \node[above right] at (2.9,0.02) {$Z_{\text{obs}}$};
\end{tikzpicture}
\caption{One depth measurement seen by the TSDF, in the grid (top) and
as a function (bottom). Top: a slice of the voxel grid; along the ray
from the camera center $\Cv$ to the observed surface hit $Z_{\text{obs}}$,
voxels well in front of the surface are carved with the clamped value
$+\tau$ (light blue), voxels inside the truncation band store the true
signed offset $\eta$ (darker blue, sign marked), and voxels more than
$\tau$ \emph{behind} the surface are left untouched (gray) --- the
surface (thick curve) is later recovered as the zero crossing between
the $+$ and $-$ cells. Bottom: the same rule \eqref{eq:tsdf} as the
truncated signed distance $d$ stored at a voxel of depth $Z_v$ along
the ray; the blue profile is what one measurement contributes, and
fusion averages many such profiles per voxel.}
\label{fig:tsdfslice}
\end{figure}

Three design choices connect \eqref{eq:tsdf} to the rest of this
document. \emph{Truncation} $\tau$ should cover the depth noise:
$\tau \approx 3\sigma_Z$ at the working depth ($\approx7$\,cm for our
rig at $4.7$\,m, \autoref{ex:deptherr}) --- larger wastes the band on
uninformative updates, smaller throws away genuine measurements.
\emph{Weights} implement the fusion discipline of
\autoref{sec:heterogeneous}: $w = 1/\sigma_Z^2(Z_{\text{obs}})$, so
near observations (and, later, laser samples, \autoref{sec:range})
dominate exactly in proportion to their information. And averaging
\emph{in distance space} is what makes the TSDF better than averaging
points: each voxel's $D$ is a $\sqrt n$-averaged estimate of the same
physical quantity, so surface precision improves with observation count
--- per-frame $\sigma_Z = 23$\,mm becomes a sub-millimetre statistical
floor within a minute of video ($23/\sqrt{900}\approx0.8$\,mm),
long before which systematic terms take over (\autoref{sec:budget}).

The stationary rig adds one more gift: because the cameras never move,
each voxel's projection into each camera --- the pixel it reads and its
depth along the ray --- is \emph{constant}, computable once at start-up
and stored. The per-frame update collapses to a table-driven sweep: one
depth-map lookup, one compare, one multiply-add per voxel per camera ---
the \emph{gather} traversal of \autoref{sec:gatherscatter}, chosen
exactly because the depth maps are dense and the projections precompute;
\autoref{ex:gatherscatter} quantifies when the scatter alternatives
would win instead.

\begin{example}[TSDF budget]
\label{ex:tsdf}
The working volume ($2.0\times1.3\times2.1$\,m) at $1$\,cm voxels:
$200\times130\times210 \approx 5.5$M voxels; at 6 bytes each (float $D$
$+$ 16-bit $W$) $\approx 33$\,MB --- trivial. At $5$\,mm:
$44$M voxels, $\approx260$\,MB --- still a desktop, not a cluster. The
per-frame sweep at $1$\,cm and $30$\,fps costs
$5.5\text{M}\times2$ cameras $\times30 \approx 330$M table-driven
updates per second --- feasible in plain C, and reducible by an order
of magnitude by updating only the truncation band around the current
surface. Sub-voxel zero-crossing interpolation means the $1$\,cm grid
does not limit surface accuracy to $1$\,cm; the averaged noise does,
and it is far smaller.
\end{example}

\subsection{Background extraction: the easiest product}
\label{sec:background}

For a stationary rig, the \emph{static scene} --- walls, floor, furniture
--- is the most robustly extractable model, because time works entirely in
its favor: the per-pixel \emph{temporal median} of depth over minutes or
hours is immune to anything that moves through the scene (the same
robust-statistics trick as classical 2D background subtraction, lifted to
depth), and every additional frame averages noise down. The pipeline is:
temporal-median depth map $\to$ TSDF fusion \cite{CurlessLevoy1996}
$\to$ marching-cubes mesh \cite{Lorensen1987} of the static scene $\to$
RANSAC planes for floor/walls/table-tops $\to$ primitive fits
\cite{Schnabel2007} on the remaining clusters for furniture-level
geometry. Every stage is rung-1--4 robust. The background model is also
\emph{self-maintaining}: a piece of furniture moved to a new position is a
persistent change --- detected exactly like a lamp event in
\autoref{sec:scenarios}, but in geometry --- triggering a local update
and, as a by-product, an event log of scene rearrangements.

\subsection{Separating the movers}
\label{sec:movers}

With a background model in hand, foreground extraction is subtraction:
any depth estimate (or triangulated point) that disagrees with the
background beyond its noise band belongs to a \emph{mover}. Foreground
points are clustered by spatial proximity per frame, clusters are tracked
over time (\autoref{sec:scenarios} implements exactly this machinery),
and classification into insect / robot / human / other falls out of
crude but effective geometric statistics --- extent, height above floor,
speed, and persistence:
a single isolated point moving at $0.1$--$1$\,m/s is an insect; a
$\sim$0.3\,m-wide cluster at constant $\sim$0.1\,m height gliding on the
floor plane is the robot; a $0.4$--$0.6$\,m cluster extending
$1.5$--$2$\,m above the floor is a person. The scenario studies of
\autoref{sec:scenarios} quantify each of these tracks separately; the
unified pipeline --- background subtraction feeding one multi-target
tracker with per-class models --- is the integration goal they build
toward.

%----------------------------------------------------------------------
\section{Scaling the rig: more cameras and more pixels}
\label{sec:scaling}

The two-camera rig is the minimum that triangulates. This section works out
what changes when the rig grows --- $N$ cameras around the same volume, or
higher (possibly unequal) resolutions --- so that hardware decisions can be
made from the error laws instead of intuition. The library is already
shaped for this: \code{mv\_triangulate} accepts any $N\ge2$, and every
camera carries its own $\K$, so heterogeneous rigs need no new code paths in
the sparse pipeline. \autoref{fig:rigs} shows the four camera-positioning
scenarios this section analyzes, with each camera's viewing frustum drawn
to the rig's actual field of view ($2\arctan(320/800)\approx44^\circ$
horizontally for the reference cameras).

% frustum wedge: x, y, direction deg, half-fov deg, range
\newcommand{\camw}[5]{%
  \fill[blue!30, fill opacity=0.35]
    (#1,#2) -- ++({#3-#4}:#5) arc ({#3-#4}:{#3+#4}:#5) -- cycle;
  \fill[black] (#1,#2) circle (1.6pt);}

\begin{figure}[ht]
\centering
\begin{minipage}[t]{0.48\textwidth}
\centering
\begin{tikzpicture}[font=\scriptsize]
  \camw{0}{0}{90}{22}{3.3}
  \camw{0.28}{0}{96}{22}{3.3}
  \draw[dashed] (-0.55,2.03) rectangle (0.55,3.19);
  \node at (0,2.6) {\tiny working volume};
  \draw[<->] (0,-0.16) -- (0.28,-0.16);
  \node[below, yshift=-1pt] at (0.14,-0.16) {$B=0.5$\,m};
\end{tikzpicture}\\[2pt]
(a) reference pair (\autoref{sec:capabilities}): $6^\circ$ convergence,
overlap covers the volume
\end{minipage}\hfill
\begin{minipage}[t]{0.48\textwidth}
\centering
\begin{tikzpicture}[font=\scriptsize]
  \camw{0}{0}{90}{22}{2.6}
  \camw{0.21}{0}{90}{22}{2.6}
  \camw{0.42}{0}{90}{22}{2.6}
  \camw{0.63}{0}{90}{22}{2.6}
  \draw[<->] (0,-0.16) -- (0.63,-0.16);
  \node[below, yshift=-1pt] at (0.32,-0.16) {$(N{-}1)B = 1.5$\,m};
  \node at (0.32,2.9)
    {widest pair sets $\sigma_Z \propto 1/((N{-}1)B)$};
\end{tikzpicture}\\[2pt]
(b) collinear multi-baseline array (\autoref{sec:ncams},
\autoref{ex:ncams}): short pairs match, wide pair measures
\end{minipage}\\[10pt]
\begin{minipage}[t]{0.48\textwidth}
\centering
\begin{tikzpicture}[font=\scriptsize]
  % C-shaped object (gap faces 0 degrees) centered at (0,1.9)
  \draw[line width=2.2mm, black!70] (0,1.9) ++(35:0.42)
    arc (35:325:0.42);
  \camw{-1.64}{0.95}{30}{22}{2.4}
  \camw{-0.65}{0.11}{70}{22}{2.4}
  \camw{0.65}{0.11}{110}{22}{2.4}
  \camw{1.64}{0.95}{150}{22}{2.4}
  \camw{1.9}{1.9}{180}{22}{2.4}
  \draw[dashed, red!70!black, thick] (1.9,1.9) -- (-0.1,1.9);
  \node[red!70!black, anchor=west] at (0.7,2.42)
    {\tiny sightline threads the opening};
\end{tikzpicture}\\[2pt]
(c) coverage ring for occlusion and topology (\autoref{sec:occlusion}):
spread beats baseline; one camera must see through the handle
\end{minipage}\hfill
\begin{minipage}[t]{0.48\textwidth}
\centering
\begin{tikzpicture}[font=\scriptsize]
  \camw{0}{0}{90}{22}{2.9}
  \camw{0.3}{0}{90}{22}{2.9}
  \camw{1.6}{0}{119}{6}{2.9}
  \draw[line width=1.2mm] (0.0,2.6) -- (0.3,2.6);
  \node[above] at (0.15,2.63) {door};
  \node[below] at (1.85,-0.06) {identity cam};
  \node[below] at (0.0,-0.06) {metric pair};
\end{tikzpicture}\\[2pt]
(d) heterogeneous rig (\autoref{sec:heterogeneous},
\autoref{sec:openset}): wide pair for geometry, narrow frustum for
face pixels at the chokepoint
\end{minipage}
\caption{Camera-positioning scenarios, top-down, frustums drawn as
viewing wedges (range truncated for the page; the real frusta extend
with distance). Overlap density shows where triangulation is possible.}
\label{fig:rigs}
\end{figure}

\subsection{$N$ cameras: accuracy}
\label{sec:ncams}

Adding cameras improves triangulation through two distinct mechanisms.

\paragraph{Geometry (the dominant effect).} The depth law
\eqref{eq:deptherr} is governed by the baseline: rays from nearby centers
intersect at grazing angles, and the intersection point slides far along
the rays for small image perturbations. With cameras spread out, the
\emph{widest} usable pair sets the leading term: for $N$ collinear cameras
spaced $B$ apart, the widest baseline is $(N{-}1)B$ and
\begin{equation}
\sigma_Z^{\text{widest}} \;=\; \frac{Z^2}{f\,(N{-}1)B}\,\sigma_d,
\label{eq:multibaseline}
\end{equation}
an $(N{-}1)$-fold improvement before any averaging. The catch is
correspondence: wide pairs see the surface from very different angles, so
matching is harder --- which is exactly why multi-baseline stereo
\cite{OkutomiKanade1993} matches \emph{short} pairs (easy, unambiguous) and
accumulates their evidence in a common inverse-depth parameterization,
getting wide-pair accuracy at short-pair matching difficulty. Repetitive
texture that fools one pair (a periodic fence aliases at one baseline) is
disambiguated by any other baseline whose period does not divide it --- the
match must be consistent across \emph{all} pairs simultaneously.

\paragraph{Redundancy.} With visibility and geometry held fixed,
$N$ views of the same point give the usual statistical $\sqrt{N}$: the DLT
stack \eqref{eq:dlt} grows to $2N\times4$ rows and the estimate averages the
per-view noise. This is the smaller effect --- doubling $N$ buys
$\sqrt2\approx1.4\times$; doubling the \emph{spread} buys $2\times$.

\begin{example}[four collinear cameras]
\label{ex:ncams}
Extend the experimental rig of \autoref{ex:deptherr} ($f=800$\,px,
$Z=4.7$\,m, $\sigma_d=0.42$\,px) from two cameras $0.5$\,m apart to four
cameras at $0,\,0.5,\,1.0,\,1.5$\,m. The widest pair has $B=1.5$\,m, so by
\eqref{eq:multibaseline}
$\sigma_Z = 4.7^2\cdot0.42/(800\cdot1.5) \approx 7.7$\,mm --- three times
better than the 23\,mm of the original pair, from geometry alone; fusing
the remaining pairs tightens it further. The price: the widest pair's
viewing directions differ by
$2\arctan\!\bigl(0.75/4.7\bigr)\approx18^\circ$, and matching windows must
survive that foreshortening --- the Okutomi--Kanade argument for
accumulating the short pairs instead.
\end{example}

\subsection{$N$ cameras: occlusion and topology}
\label{sec:occlusion}

A point enters the model only if at least two cameras see it. With two
cameras, anything hidden from either one is lost; the reconstructed surface
is only the band that is simultaneously front-facing and unobstructed for
both --- for objects with complex topology (handles, tunnels, deep
concavities) that band can be a small fraction of the surface, and the
holes it leaves are the familiar occlusion artifacts.

A useful back-of-envelope model: if a surface point is occluded in any
single view independently with probability $q$, then
\begin{equation}
P(\text{reconstructible}) \;=\; P(\ge2\text{ of }N\text{ see it})
\;=\; 1-q^N-N\,q^{N-1}(1-q).
\label{eq:occlusionprob}
\end{equation}
Independence is optimistic for clustered cameras (they get occluded
together) and pessimistic for well-spread ones --- spreading the cameras is
what makes the assumption approachable.

\begin{example}[occlusion coverage]
\label{ex:occlusion}
Take $q=0.3$ (a fairly convoluted scene). By \eqref{eq:occlusionprob}:
$N=2$: $(1-q)^2=0.49$ --- half the surface. $N=4$:
$1-0.3^4-4\cdot0.3^3\cdot0.7 = 1-0.0081-0.0756 \approx 0.92$. $N=6$:
$1-0.3^6-6\cdot0.3^5\cdot0.7 \approx 0.99$. Going from two to four cameras
nearly doubles the reconstructible surface; four to six mops up the
remainder. The marginal camera always buys less --- but for high-genus
objects the last percent \emph{is} the topology.
\end{example}

Topology sharpens the point. Silhouette-based reasoning can never recover
concavities: the best any number of silhouettes gives is the visual hull
\cite{Laurentini1994}, which fills in bowls and blocks handles. Capturing
the true genus of an object --- the hole of a handle, the tunnel through an
arch --- requires cameras whose \emph{sightlines pass through the hole},
seeing background where the hull would put material; photo-consistency
frameworks (space carving \cite{KutulakosSeitz2000}, patch-based multiview
stereo \cite{FurukawaPonce2010}) then remove exactly the voxels/patches
contradicted by those views. So for complex topology, added cameras should
be \emph{spread to thread the object's openings and look into its
concavities}, not packed for baseline; the standard benchmark evidence
\cite{Seitz2006} is that view coverage, more than per-view quality, is what
separates complete reconstructions from holed ones. In this system's terms:
the two rig cameras provide the calibrated metric backbone, and
\autoref{sec:photos}'s registered auxiliary photos are the cheap way to add
exactly such coverage views.

Two practical costs scale with $N$: (i) calibration and synchronization ---
each camera needs its own \autoref{sec:calib} session against the same
target, all cameras a common trigger; (ii) computation --- pairwise
geometry (fundamental matrices, rectifications) grows as $N(N{-}1)/2$,
though in practice a hub-and-spoke set against one reference camera
suffices for gating.

\subsection{Resolution}
\label{sec:resolution}

For a fixed field of view, the focal length \emph{in pixels} scales with
the sensor's linear resolution: doubling from $640\times480$ to
$1280\times960$ doubles $f$ from $800$ to $1600$\,px. Two consequences
follow directly.

\paragraph{Depth precision.} Matching accuracy $\sigma_d$ is
an approximately constant \emph{fraction of a pixel} (our block matcher's
parabolic refinement sits around $0.1$--$0.3$\,px on textured surfaces), so
in \eqref{eq:deptherr} the denominator's $f$ doubles while $\sigma_d$ stays
put: $\sigma_Z$ halves. Depth precision is linear in resolution ---
until lens blur, diffraction, or per-pixel photon noise (smaller pixels
collect less light) starts inflating $\sigma_d$ and returns diminish.

\paragraph{Lateral detail.} The pixel footprint on the scene (ground
sampling distance) is $Z/f$ metres per pixel. Surface detail must span at
least $\sim$2 pixels to be matchable, so the smallest reconstructible
geometric feature is about $2Z/f$ --- this, not depth noise, is what limits
how much of a complex object's fine structure (thin ribs, small holes)
survives into the model. In the fidelity sense of
\autoref{sec:occlusion}: resolution sets the smallest feature you can
capture, view coverage sets whether you capture it at all.

\begin{example}[resolution budget]
\label{ex:resolution}
Experimental rig, $Z=4.7$\,m: footprint $Z/f = 4.7/800 = 5.9$\,mm/px,
smallest matchable feature $\approx12$\,mm, $\sigma_Z\approx23$\,mm
(\autoref{ex:deptherr}). Doubling both cameras' resolution:
footprint $2.9$\,mm/px, feature floor $\approx6$\,mm,
$\sigma_Z\approx12$\,mm. Everything improves by the same factor of two.
\end{example}

\subsection{Unequal resolutions}
\label{sec:heterogeneous}

Nothing in the model requires the cameras to match: each carries its own
$\K$ (and distortion), the fundamental matrix \eqref{eq:fundamental} exists
between any pair, and triangulation consumes pixels through each camera's
own $\PP$. What changes is the error budget. Each camera contributes
\emph{angular} noise $\sigma/f_i$ (radians); ray-intersection variance adds
them, so a pair's effective noise is
\begin{equation}
\sigma_\theta \;=\; \sqrt{(\sigma_1/f_1)^2+(\sigma_2/f_2)^2}:
\label{eq:hetero}
\end{equation}
the \emph{coarser camera dominates}, and money spent on one camera is
mostly wasted unless the other keeps up.

\begin{example}[upgrading one camera vs both]
\label{ex:hetero}
Rig of \autoref{ex:deptherr}, $\sigma=0.3$\,px per camera. Both at
$f=800$: $\sigma_\theta=\sqrt2\,(0.3/800)$. Upgrade \emph{one} camera to
$f=1600$: $\sigma_\theta=(0.3/800)\sqrt{1+\tfrac14}$, an improvement of
only $\sqrt{1.25/2}\approx0.79$ --- depth error drops from $23$\,mm to
$18$\,mm. Upgrade \emph{both}: factor $0.5$, $12$\,mm
(\autoref{ex:resolution}). Half the hardware upgrade buys about a fifth of
the benefit.
\end{example}

Two implementation notes for mixed rigs. First, rectification
(\autoref{sec:stereo}) forces a \emph{shared} $\K_{\mathrm{new}}$; our
implementation averages the two intrinsic matrices, which for very unequal
cameras oversamples the coarse image (interpolated pixels carry no new
information) --- resampling both to the coarser camera's grid keeps the
cost of dense matching honest, at the price of discarding the fine
camera's lateral detail. Second, the DLT \eqref{eq:dlt} as implemented
weights all rows equally; with strongly unequal cameras the rows of the
finer camera deserve proportionally more weight ($\propto f_i/\sigma_i$
after each view's normalization), a small planned refinement alongside the
Hartley--Sturm step (\autoref{sec:roadmap}).

%----------------------------------------------------------------------
\section{Planned extensions}
\label{sec:extensions}

\subsection{Auxiliary photo collections}
\label{sec:photos}

Still photographs of the same volume --- arbitrary viewpoints, possibly a
different camera --- densify coverage and fill occlusions. The integration
path follows the structure-from-motion recipe of Photo Tourism
\cite{Snavely2006}: detect scale-invariant features \cite{Lowe2004} in the
photos \emph{and} in the two calibrated video frames; match with a RANSAC
gate \cite{FischlerBolles1981}; register each photo to the rig's world frame
by resection from matches against already-triangulated points; then
re-triangulate all tracks with the registered views via the existing
$N$-view DLT (\autoref{sec:triangulation}); finally refine everything with
bundle adjustment \cite{Triggs2000}. The two calibrated cameras anchor the
metric scale --- the classic scale ambiguity of pure SfM does not arise.
Dense multiview stereo over the registered set (e.g.\ patch-based methods
\cite{FurukawaPonce2010}) is a further optional stage.

The registration core of this recipe is now \emph{built and
sim-validated} (\code{mv/photo.h}): normalized DLT resection with RQ
decomposition (\code{mv\_resect\_dlt},
\code{mv\_cam\_from\_projection}), RANSAC-seeded and
median-trimmed (\code{mv\_resect\_robust}), a full-LM geometric
polish, and the end-to-end \code{mv\_photo\_register}: the
calibrated pair triangulates a descriptor-carrying anchor bank, the
photo matches into it, and resection recovers the photo's own K and
pose --- \emph{unknown camera included}. Following standard
casual-photo practice \cite{Snavely2006}, the principal point is
pinned at the image center (it is nearly unobservable from one
resection over a shallow scene, trading against lateral pose --- a
failure measured, not assumed, in development: the free-pp solution
put it $105$\,px off). Sim results at a $10\%$ magnification
mismatch and $15^\circ$ roll: focal recovered within $2.6\%$,
orientation $0.25^\circ$, position $0.11$\,m from 210 resection
inliers over 330 anchors — the position term is the measured cost of
single-scale descriptor matching under magnification mismatch, and
bundle adjustment over the registered set is the designated
tightening stage. A practical capture note: a dedicated phone capture
app could attach each photo's own lens intrinsics and a
gravity-vector attitude prior (both exposed by modern phone APIs),
turning unknown-camera resection into known-K resection with an
orientation prior --- fewer correspondences needed and a sanity gate
on the vision solution; indoor compass yaw is too coarse to replace
the vision pose, so the pipeline above remains the foundation either
way.

The dynamic limit of this idea is the \emph{tracked mobile camera}: a
drone or handheld camera moving through the volume is, to the fixed rig,
a rigid mover like any other (\autoref{sec:movers}) --- so the rig can
track its pose over time with the machinery already validated in
\autoref{sec:scenarios}, timestamp it via \autoref{sec:softsync}, and
hand the mobile camera its own trajectory. Its frames then enter as
registered auxiliary views with poses \emph{measured rather than
estimated}: no ego-motion SfM, scale and drift anchored by the fixed
backbone. The system becomes self-tracking in the useful sense --- the
fixed cameras are the ground truth that mobile sensors borrow.

\subsection{Projected structured light (built, awaiting hardware)}
\label{sec:slight}

The dense-stereo results carry a standing caveat: SAD matching needs
texture, and the texture gate's honest answer on a blank wall is ``no
depth here'' (the texture-desert failure measured in
\autoref{sec:results}). Real rooms are mostly blank wall, bare
floor, and blank ceiling. A projector solves this by \emph{painting} structure: it casts
the Gray-code stripe sequence of \autoref{sec:display} onto the
scene, so every lit surface point wears its projector coordinate as a
temporal label. That is the primary role --- marking and measuring
structure-free walls, floors, and (aim it up: in an enclosed space
the most reliably empty surface of all) ceilings. Two useful corollaries come free.
First, cross-camera matching needs no feature detector at all: pixels
in two cameras that decoded the same (column, row) label correspond
(\code{mv\_graycode\_match}), which yields tens of thousands of
correspondences on surfaces that offer none naturally. Second, those
correspondences give the cameras' \emph{relative pose} through the
essential matrix (\code{mv\_fundamental\_8point\_robust} $\to$
\code{mv\_essential\_pose}) without both cameras needing to see the
calibration display --- an extrinsics bridge for camera pairs at
awkward angles. The projector itself needs no calibration for any of
this: it is only a label dispenser; all geometry lives in the
cameras. Scale is the one quantity the labels cannot supply --- one
tape-measured baseline, or the display in view of either camera,
anchors it.

The whole path is implemented and validated in simulation ---
display app mode M5 (46 slots of $\sim$$0.5$\,s: white and black
marker slots, then 11 column and 11 row Gray bits, each with its
inverse; cycle $\approx23.5$\,s), the algorithm round trip
(\code{test\_structured\_light}: 13k+ matches, relative pose within
$0.33^\circ$ of truth in rotation and baseline direction), and the
\emph{tool-level} integration (\code{demo/slightsim.c} fabricates a
two-camera capture as PGM files, frame timing included, and
\code{tools/slreal.c} recovers the pose to the fourth digit and
triangulates 62k points from it). Two lessons from getting there are
worth keeping. A sim-fidelity trap: painting the projected codes onto
finite-resolution scene textures with nearest sampling aliased a
moir\'e between the texture grid and the stripe pitch, biasing the
recovered baseline by $10^\circ$ --- a real projector has no
intermediate raster, so the sim must supersample to avoid inventing
one. And an estimator lesson: dense code matching has a fat tail of
boundary-pixel mismatches, so the 8-point runs with two rounds of
median-residual trimming (now a library function, useful wherever
correspondences come cheap and dirty).

\emph{Runbook for the hardware session} (also atop
\code{tools/slreal.c}): (1) projector shows \code{pattern.html},
key 5, room darkened; (2) both cameras fixed, views overlapping on
the lit surfaces, record $\geq60$\,s ($\geq2$ cycles); (3)
intrinsics per camera from a separate mode-1/mode-4 pattern film if
not already known; (4) extract frames at $0.1$\,s; (5) write the
two spec files (intrinsics line, \code{interval 0.1}, frame paths);
(6) \code{./slreal <baseline\_m> camA.spec camB.spec out}. Outputs:
relative pose printout and \code{out.ply}. If cycle detection fails,
the room was too bright or the capture too short.

\subsection{Range-finder fusion (optional, kept open)}
\label{sec:range}

The system is designed to work without a range finder; this subsection
records what a single-beam laser ranger would add if one arrives later, so
the option stays open at zero present cost. Two integration modes:
(i) if the range finder is rigidly mounted and extrinsically calibrated to
the rig, each range sample is simply another world-frame point with small
$\sigma_Z$, fused into the cloud with inverse-variance weights; (ii) if it
produces an independent scan, the scan is registered to the vision cloud by
closed-form absolute orientation \cite{Horn1987} initialized
correspondence-free and refined with ICP \cite{BeslMcKay1992}.

\paragraph{At calibration time (with the rotating display of
\autoref{sec:rotating}).} Four distinct improvements. (i) \emph{Independent
absolute scale}: the vision-only metric chain hangs entirely on the
display's pixel pitch; a $\sim$2\,mm-accurate range at 4.7\,m is a 0.04\%
scale reference --- a second chain that turns a silent global scale bias
into a detectable discrepancy. (ii) \emph{Orbit metrology}: aimed so the
rotating screen sweeps through the beam, the periodic range trace $d(t)$
yields $\omega$, phase, orbit radius and axis distance at millimetre level,
independently of every camera, and ties laser and camera timestamps to the
same physical event. (iii) \emph{The focal--depth weak direction}
(\autoref{sec:printed}) is attacked head-on: an absolute distance to the
target plane is exactly the constraint type planar homographies lack.
(iv) \emph{The plate calibrates the laser}: if the spot is visible to the
cameras, the sweeping plate carries it through continuously varying depths;
each frame gives (range, pixel$_1$, pixel$_2$), the cameras triangulate the
spot, and the beam's ray in the rig frame ($\sim$5 DOF) falls out of a
least-squares line fit --- no manual procedure at all.

\paragraph{At run time.} Laser error is roughly constant while stereo error
grows as $Z^2$ \eqref{eq:deptherr}, so the laser anchors the far field and
the absolute scale (sparse metric skeleton, dense stereo flesh); it
provides depth in texture deserts where block matching has nothing to
match, and used as a prior it narrows the disparity search there; and a
persistent bias between laser and stereo depth at the same spot is an
online health monitor for the metric chain, complementing the $\F$-drift
monitor of \autoref{sec:epipolar}.

\begin{example}[laser--stereo crossover]
\label{ex:crossover}
With $\sigma_{\text{laser}}=2$\,mm constant and the rig's
$\sigma_Z=Z^2\sigma_d/(fB)$: equality at
$Z=\sqrt{0.002\cdot 800\cdot 0.5/0.42}\approx1.4$\,m. Nearer than that,
stereo wins per point; beyond it the laser is better along its ray ---
tenfold better at $4.7$\,m ($2$\,mm vs $23$\,mm) --- while stereo retains
the advantage of density. Inverse-variance fusion realizes both.
\end{example}

%----------------------------------------------------------------------
\section{Numerical implementation notes}
\label{sec:numerics}

All decompositions reduce to one kernel: a one-sided (Hestenes) Jacobi SVD
\cite{GolubVanLoan2013} for $m\ge n$ matrices, $n\le9$ in every present use
(the design matrices of \eqref{eq:planehom}, \eqref{eq:zhangconstraints},
\eqref{eq:dlt}, and the 8-point system). Jacobi SVD was chosen over
Golub--Kahan bidiagonalization for its simplicity ($\sim$100 lines, no
workspace queries), unconditional convergence, and excellent relative
accuracy on small matrices; its $O(mn^2)$-per-sweep cost is irrelevant at
these sizes. Homogeneous solutions are the right singular vector of the
smallest singular value; rank-2 enforcement for $\F$ zeroes the smallest
singular value and recomposes.

Conditioning is handled where it matters: design matrices built from raw
pixel coordinates mix terms of order $1$, $u$, and $u^2$
($u\sim10^2$ pixels), giving condition numbers around $10^8$; Hartley
normalization \cite{Hartley1997} (translate to centroid, scale to RMS
radius $\sqrt2$) reduces this to $O(10)$ and is applied in both the 8-point
and the homography estimators, with results denormalized afterwards. All
fundamental matrices are canonicalized (unit Frobenius norm,
largest-magnitude entry positive) so that analytic and estimated matrices
are directly comparable --- this is what makes the drift monitor of
\autoref{sec:epipolar} a plain matrix norm. Calibration runs Zhang's
closed form inside the small alternation loop of \autoref{sec:radial};
a full Levenberg--Marquardt refinement over all parameters is the planned
upgrade (\autoref{sec:roadmap}).

The code is strict C99 (\code{-std=c99 -pedantic -Wall -Wextra} clean),
depends only on \code{libm}, allocates only in unavoidable places (design
matrices, images, clouds), and contains no hidden state except the demos'
seeded generators. Determinism is treated as a feature: identical inputs
give bit-identical outputs across runs, so every regression is reproducible.

%----------------------------------------------------------------------
\section{Basic results}
\label{sec:results}

Three validation layers accompany the library. \code{tests/test\_mv.c}
checks exact properties on noiseless data (47 assertions: SVD
reconstruction and orthonormality, ray/projection consistency,
\eqref{eq:epiconstraint} to $10^{-8}$, exact homography and triangulation
recovery, exact intrinsic/pose recovery by Zhang's method, Gray-code
encode/decode round-trip, row alignment after rectification); all pass.
The two demo programs are controlled noisy experiments.

\paragraph{Two-camera reconstruction (\code{demo/synthetic.c}).} Two
cameras with $f=800$\,px, $640\times480$ images, baseline $B=0.5$\,m, the
second camera yawed $6^\circ$ inward, observe 250 points drawn uniformly in
a $2\times1.5$\,m window at depths $3.5$--$6$\,m; projections carry
Gaussian noise $\sigma=0.3$\,px per coordinate (seed fixed).

\begin{table}[ht]
\centering
\begin{tabular}{lr}
\toprule
Quantity & Value \\
\midrule
points visible in both views                       & $250/250$ \\
$\|\F_{\text{analytic}}-\F_{\text{8-point}}\|_F$ (unit-norm)  & $4.5\times10^{-3}$ \\
mean symmetric epipolar distance (analytic $\F$)   & $0.345$ px \\
mean symmetric epipolar distance (8-point $\F$)    & $0.341$ px \\
triangulation RMS 3D error                         & $0.0265$ m \\
triangulation max 3D error                         & $0.0876$ m \\
RMS reprojection error                             & $0.218$ px \\
mean row misalignment $|v_1-v_2|$ before rectification & $0.764$ px \\
mean row misalignment after rectification          & $0.0$ px \\
\bottomrule
\end{tabular}
\caption{Reconstruction experiment, seed 42 (reproduce with \code{make demo}).}
\label{tab:results}
\end{table}

The numbers agree with theory: the epipolar residual $\approx0.34$\,px
matches the injected noise, and the RMS 3D error of 2.65\,cm matches the
2.3\,cm depth-noise floor computed by hand in \autoref{ex:deptherr} (the
measured value also contains the smaller lateral components). The
implementation performs at the noise floor of the geometry rather than
being implementation-limited. The reconstructed cloud is written to
\code{out\_cloud.ply}.

\paragraph{Calibration (\code{demo/calibrate.c}).} A camera with
ground-truth $f_x=800$, $f_y=805$, $c_x=322$, $c_y=238$, zero skew
and zero distortion photographs the letter-page target
(\autoref{sec:printed}; $7\times9$ corners, 24\,mm squares) in six poses
with tilts up to $35^\circ$ at 0.45--0.65\,m; corners carry
$\sigma=0.3$\,px noise.

\begin{table}[ht]
\centering
\begin{tabular}{lr}
\toprule
Quantity & Value \\
\midrule
$f_x$ error   & $-7.0$ px \ ($0.9\%$) \\
$f_y$ error   & $-7.9$ px \ ($1.0\%$) \\
$c_x$, $c_y$ error & $-1.0$, $-1.8$ px \\
mean rotation error over views    & $0.31^\circ$ \\
mean translation error over views & $5.2$ mm \\
reprojection RMS                  & $0.48$ px \\
\bottomrule
\end{tabular}
\caption{Calibration experiment, seed 7, six views, $\sigma=0.3$ px
(reproduce with \code{make demo}).}
\label{tab:calibresults}
\end{table}

Sub-1\% intrinsics and millimetre-level poses from six noisy views of one
printed page is the expected performance of Zhang's method without
nonlinear refinement; near-frontal pose sets degrade the focal estimate
markedly (weak observability, \autoref{sec:printed}), which the experiment
reproduces if the tilt angles are reduced. The fitted $(k_1,k_2)$ absorb a
little noise as a near-zero distortion \emph{curve} while being individually
large --- the identifiability effect discussed in \autoref{sec:radial}. A
Levenberg--Marquardt refinement stage is the roadmap item that tightens all
of these numbers.

\paragraph{The calibration pattern pipeline
(\code{demo/patternsim.c}).} The first \emph{image-tier} experiment:
the spec-v1 screen (\autoref{sec:calibspec}) is rendered at metric
scale, imaged by the standard camera under seven poses (tilts to
$30^\circ$, one with the camera rolled $180^\circ$; bilinear resampling
plus 2 gray levels of sensor noise), and decoded \emph{blind} by the
reader --- no pose prior, no ground-truth assistance.

\begin{table}[ht]
\centering
\begin{tabular}{lr}
\toprule
Quantity & Value \\
\midrule
corners identified (7 poses)      & $1134/1134$ \\
corner misidentifications         & $0$ \\
orientation under $180^\circ$ roll & recovered \\
frame counter decoded             & $7/7$ frames \\
saddle localization RMS           & $0.25$--$0.35$ px \\
Zhang through the pipeline: $f_x$, $f_y$ error & $+3.5$, $+3.1$ px \\
principal point error             & $0.3$, $1.4$ px \\
reprojection RMS                  & $0.314$ px \\
\bottomrule
\end{tabular}
\caption{Pattern-pipeline experiment, seed 99 (reproduce with
\code{make demo}).}
\label{tab:patresults}
\end{table}

\begin{figure}[ht]
\centering
\includegraphics[width=0.55\textwidth]{figs/pattern_view.png}
\caption{What the reader sees: rendered frontal view of the spec-v1
screen (pose 0 of \autoref{tab:patresults}) --- saddle grid with
M-array dots, counter strip (bottom), version block and fine grid (top),
phase patches; from \code{demo\_patternsim}'s PGM output.}
\label{fig:patternview}
\end{figure}

Two readings. First, the measured saddle-detector noise
($0.25$--$0.35$\,px per corner, part systematic rendering/template
aliasing, part sensor noise) is the first \emph{empirical} $\sigma$ for
the error laws --- at the upper edge of the $0.1$--$0.3$\,px assumed in
\autoref{sec:resolution}, and the reprojection RMS of the through-pipeline
calibration ($0.314$\,px) matches it via the
$\sqrt2\,\sigma_c\sqrt{1-p/n}$ rule of \autoref{sec:rms}, so the whole
chain --- renderer, detector, decoder, estimator --- is again
noise-floor-limited rather than implementation-limited. Second, the
M-array machinery works as specified: zero misidentifications in
$1134$ corners across strong obliquity and a fully rolled camera is the
property the rotation-unique window design was bought for.

\paragraph{TSDF fusion (\code{demo/tsdfsim.c}).} The first model
extraction beyond point clouds: a sphere of known radius ($0.3$\,m at
$4.7$\,m depth) observed by both cameras with depth-law noise
($\sigma_Z\approx21$\,mm per sample at the sphere); enriched samples
(point, origin, $1/\sigma_Z^2$) are fused into a $1$\,cm-voxel TSDF
(\autoref{sec:tsdf}) and the zero level set is extracted by marching
tetrahedra; every mesh vertex is scored against the true sphere.

\begin{table}[ht]
\centering
\begin{tabular}{lrr}
\toprule
 & 10 frames & 160 frames \\
\midrule
surface RMS error & $4.40$ mm & $2.17$ mm \\
triangles         & $38{,}030$ & $33{,}726$ \\
\bottomrule
\end{tabular}
\caption{TSDF-fusion experiment, seed 4242 (reproduce with
\code{make demo}); mesh written to \code{out\_tsdf.ply}.}
\label{tab:tsdfresults}
\end{table}

The frame-count ratio is $2.03$, not the naive $\sqrt{16}=4$ --- and
this is the T3 plateau diagnostic of \autoref{sec:statrules} catching a
floor, exactly as designed: the two measurements fit
$e(n)=\sqrt{b^2+a^2/n}$ with $b=2.0$\,mm perfectly
($\sqrt{2^2+3.9^2}=4.4$; $\sqrt{2^2+0.98^2}=2.2$). The noise term
averages down as $\sqrt n$ per the fusion promise of
\autoref{sec:tsdf}; the $2$\,mm floor ($\approx$ voxel$/5$) is the
known \emph{projective-TSDF obliquity artifact} --- the along-ray
offset $\eta$ degrades as a distance proxy near grazing incidence at
the sphere's silhouette, a documented property of projective fusion,
not a defect (the noiseless unit test measures the same floor). Known
remedies when it matters: obliquity-weighted updates or
gradient-corrected distances; at $21$\,mm-per-frame input noise
producing a $2.2$\,mm surface from five seconds of video, the floor is
not the binding constraint.

\paragraph{The room model and change detection
(\code{demo/roomsim.c}).} The first \emph{dense image-tier} experiment
--- the full pipeline of \autoref{sec:overview} on rendered pixels: a
textured synthetic room (floor, back wall, side wall, and a box) is
imaged as stereo pairs, rectified by warping
(\code{mv\_warp\_homography}), matched densely
(\code{mv\_stereo\_sad} with a local-texture validity gate), converted
to depth, median-filtered over nine frames into a background model, fused
into the TSDF, and meshed. Then the box moves and one new frame is
compared against the background depth.

\begin{table}[ht]
\centering
\begin{tabular}{lr}
\toprule
background coverage (after texture gate) & $51.9\%$ of pixels \\
room mesh & $421{,}224$ triangles \\
mesh surface RMS vs true planes & $28.6$ mm \\
vertices within $0.5$ m of truth & $99.8\%$ \\
change-detection recall (moved box) & $97.9\%$ \\
change false alarms & $1.39\%$ of image \\
\bottomrule
\end{tabular}
\caption{Room-model experiment, seed 20260804 (reproduce with
\code{make demo}; $\approx$6\,s). Outputs shown in
\autoref{fig:room} and \autoref{fig:roompair}.}
\label{tab:roomresults}
\end{table}

\begin{figure}[p]
\centering
\begin{minipage}[t]{0.46\textwidth}
\centering
\includegraphics[width=\textwidth]{figs/room_left.png}\\[1pt]
{\scriptsize (a) left camera view: speckle-textured floor, two walls,
and the box; the dark band at top is the untextured void beyond the
walls}
\end{minipage}\hfill
\begin{minipage}[t]{0.46\textwidth}
\centering
\includegraphics[width=\textwidth]{figs/room_rect.png}\\[1pt]
{\scriptsize (b) the same frame after rectification warping
(\code{mv\_warp\_homography}): epipolar lines are now image rows, the
precondition for 1D matching}
\end{minipage}\\[3pt]
\begin{minipage}[t]{0.46\textwidth}
\centering
\includegraphics[width=\textwidth]{figs/room_disp.png}\\[1pt]
{\scriptsize (c) disparity: bright $=$ near (floor sweeping toward the
camera), dark $=$ far (back wall); black $=$ gated invalid --- the
texture desert above the walls and occlusion fringes beside the box,
exactly the failure modes \autoref{sec:stereo} predicts}
\end{minipage}\hfill
\begin{minipage}[t]{0.46\textwidth}
\centering
\includegraphics[width=\textwidth]{figs/room_change.png}\\[1pt]
{\scriptsize (d) change mask after the box moves: two blobs --- the
box's \emph{old} footprint (background now visible) and its \emph{new}
position; isolated flicker removed by a $3\times3$ majority filter}
\end{minipage}
\caption{Dense-pipeline stages of the room experiment --- generated by
\code{demo\_room}, converted from its PGM outputs. The concluding
truth-vs-reconstruction pair is \autoref{fig:roompair}.}
\label{fig:room}
\end{figure}

\begin{figure}[tp]
\centering
\begin{minipage}[t]{0.46\textwidth}
\centering
\includegraphics[width=\textwidth]{figs/room_truth.png}\\[1pt]
{\scriptsize (e) the \emph{generating model}: the true scene geometry
rendered by the same previewer from the same viewpoint; hue encodes
surface orientation --- red $=$ $x$-facing (side wall), green $=$
horizontal (floor, box top), blue $=$ $z$-facing (back wall, box
front) --- with per-surface albedo on top}
\end{minipage}\hfill
\begin{minipage}[t]{0.46\textwidth}
\centering
\includegraphics[width=\textwidth]{figs/room_mesh.png}\\[1pt]
{\scriptsize (f) the \emph{reconstruction}, same viewpoint and same
orientation-hue code as (e): the red/blue/green surfaces land where the
model says they should, the box's front reads blue, and its occlusion
shadow is the black void behind it; holes are honestly-unobserved
regions (texture-gated grazing floor, occlusions)}
\end{minipage}
\caption{The room experiment's conclusion: generating model (e) versus
reconstruction (f), rendered by the same previewer from the same
viewpoint. Panel letters continue \autoref{fig:room}.}
\label{fig:roompair}
\end{figure}

Reading the imagery, stage by stage: panel (a) is an
\emph{acquisition} --- one of the eighteen frames (nine per camera)
that are the experiment's only input; every later panel is derived from
such frames alone. Panel (b) shows the one-time rectification warp
doing its job: after it, corresponding points share image rows, and
matching collapses to the 1D search of \autoref{sec:stereo}. In (c)
the disparity gradient reads as depth to the eye --- floor bright and
sweeping nearer, back wall uniformly dark, the box standing out of its
surroundings --- while the black regions are the validity gate
declining to guess: the untextured void, and the occlusion fringe
beside the box where the left camera sees background the right camera
cannot. The truth/reconstruction pair (e)/(f)
(\autoref{fig:roompair}) is rendered by the same
software previewer from the same viewpoint --- a viewpoint \emph{no
physical camera occupies}, which is the point of having a model at all;
comparing them shows what survives (both walls, floor, the box as a
volume) and what does not (the box's occlusion shadow, the gated
grazing-floor holes). The change mask (d) is the background model
earning its keep: one new frame against the median background, and the
box's departure and arrival are both localized.

Reading the numbers: the $28.6$\,mm mesh RMS is dominated by the
grazing-incidence floor (slanted-surface bias of fronto-parallel SAD
windows plus the projective-TSDF obliquity of
\autoref{tab:tsdfresults}); the back wall alone reconstructs near the
$\sim$14\,mm median-of-nine depth-noise floor. The texture gate was
\emph{added during this experiment} for the classic reason: without it,
the untextured region above the walls produced garbage disparities and a
$34\%$ change false-alarm rate --- the doc's own texture-desert warning
(\autoref{sec:stereo}, \autoref{sec:range}) demonstrated, then fixed,
by validity gating; $1.39\%$ residual false alarms sit on occlusion
fringes. This experiment closes roadmap items 5 and 6 at the sim tier:
the first model of a scene produced from \emph{pixels} end to end.

\paragraph{First real data (iPhone footage of the display app).} Two
statically mounted iPhones filmed \code{tools/pattern.html} on a
MacBook in a living room (HEVC video, frames extracted by
\code{tools/extract\_frames.swift}, read by \code{tools/calibreal.c}).
Results of the reader's first encounter with real photons, after a
debugging session that added four robustness layers now permanent in
\code{src/reader.c} (detection-threshold and 2$\times$-scale
escalation; local-contrast 2-means bit classification;
checkerboard-coherence pruning of grown-in clutter; projective cell
centers by diagonal intersection):

\begin{itemize}
  \item the near-frontal view decodes \textbf{38/42 frames at
        162/162 corners} with zero misidentifications, despite the
        pattern occupying only $\sim$300\,px of a 1080-px frame
        (browser not fullscreen --- half the intended scale);
  \item the display counter is tracked across frames
        ($\approx$29.4 counts per 0.5\,s frame spacing $=$
        \textbf{58.8\,Hz refresh, measured through the camera}) ---
        the temporal layer of \autoref{sec:tempcalib} working on real
        hardware;
  \item measured per-corner temporal noise over the static sequence:
        $\sigma_u = 0.28$\,px, $\sigma_v = 0.21$\,px --- inside the
        $0.25$--$0.35$\,px band the simulations assumed
        (\autoref{tab:patresults}): the noise model survives contact
        with reality;
  \item Zhang calibration from these frames is \emph{degenerate},
        exactly as \autoref{sec:printed} predicts: every frame shows
        the same pose (nothing moved), and 38 copies of one homography
        determine nothing --- the pose-diversity requirement
        demonstrated on real data;
  \item the second, steeply oblique view ($\gtrsim$55$^\circ$)
        recovers corners, lattice, and coherence but not the identity
        dots --- beyond the obliquity gate of \autoref{sec:rotating},
        \emph{as designed}: geometry degrades gracefully, identity
        fails loudly.
\end{itemize}

The reshoot protocol that follows from these measurements: pattern
fullscreen (doubles the scale), display \emph{moved} through
10--15 paused tilted poses within the obliquity gate, cameras
AF/AE-locked.

\paragraph{Round two: the first real calibration.} The lid-hinge
protocol of \autoref{sec:tempcalib}'s sibling note (tilt the lid,
swivel the base) delivered. From a 54-second hand-off-free video:

\begin{table}[ht]
\centering
\begin{tabular}{lr}
\toprule
frames decoded blind & $67/68$ (162 corners each) \\
$f_x$, $f_y$ & $1687.2$, $1683.8$ px \ (ratio $0.998$: square pixels) \\
$c_x$ & $543.8$ px \ (image center: $540$) \\
$c_y$ & $1043.8$ px \\
$k_1$, $k_2$ & $-0.024$, $+0.218$ \\
reprojection RMS & $0.351$ px \\
camera--screen distance & $0.93$--$1.09$ m, stepping with lid poses \\
\bottomrule
\end{tabular}
\caption{First real-camera calibration (iPhone, 1080$\times$1920
HEVC video; \code{tools/calibreal.c}). The reprojection RMS matches
the independently measured $0.28/0.21$\,px detector noise --- the
estimator runs at the real noise floor.}
\label{tab:realcalib}
\end{table}

Two further lessons from the same round, both caught by the built-in
diagnostics. A first attempt on a longer video returned degenerate
intrinsics with \emph{constant} per-view distances --- the glue tool
was truncating to the first 64 decodable frames, all from a static
lead-in (fixed); the rerun, sampled evenly across the timeline, was
\emph{still} constant-distance: that session genuinely contained no
display motion, and no estimator can conjure pose diversity that
physics did not provide. The constant-distance printout is the
practical degeneracy alarm: motion shows up as stepping distances
(\autoref{tab:realcalib}) or it did not happen.

\paragraph{Round three: robust video calibration, both cameras.} The
reshoot round supplied minute-long videos from both phones and, with
them, the failure that motivated \autoref{sec:calibrobust}: on the
first video, plain Zhang over all 117 decoded frames returned
$f_x=458$ at $35$\,px RMS, while a hand-picked six-frame subset of the
same video returned $f_x\approx1599$ at $0.39$\,px --- the informative
tilted frames were being outvoted by the fronto-parallel majority.
\code{mv\_calib\_planar\_robust} settles it automatically: camera A
calibrates to $f_x{=}1606.4$, $f_y{=}1607.0$ (square to $0.04\%$),
RMS $0.315$\,px, distances $0.83$--$0.96$\,m; camera B to
$f_x{=}1582.6$, $f_y{=}1627.4$, RMS $0.367$\,px at a near-constant
$1.26$\,m --- flagged as weakly conditioned (the $2.8\%$ focal
asymmetry and inflated $k_2$ are the fingerprints; its per-view
distances barely step). Instructively, the estimator's guard rejected
the inlier refit and kept a six-view fit in both cases: the redundant
fronto-parallel frames are geometric \emph{inliers} under the correct
$\K$ --- consensus cannot exclude them --- yet refitting on them still
degrades the solution, so outlier rejection alone is insufficient; the
median-score guard is what actually protects the answer. One more
real-data observation worth recording: camera A's focal came out
$4.8\%$ below its round-two value ($1687$ at $0.93$--$1.09$\,m focus
vs.\ $1606$ at $0.83$--$0.96$\,m) at identical video resolution ---
consistent with focus breathing, the effective-focal shift of an
autofocus lens with subject distance (\autoref{sec:hardware}); a
per-session calibration, which the pattern makes cheap, sidesteps it.

\paragraph{The first real point cloud.} The round-three footage
yielded more than intrinsics. Every decoded frame carries a metric
camera pose (\code{mv\_calib\_plane\_pose} on 162 known screen
points), so one handheld video is a \emph{many-camera rig on a time
budget}: frame pairs with accidental baselines of $3$--$9$\,cm are
calibrated stereo pairs. \code{tools/scenecloud.c} chains the
library's real-data path end to end --- blind read $\to$ pose $\to$
undistort $\to$ Fusiello rectification $\to$ SAD stereo $\to$ texture
and speckle gates $\to$ metric back-projection --- and adds one new
idea, \emph{cross-pair confirmation}: a stereo false match is random
along its viewing ray, so it is not reproduced by an independent
pair; points are kept only if a second pair placed structure within
their $10$\,mm voxel neighborhood ($108$k $\to$ $82$k points; the
first attempt without this filter was buried in ray-streak
artifacts). \autoref{fig:scenecloud} shows the result: the laptop
displaying the pattern, its keyboard deck, the table and legs, and
the curtains behind --- the system's first 3D model built from real
imagery, in the metric frame of the display it calibrated itself
against.

\begin{figure}[ht]
\centering
\includegraphics[width=\textwidth]{figs/scene_cloud.png}
\caption{Three splat renders (orbit views) of the 82{,}000-point
metric cloud extracted from a single handheld phone video of the
pattern laptop (\code{tools/scenecloud.c}). Gray values are carried
from the source frames. Left to right: $-40^\circ$, frontal, and
$+40^\circ$ orbits around the cloud center. Visible structure: the
laptop with the pattern on screen, keyboard deck, table with legs,
and curtains at left. Residual speckle is honest: these are raw
confirmed stereo points, no smoothing, no mesh.}
\label{fig:scenecloud}
\end{figure}

\paragraph{Round five: three cameras, and the first real extrinsics.}
A reshoot with the cameras close to the pattern's dwell region
delivered the multi-camera milestone. All three cameras calibrated ---
including the first two \emph{webcam} calibrations ($f_x/f_y$
$926.5/927.0$ at $0.612$\,px RMS over 192 accepted views, and
$966.9/965.7$ at $0.418$\,px; the phone at $1676.1/1680.7$,
$0.333$\,px). Notably the first webcam's inlier refit was
\emph{accepted} by the robust estimator's guard: with genuine pose
diversity in the video, 192 of 212 views contribute --- the guard
distinguishes healthy sets from degenerate ones automatically, in
both directions. Each camera also produced a point cloud
(\code{tools/scenecloud.c}), with an instructive inversion: for a
\emph{fixed} camera watching a \emph{moving} display, reconstruction
happens in the display's frame, so the cloud is a turntable-style
scan of whatever moves rigidly with the pattern (the laptop, crisply)
while the static room, parallax-free in that frame, is suppressed by
cross-pair confirmation.

The extrinsics came from the display counter as a shared clock. Each
camera's decoded counters fit a linear clock to $\pm1$ refresh over
two minutes (the temporal camera model of \autoref{sec:timemodel} on
real data), which located, for each webcam valid-counter instant, the
sub-second window of the phone's video containing the simultaneous
frame; dense extraction there manufactured counter-matched pairs.
\code{tools/rigcalib.c} then solved phone$\to$webcam from three
simultaneous observations: baseline $0.330$\,m, relative rotation
$12.6^\circ$, with median pair-to-pair consistency $2.3^\circ$ /
$6$\,mm --- three independent estimates, taken $\sim$$100$\,s apart,
agreeing at millimetre scale (which also certifies both cameras held
still). The second webcam yielded only one simultaneous view: the
fine tier's $64$\,px counter cells are its weakest feature at range
(valid in $2$--$5\%$ of decoded frames vs.\ $21\%$ for the phone),
which is precisely the deficiency the coarse tier's isolated-square
counter (\autoref{sec:coarsetier}) removes; the next capture runs in
mode M4.

\paragraph{Detecting the laptop itself.} The noisy point clouds
invite a cleaner demonstration of what the system knows: draw it on
the image. \code{mv\_display\_outline} (\code{mv/reader.h}) is the
system's first scene-object detector: given a decoded pattern read,
it marches rays \emph{outward in pattern-plane coordinates} --- where
the lit panel's edges are axis-aligned straight lines by construction
--- sampling brightness through the homography until each ray goes
dark, and takes the median stop per side. The median is the point:
an earlier region-flooding version leaked through any bright object
touching the panel in the image (a lit arm, a bright couch) and bent
the fitted edges; a median over 48 rays shrugs off such bridges.
Brightness references come from the pattern's own black and white
cells, so no absolute thresholds are assumed.
\autoref{fig:detect} shows \code{tools/annotate.c} on a real webcam
frame: detected sub-pixel corners, the identified lattice, the
pattern extent under the fitted homography, and the laptop's lit
panel found by the detector --- the system drawing a box around the
laptop it is looking at. The sim round trip (\code{make check})
requires the recovered panel corners within 4\,px of ground truth.

\begin{figure}[ht]
\centering
\includegraphics[width=0.9\textwidth]{figs/detect_frame.png}
\caption{Everything the system detected in one real 720p webcam
frame (\code{tools/annotate.c}). Green: 159 sub-pixel saddle
corners. Cyan: the identified corner lattice. Yellow: the pattern's
$1920\times1080$ extent mapped through the fitted homography. Red:
the laptop's lit display panel, \emph{detected} by
\code{mv\_display\_outline}'s pattern-frame ray marching --- found
in the image, not assumed from a spec. The hand adjusting the lid
and the couch behind do not disturb the median-based edges.}
\label{fig:detect}
\end{figure}

\paragraph{Round four: the resolution floor, measured in the field.}
An ambitious capture --- three fixed cameras (two 720p laptop webcams,
one phone) watching the pattern laptop being carried around a room at
$2$--$3$\,m --- produced a clean negative result: $2/196$, $7/200$, and
$0/193$ frames decoded, and every decoded counter strip invalid. The
frames explain it exactly. At that range the pattern spans
$\sim$$170$\,px, so one $80$\,px grid cell subtends $\sim$$9$\,px, an
M-array dot $2$--$3$\,px, and a counter cell $\sim$$7$\,px: the saddle
\emph{lattice} survives (camera B repeatedly found all 162 corners),
but the identity dots and counter bits are below the sensor's
resolving floor --- the information is not on the sensor, and no
escalation ladder can decode bits that were never resolved. The
successful rounds ran at $\sim$$21$\,px/cell; the working rule that
falls out is that a camera can use the pattern out to roughly
\begin{equation}
Z_{\max} \approx \frac{f\,W_{\mathrm{pat}}}{320\ \mathrm{px}}
\label{eq:patrange}
\end{equation}
(pattern width $W_{\mathrm{pat}}=217.5$\,mm on our display, $320$\,px
$\approx$ 19 cells at the empirically safe $\sim$$17$\,px/cell): about
$1.1$\,m for the phones ($f\approx1600$\,px) and only $\sim$$0.6$\,m
for a 720p webcam. Reading the same display across a whole room needs
either longer focal lengths, more pixels, or a coarser signal. The
coarser signal now exists: the same session's negative result drove
the coarse tier of \autoref{sec:coarsetier}, which decodes in
simulation at exactly this geometry (pattern $160$\,px wide on a 720p
sensor).

\paragraph{Tracking a moving object (\code{demo/track\_robot.c}).} A
disk-shaped vacuum robot (radius $0.17$\,m, height $0.09$\,m) wanders the
monitored volume for 20\,s at $0.3$\,m/s with random turns, on a floor
$0.8$\,m below the standard rig. Feature points sit on its cylindrical
bumper band; per camera, the far side of the band is self-occluded
(surface-normal test), so each frame observes only a partial arc ---
on average 16.7 of 36 band points survive visibility, image bounds, and
the $1.5$\,px epipolar gate built from the analytic $\F$
\eqref{eq:fundamental}. Each frame's surviving correspondences are
triangulated and the ground-plane circle of \emph{known} radius is fit to
them by Gauss--Newton; the fitted center is scored against the simulated
trajectory.

\begin{table}[ht]
\centering
\begin{tabular}{lr}
\toprule
Quantity & Value \\
\midrule
frames / rate                    & $600$ / $30$ fps \\
mean visible band points         & $16.7$ of $36$ \\
center RMS error, lateral ($x$)  & $5.1$ mm \\
center RMS error, depth ($z$)    & $7.7$ mm \\
center RMS error, horizontal     & $9.2$ mm \\
center max error                 & $26.2$ mm \\
naive-centroid RMS (contrast)    & $116.5$ mm \\
speed RMS error ($\pm2$-frame difference) & $95$ mm/s \\
\bottomrule
\end{tabular}
\caption{Robot-tracking experiment, seed 12345, $\sigma=0.3$ px
(reproduce with \code{make demo}). Trajectories written to
\code{out\_track.ply}.}
\label{tab:trackresults}
\end{table}

Three lessons, each checkable by hand. First, \emph{model knowledge
removes the partial-view bias}: the centroid of a half-arc of a circle of
radius $R$ sits $2R/\pi\approx108$\,mm from the center --- the naive
centroid's measured 116\,mm RMS is exactly this geometric bias, and the
known-radius fit eliminates it (9\,mm). Second, the per-frame precision
follows the error laws: single-point depth noise
$\sigma_Z\approx23$\,mm (\autoref{ex:deptherr}) averaged over
$\sim$17 points predicts $\sigma\approx5.6$\,mm, and the measured
$7.7$\,mm depth RMS carries the expected extra factor from fitting a
center to a partial arc; the residual anisotropy ($z$ worse than $x$) is
the depth law itself. Third, \emph{differentiation amplifies noise}:
$9.2$\,mm of position noise differenced over $4/30$\,s predicts
$\sqrt2\cdot9.2/0.133\approx98$\,mm/s of speed noise --- the measured
$95$\,mm/s, i.e.\ a third of the robot's actual speed, which is why
velocity estimates in practice come from a motion-model filter (constant
velocity Kalman or similar) rather than raw differencing; such a filter is
a natural later addition to the temporal-statistics roadmap item. This
experiment is geometry-level simulation (points, not pixels); the
rendered-image tier --- the same scenario in a physically-based renderer
exercising the dense matcher --- is the planned next validation stage.

\subsection{Error budget and consistency: is there a bug?}
\label{sec:budget}

Measured errors alone cannot distinguish ``the physics'' from ``a
defect.'' \code{demo/diagnose.c} therefore runs three targeted checks,
built on the principle that noise-driven error and bias behave
differently: a true bias survives $\sigma\to0$, and honest noise
propagation is \emph{linear} in $\sigma$ in the operating regime. All
numbers below are deterministic and reproducible.

\paragraph{A. Triangulation vs the depth law.} The per-point prediction
$\sigma_Z = Z^2\sqrt2\,\sigma/(fB)$ \eqref{eq:deptherr}, evaluated on the
same sampled points, against the measured depth RMS:

\begin{table}[ht]
\centering
\begin{tabular}{ccccc}
\toprule
$\sigma$ (px) & measured $z$ RMS & predicted & ratio & lateral RMS \\
\midrule
$0.0$ & $0.000$ mm  & $0.000$ mm  & ---    & $0.000$ mm \\
$0.1$ & $8.75$ mm   & $8.66$ mm   & $1.01$ & $1.37$ mm \\
$0.3$ & $26.2$ mm   & $26.0$ mm   & $1.01$ & $4.12$ mm \\
$0.6$ & $52.4$ mm   & $52.0$ mm   & $1.01$ & $8.24$ mm \\
\bottomrule
\end{tabular}
\caption{Triangulation noise sweep (250 points, seed 42).}
\label{tab:diagtri}
\end{table}

Agreement with the analytic law to $1\%$ at every noise level, exact zero
at $\sigma=0$, and both error components scaling exactly linearly. The
DLT's algebraic (rather than reprojection-optimal
\cite{HartleySturm1997}) cost is invisible at these noise levels ---
the $1\%$ residual bounds it.

\paragraph{B. Tracking estimator across noise levels.} The same sweep for
the robot experiment, with the \emph{arc factor} $=$ measured center-depth
RMS over the naive $\sqrt{n}$-averaged per-point prediction:

\begin{table}[ht]
\centering
\begin{tabular}{cccc}
\toprule
$\sigma$ (px) & center $x$ RMS & center $z$ RMS & arc factor \\
\midrule
$0.0$ & $0.000$ mm & $0.000$ mm & --- \\
$0.1$ & $1.72$ mm  & $2.39$ mm  & $1.13$ \\
$0.3$ & $5.12$ mm  & $7.68$ mm  & $1.21$ \\
$0.6$ & $10.7$ mm  & $40.0$ mm  & $3.14$ \\
\bottomrule
\end{tabular}
\caption{Tracking noise sweep (600 frames, seed 12345).}
\label{tab:diagtrack}
\end{table}

Zero bias at $\sigma=0$ (the fit recovers exact circles to machine
precision), near-linear scaling from $0.1$ to $0.3$\,px with a mild,
stable arc factor of $1.1$--$1.2$ --- and then a genuine
\emph{nonlinearity at $\sigma=0.6$}: depth error jumps to $40$\,mm
(factor $3.1$) instead of the linear $15$\,mm. This is not a defect but a
measured limitation of the estimator: per-point depth noise at
$\sigma=0.6$ reaches $\sim$46\,mm --- a quarter of the robot's
\emph{radius} --- and the circle fit assumes isotropic residuals while
the actual noise is strongly depth-elongated, so the fitted center gets
dragged along $z$. The fix, when needed, is a Mahalanobis-weighted fit
using the known per-point error ellipse (same family as the weighted DLT
of \autoref{sec:heterogeneous}). At the operating point
$\sigma=0.3$\,px the estimator sits comfortably in its linear regime.

\paragraph{C. Calibration: is $-7$\,px a typical draw?} The single
reported focal error (\autoref{tab:calibresults}) is one noise
realization. Across ten seeds at $\sigma=0.3$\,px the $f_x$ errors are
$-4.0$, $-1.0$, $+3.5$, $+1.5$, $-2.5$, $+3.4$, $-7.0$, $+0.7$, $+1.2$,
$+3.4$: mean
$-0.08$\,px (\emph{unbiased}), sample standard deviation $3.5$\,px. The
demo's $-6.95$\,px is a $2\sigma$ draw --- unlucky, not anomalous. At
$\sigma=0.15$\,px the mean $|f_x|$ error is $1.41$\,px, exactly half the
$2.82$\,px of $\sigma=0.3$: linear again.

\paragraph{Verdict.} Every check a bias-type bug could fail is passed:
all estimators are exact on noiseless data (here and in the 30-assertion
unit suite), all errors scale linearly with input noise in the operating
regime, the dominant error channel matches its analytic prediction to
$1\%$, and the calibration estimator is unbiased in expectation. The
error sources, ranked: (1) pixel noise through the depth law ---
dominant, modeled, verified; (2) estimator geometry factors (partial-arc
factor $1.2$; calibration's finite-view variance of $3.5$\,px std,
inflated further by weak pose diversity, \autoref{sec:printed});
(3) unmodeled noise \emph{anisotropy} in the circle fit --- negligible at
$0.3$\,px, dominant by $0.6$\,px; (4) DLT algebraic suboptimality and the
$3.5\sigma$ gating truncation --- both bounded below the $1\%$ level
here. A bug can never be excluded, but everything measurable currently
behaves exactly as the theory of
\autoref{sec:triangulation}--\autoref{sec:stereo} says it must.

\subsection{Statistical decision rules: when the numbers would say ``bug''}
\label{sec:statrules}

The preceding subsection can be formalized into a standing test battery.
Under the null hypothesis $H_0$ = ``implementation correct,'' the
measurement model (i.i.d.\ Gaussian pixel noise of scale $\sigma$) fixes
the sampling distribution of every summary statistic we compute; a bug is
declared when a statistic leaves its null band. Because the whole system
is deterministic, any flag is exactly reproducible --- a fired tripwire
can be replayed under a debugger.

\begin{description}
  \item[T1 --- exactness at $\sigma=0$.] Under $H_0$ every estimator is
    algebraically exact, so noiseless residuals are bounded by
    floating-point round-off amplified by conditioning,
    $\varepsilon\cdot\kappa\lesssim10^{-9}$. This test has \emph{no}
    statistical tolerance: any exceedance is a defect (sign error, wrong
    index, forgotten term). It is the single most powerful test in the
    suite and is what the 30-assertion unit suite automates.
  \item[T2 --- prediction-ratio test.] For a measured RMS over $N$
    residuals against an analytic prediction, the sample RMS under $H_0$
    fluctuates with relative standard deviation $\approx1/\sqrt{2N}$
    ($\chi^2$). Flag when $|r-1|>3/\sqrt{2N}$. A ratio that is stable but
    $\neq1$ across $\sigma$ and seeds is the signature of a wrong constant
    (missing $\sqrt2$, wrong baseline, mis-scaled $f$).
  \item[T3 --- scaling-exponent test.] Regress $\log e$ on $\log\sigma$:
    under $H_0$, slope $=1$ in the linear regime. Slope $\approx2$
    betrays a missing square root or an estimator leaving its regime;
    slope $\to0$ at small $\sigma$ betrays an additive floor --- fitting
    $e(\sigma)=\sqrt{b^2+a^2\sigma^2}$ and finding $b$ above the
    numerical floor reveals a hidden bias even when T1 cannot be run.
  \item[T4 --- ensemble bias test.] Over $M$ independent noise
    realizations, the standardized mean $|\bar e|/(s/\sqrt M)$ is
    Student-$t$ under $H_0$; flag beyond $3$. Distinguishes systematic
    bias from an unlucky draw.
  \item[T5 --- residual-consistency ($\chi^2$) test.] A least-squares fit
    with $p$ parameters against $n$ residual coordinates must leave
    RMS $=\sqrt2\,\sigma\sqrt{1-p/n}$ (per-point distance); the ratio
    fluctuates with relative std $1/\sqrt{2(n-p)}$. A ratio
    \emph{above} the band means the model or estimator is not absorbing
    what the noise model says it should (underfit, suboptimal estimator,
    contamination); \emph{below} means overfit or a too-permissive
    model.
  \item[T6 --- outlier-count test.] The epipolar gate's rejection count
    among true correspondences is Poisson with rate given by the noise
    tail beyond the gate; a large excess indicates contamination or a
    mismatched noise model, a large deficit an accidentally widened gate.
\end{description}

\begin{table}[ht]
\centering
\small
\begin{tabular}{llll}
\toprule
Test & Bug-flag condition & Current value & Verdict \\
\midrule
T1 exactness ($\sigma{=}0$) & any residual $>10^{-9}$ & $\le10^{-12}$ & pass \\
T2 ratio, triangulation & $|r-1|>0.13$ \ ($N{=}250$) & $r=1.01$ & pass \\
T3 slope, triangulation & $|\text{slope}-1|>0.1$ & $1.00$ & pass \\
T3 slope, tracking $0.1\!\to\!0.3$ & $|\text{slope}-1|>0.1$ & $1.06$ & pass \\
T3 slope, tracking $0.3\!\to\!0.6$ & --- & $2.4$ & fired$^{a}$ \\
T4 bias, calibration $f_x$ & $|t|>3$ \ ($M{=}10$) & $t=0.07$ & pass \\
T5 residual, triangulation & $|r-1|>0.13$ & $r=1.03$ & pass \\
T5 residual, calibration & $|r-1|>0.08$ & $1.17\to0.999$ & resolved$^{b}$ \\
T6 gate rejections & excess over noise tail & $6/\sim10^4$ & pass \\
\bottomrule
\end{tabular}
\caption{Tripwire standings. $^{a}$Investigated and resolved twice
over: first explained as a regime boundary (noise anisotropy,
\autoref{tab:diagtrack}), then retired by the Mahalanobis-weighted
fit of \code{mv/optimal.h} ($1.4\times$ lower center error at the
same noise). $^{b}$Resolved by LM refinement, see text.}
\label{tab:tripwires}
\end{table}

\paragraph{The flag, and its resolution.} T5 fired on calibration
for most of the project's life: across ten seeds the fitted
reprojection RMS averaged $0.484$\,px against the noise-model
prediction $\sqrt2\,\sigma\sqrt{1-42/756}=0.412$\,px --- ratio
$1.17$, roughly twenty standard errors outside the null band. The
statistics had detected that the alternation estimator of
\autoref{sec:radial} (Zhang closed form $+$ linear radial step) is
\emph{not} the joint maximum-likelihood fit --- it minimizes
algebraic surrogates, leaving $\sim17\%$ systematic residual above
the noise floor. That handed the Levenberg--Marquardt roadmap item a
\emph{pre-registered} acceptance criterion, stated here well before
the implementation existed: after LM refinement the T5 ratio must
fall to $1.00\pm0.03$, else the implementation is buggy. The joint
refinement (\code{mv\_calib\_refine}, \code{mv/refine.h}: shared
intrinsics $+$ distortion and per-view Rodrigues poses, Schur
complement over the view blocks) was then built against that bar and
passed on the first green run: ensemble ratio $1.1087$ before,
$\mathbf{0.9990}$ after, refined RMS $\leq$ alternation RMS on every
member, exact recovery at $\sigma=0$
(\code{tests/test\_refine.c}). This is the general pattern the
project commits to: every claimed improvement must move a named
statistic into a predicted band, and every statistic outside its band
must be either explained to this standard or treated as a defect ---
T5 is the pattern's first full life cycle, flag to acceptance.

%----------------------------------------------------------------------
\section{Scenario studies}
\label{sec:scenarios}

Four concrete monitoring scenarios, each simulated end-to-end against
ground truth in the deterministic demo style (all at the geometry tier:
points, not pixels --- detections are assumed, estimation is exercised;
each result is therefore an upper bound whose image-tier feasibility is
noted). Together they instantiate the mover-separation architecture of
\autoref{sec:movers}.

\subsection{Insect counting and entry/exit statistics}
\label{sec:insects}

Point-like targets enter the monitored box through a random face, fly an
Ornstein--Uhlenbeck random walk, and leave
(\code{demo/track\_insects.c}: 300\,s, spawn rate $0.1$/s, joint
detection probability $0.9$ per frame, \emph{plus two uniform clutter
detections per camera per frame}). Stereo association is by the epipolar
gate alone; a nearest-neighbor tracker with 3-hit confirmation and 5-miss
termination maintains identities; confirmed births/deaths map to box
faces. Results (seed 777): ground truth 31 insects, \textbf{29 tracks
confirmed} (two lived too briefly to confirm); entry/exit face
histograms match ground truth to within $\pm2$ per face; track position
RMS $24.4$\,mm --- the single-point depth law of \autoref{ex:deptherr},
as an unmodeled point target admits no $\sqrt n$ averaging. Of
$\sim$36{,}000 clutter detections, only 231 pairs slipped through the
epipolar gate and \emph{none} survived track confirmation: geometric
gating plus temporal confirmation suppresses clutter completely at these
densities. Image-tier caveat: at $4.7$\,m a 5\,mm insect subtends
$0.85$\,px --- below the $2$-px feature floor of
\autoref{sec:resolution} --- so real detection must be
temporal-differencing of sub-pixel blobs; counting accuracy will be
detection-limited, not geometry-limited.

\subsection{Diurnal cycle and artificial lighting}
\label{sec:lightscen}

The cameras double as calibrated photometers (Appendix; radiometric
calibration assumed): mean scene luminance is one number per frame.
\code{demo/lightlog.c} simulates 120 days at 1-minute resolution ---
natural light with drifting sunrise ($-1.5$\,min/day), per-day weather
($0.4$--$1.0\times$), slow AR(1) cloud noise, two lamps on noisy
schedules, two rare middle-of-the-night events --- and analyzes the sum.
Step detection (median-filter differencing): \textbf{484/484 on/off
events found, 0 missed, 33 false alarms} (cloud edges), timing RMS
$1.02$\,min, lamp identity by step magnitude \textbf{484/484 correct},
including both night events. The natural component is recovered by
subtracting the reconstructed lamp state, with a \emph{nightly
re-anchor} (pre-dawn darkness forces the natural component to zero ---
a physical constraint that stops false-alarm error from propagating
across days). Sunrise is then estimated per day by two-level template
inversion (crossings of 15\% and 35\% of the day's robust peak invert
the known diurnal shape analytically; amplitude and shape cancel):
offset $0.7$\,min (unbiased), day-to-day jitter $25.3$\,min RMS ---
the genuine information floor imposed by 100-min-correlated cloud
cover, not an estimator defect --- and the seasonal drift is recovered
as \[ \widehat{\text{drift}} = -1.61\ \text{min/day} \quad
(\text{true } -1.50,\ \sigma_{\text{slope}} \approx 0.07). \]
A 30-day run is statistically \emph{underpowered} for this drift
($\sigma_{\text{slope}}\approx0.7$): a season of data is what makes the
trend measurable --- the T4 logic of \autoref{sec:statrules} applied to
experiment design.

\subsection{Human occupancy}
\label{sec:occupancy}

A known three-person cohort (heights $1.62/1.75/1.88$\,m) makes 13
scheduled visits over a simulated day (\code{demo/track\_people.c}):
enter at the door, walk to desk or window, linger, exit. Each person is
a cylinder with self-occluding torso-band features plus a head point;
per frame the known-radius circle fit gives ground position and the
triangulated head gives height. Results (seed 31337): \textbf{13/13
episodes detected, 13/13 identities correct} (nearest cohort height;
the $130$\,mm height gaps dwarf the $0.3$\,mm height RMS), 13/13
trajectory targets correct, ground-position RMS $14.1$\,mm, entry
latency $0.2$\,s (the 3-frame confirmation), per-person occupancy
minutes recovered to $0.1$\,min. Caveats: identity-by-height is valid
only for small cohorts with distinct heights (it is a household
biometric, not a general one), and the idealized head/torso detections
make these upper bounds --- real image-tier person detection is where
the learned-detector boundary of \autoref{sec:ladder} sits.

\subsection{What the scenarios share}

All four reuse the same small kernel --- epipolar gating, triangulation,
model-constrained fitting, temporal confirmation --- plus one scenario
layer each (multi-target association; photometric step decomposition;
identity classification). The common upgrades they all point to:
constant-velocity Kalman filtering (speed estimates,
\autoref{tab:trackresults}), the Mahalanobis-weighted fits of
\autoref{sec:budget}, and the background-subtraction front end of
\autoref{sec:movers}.

%----------------------------------------------------------------------
\section{Person detection and face recognition with open-source components}
\label{sec:faces}

The learned-detector boundary drawn in \autoref{sec:ladder} does not have
to remain outside the system: mature open-source detectors and face
recognizers can be attached \emph{without} touching the dependency-free
core, and the occupancy scenario (\autoref{sec:occupancy}) already defines
the exact seam they plug into. This section records the component
landscape, the integration architecture, and --- the question that
determines what to promise --- how accuracy differs between a
pre-registered cohort and unknown faces.

\subsection{Component landscape}
\label{sec:osscomponents}

Three roles, each with solid open-source fillings:
\emph{person detection} --- YOLO-family single-shot detectors
\cite{Redmon2018} (the original darknet implementation is plain C;
Apache-licensed successors such as YOLOX run everywhere), or classical
HOG pedestrian detection in OpenCV as a weak-but-tiny fallback;
\emph{face detection + landmarks} --- RetinaFace \cite{Deng2020} or its
lightweight successors (SCRFD, YuNet), which return a box plus five
landmarks (eyes, nose, mouth corners); \emph{face embedding} ---
deep metric networks mapping an aligned $112\times112$ crop to a unit
vector such that same-person pairs are close: FaceNet established the
recipe \cite{Schroff2015}, ArcFace-class models are the current
open-source standard \cite{Deng2019}. The practical integration seam for
all three is an ONNX-exported model behind the ONNX Runtime C API
(MIT-licensed, single shared library, plain-C interface) --- the one
boundary object between the C99 world and the learned world. License
note: several popular YOLO packagings are AGPL; the Apache/MIT
alternatives keep the system's licensing simple.

\subsection{Piping it in}
\label{sec:sidecar}

The architecture is a \emph{sidecar oracle}: the core stays pure C99 and
treats the detector exactly as the scenario demos treat their simulated
detections. Per frame and per camera, the sidecar consumes the image and
emits rows of \code{(frame, camera, class, box, landmarks, embedding)} ---
a text/CSV stream over a pipe or file, trivially parseable, recordable,
and replayable (determinism is preserved by recording the sidecar's
output once and replaying it in tests --- the oracle's answers become
fixtures). The geometric core then does what it already does:
\begin{enumerate}
  \item \emph{stereo association}: detections in the two cameras are
        paired by the epipolar gate on box centers/landmarks
        (\autoref{sec:epipolar}) --- the same gate that suppressed
        clutter in \autoref{sec:insects};
  \item \emph{triangulation}: paired boxes give the person's 3D position;
        paired eye landmarks give a metric face position and, with the
        floor plane, the height biometric of \autoref{sec:occupancy};
  \item \emph{track-level identity}: embeddings are not judged per frame
        but accumulated per \emph{track} (quality-weighted average of
        embeddings across the track's frames): the tracker turns many
        marginal glimpses into one confident identity, and the identity
        then travels with the track through frames where the face is
        turned away entirely.
\end{enumerate}
Identity fusion is a small score-level combination: face-embedding
similarity where a face is visible, the height/gait geometric cues
otherwise --- the heterogeneous-evidence pattern of the appendix
(\autoref{sec:blend}) applied to identity.

\subsection{Pre-registered vs unknown faces}
\label{sec:openset}

The accuracy question splits into three regimes with \emph{very}
different numbers.

\paragraph{Verification (1:1) and closed-set identification (1:$N$,
person guaranteed enrolled).} On the classic verification benchmark LFW
\cite{Huang2007} modern open-source models exceed $99.5\%$ --- the
benchmark is saturated and mostly measures that nothing is broken. The
more honest unconstrained figure is IJB-C \cite{Maze2018} verification at
strict false-accept rates: ArcFace-class models reach a true-accept rate
around $96$--$97\%$ at FAR $=10^{-4}$ \cite{Deng2019}. For a
\emph{pre-registered household cohort} --- $N$ of order 10, controlled
enrollment photos, closed-set --- rank-1 identification is effectively
$\ge99\%$ whenever the probe image is adequate: with guaranteed gallery
membership, the decision is an argmax among a few well-separated
identities, and errors are driven almost entirely by probe quality, not
confusion.

\paragraph{Open-set identification (the person may be unknown).} This is
a different problem: the system must first decide \emph{whether} the
probe is anyone enrolled, which requires thresholding an absolute
similarity score rather than taking an argmax. Impostor scores from
unknown faces overlap the tail of genuine scores, so every operating
point trades false alarms (visitor labeled as a resident) against misses
(resident labeled unknown). NIST's identification evaluations
\cite{Grother2019} quantify the cost: even leading algorithms give up
substantial accuracy relative to closed-set --- at a false-positive
identification rate of $1\%$, miss rates range from a few percent with
high-quality frontal probes to \emph{tens of percent} with
surveillance-quality probes, and degradation grows with gallery size and
probe--gallery quality gap. The rule of thumb this system should adopt:
\textbf{closed-set household identification is a solved problem at our
scale; reliable ``this is a stranger'' detection is not free} --- it
needs a margin-calibrated threshold, benefits enormously from track-level
embedding fusion, and should be reported with its false-alarm/miss
operating point, never as a single accuracy number.

\paragraph{The resolution gate.} All of the above presumes enough face
pixels, and here our own \autoref{sec:resolution} bites hard.

\begin{example}[face pixels at range]
\label{ex:facepixels}
A face is $\approx0.20$\,m tall with $0.064$\,m interocular distance. At
the experimental rig's $f=800$\,px and $Z=4.7$\,m: face height
$0.20\cdot800/4.7\approx34$\,px, interocular
$\approx11$\,px --- far below the $\gtrsim80$--$112$\,px face crops the
embedding networks are built for; expect severely degraded embeddings.
For a $100$\,px face at $4.7$\,m the rig needs
$f=100\cdot4.7/0.20=2350$\,px --- a 4K-class sensor, or a normal sensor
with a narrow-field lens.
\end{example}

The systems answer follows the appendix's blending argument
(\autoref{sec:blend}): do not demand identity everywhere. Add one
\emph{identity camera} --- a narrow-field unit aimed at the natural
chokepoint (the door of \autoref{sec:occupancy}, where every track
begins and ends) --- capture the face at high resolution once per entry,
and let the tracker carry that identity through the wide-angle geometric
volume. Identity becomes an attribute assigned at a favorable moment and
\emph{propagated by geometry}, rather than a per-frame recognition
burden. A final scope note befitting the controlled environment: the
enrollment gallery is the household's own, processing is local, and the
open-set threshold should be set to prefer ``unknown'' over
misattribution.

\subsection{Plugin matrix: OSS solutions, features, and license isolation}
\label{sec:pluginmatrix}

\autoref{tab:plugins} surveys the practical candidates. Feature columns:
person detection, face detection, landmarks, embeddings, ONNX
exportability (i.e.\ runs behind the single ONNX Runtime seam), and
CPU-only viability. The last column states the \emph{required} degree of
separation to avoid license contamination of the MIT-clean core:
\begin{itemize}
  \item \textbf{link} --- permissive license (MIT/BSD/Apache/BSL): may be
        linked into the same process; no obligations beyond attribution.
  \item \textbf{process} --- AGPL: never link or import; run as a
        separate OS process communicating \emph{data only} (frames out,
        text rows back). The combined system must not be distributed as
        one work, and if the sidecar is ever offered as a network
        service, AGPL source obligations attach to the sidecar alone.
  \item \textbf{weights apart} --- code is permissive but the pretrained
        \emph{weights} carry a research/non-commercial license: treat
        weights as user-fetched artifacts at install time, never
        redistributed with the system.
\end{itemize}
Architecturally we put \emph{every} detector behind the sidecar protocol
of \autoref{sec:sidecar} regardless of license --- hygiene first --- so
the license question collapses to ``which side of the pipe, and who
ships the bytes.''

\begin{table}[ht]
\centering
\scriptsize
\setlength{\tabcolsep}{2.5pt}
\newcommand{\cm}{$\checkmark$}
\begin{tabular}{l l c c c c c c l}
\toprule
Solution & License (code/weights) & Person & Face & Lmk & Embed & ONNX & CPU & Isolation \\
\midrule
darknet YOLOv3/v4 & MIT-like / same & \cm & -- & -- & -- & part & slow & link \\
YOLOX & Apache / Apache & \cm & -- & -- & -- & \cm & ok & link \\
Ultralytics v8/v11 & AGPL / AGPL$^{a}$ & \cm & -- & pose & -- & \cm$^{a}$ & ok & \textbf{process} \\
OpenCV (HOG/YuNet/SFace) & Apache / Apache & weak & \cm & 5pt & \cm & n/a & \cm & link \\
MediaPipe & Apache / Apache & pose & \cm & \cm & -- & -- & \cm & link \\
dlib / face\_recognition & BSL / public & HOG & \cm & 68pt & \cm & -- & slow & link \\
InsightFace (SCRFD+ArcFace) & MIT / research$^{b}$ & -- & \cm & 5pt & \cm & \cm & \cm & weights apart \\
facenet-pytorch (MTCNN+FaceNet) & MIT / MIT & -- & \cm & 5pt & \cm & \cm & ok & process$^{c}$ \\
ONNX Runtime (the seam) & MIT & \multicolumn{6}{c}{--- inference runtime for all ONNX rows ---} & link \\
\bottomrule
\end{tabular}
\caption{OSS plugin candidates. $^{a}$AGPL applies to exported/converted
weights too --- conversion does not launder the license; mandatory
process isolation, data-only IPC, no combined distribution.
$^{b}$InsightFace \emph{code} is MIT but the model-zoo weights are
research/non-commercial: fine in this controlled personal deployment,
user-fetched at install, never redistributed. $^{c}$License permits
linking; the Python runtime makes a separate process the practical
choice anyway.}
\label{tab:plugins}
\end{table}

\autoref{tab:pluginquality} adds the published quality metrics needed to
actually decide. Conventions: person detection is COCO mAP$_{50\text{--}95}$
(small$\to$large model variants as a range); face detection is
WIDER~FACE validation AP for the easy/medium/hard splits (hard is the
discriminative one --- small, blurred, occluded faces, i.e.\ our
at-range regime); embeddings are LFW verification \cite{Huang2007}
(saturated; a sanity floor) and IJB-C TAR at FAR $=10^{-4}$
\cite{Maze2018} (the honest unconstrained number). ``n/p'' = no
comparable published figure.

\begin{table}[ht]
\centering
\scriptsize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{l c c c c}
\toprule
Solution & COCO mAP & WIDER e/m/h & LFW & IJB-C TAR@$10^{-4}$ \\
\midrule
darknet YOLOv3 / v4 & $33.0$ / $43.5$ & -- & -- & -- \\
YOLOX (s$\to$x) \cite{Ge2021} & $40.5$--$51.1$ & -- & -- & -- \\
Ultralytics v8/v11 (s$\to$x) & $44.9$--$54.7$ & -- & -- & -- \\
OpenCV HOG & legacy$^{a}$ & -- & -- & -- \\
OpenCV YuNet / SFace & -- & $\approx89/87/77$ & $99.60$ & n/p \\
MediaPipe (BlazeFace/Pose) & n/p$^{b}$ & n/p$^{b}$ & -- & -- \\
dlib / face\_recognition & -- & n/p & $99.38$ & n/p \\
InsightFace SCRFD-10GF \cite{Guo2021} & -- & $\approx95/94/83$ & -- & -- \\
InsightFace RetinaFace-R50 \cite{Deng2020} & -- & $\approx97/96/92$ & -- & -- \\
InsightFace ArcFace-R100 \cite{Deng2019} & -- & -- & $99.83$ & $\approx96$--$97$ \\
facenet-pytorch (MTCNN / FaceNet) & -- & $\approx85/82/61$ & $99.63$ & n/p \\
\bottomrule
\end{tabular}
\caption{Published quality metrics, as reported by the respective
authors and model zoos (values rounded; model-zoo figures marked
$\approx$). $^{a}$Pre-CNN pedestrian detection; an order of magnitude
behind modern detectors on all benchmarks, listed only as a
zero-dependency fallback. $^{b}$Optimized for mobile near-range
operation; not benchmarked in comparable terms.}
\label{tab:pluginquality}
\end{table}

Reading the two tables together, the decision logic is: the
license-clean default stack --- YOLOX (person), SCRFD or YuNet (face),
SFace or user-fetched ArcFace (embedding), all as ONNX graphs behind
ONNX Runtime --- concedes very little. On person detection YOLOX-m
($46.9$) sits within a few mAP of the AGPL-fenced Ultralytics models,
nowhere near a factor that survives our track-level temporal fusion. On
face detection SCRFD trails RetinaFace-R50 mainly on the hard split
($83$ vs $92$) --- relevant, since at-range faces \emph{are} the hard
split, but the identity-camera design (\autoref{ex:facepixels}) moves
recognition to the easy split at the chokepoint. On embeddings the gap
that matters is SFace ($99.60$ LFW, no published IJB-C) vs ArcFace
($99.83$ LFW, $\approx96$--$97$ IJB-C): for \emph{closed-set} household
identification both are past the point of mattering; for \emph{open-set}
stranger detection (\autoref{sec:openset}) the strongest available
embedding is the one to fetch, which argues for ArcFace weights as the
user-supplied artifact. MTCNN's $61$ on WIDER-hard is the reason it is
not the default despite MIT-clean weights. One standing caveat, in the
spirit of \autoref{sec:statrules}: all these numbers are measured on
academic benchmarks at favorable resolutions --- they \emph{rank} the
candidates but do not predict absolute in-system accuracy in our
at-range regime, so the integrated sidecar gets its own acceptance test
(recorded-oracle replays scored against identity-camera ground truth)
before any number from this table is quoted as a system property. The
Ultralytics row exists because its tooling is convenient for
experiments --- inside its own fence.

\subsection{Deployment architecture: hardware boundaries and communication}

\paragraph{Live rig v1 (built).} The streaming skeleton of this
architecture now exists and has been soak-tested in loopback:
\code{tools/stream\_cam.swift} (macOS capture) and
\code{tools/stream\_cam\_v4l2.c} (Linux capture, same wire format)
send grayscale frames over TCP to \code{tools/livehub.c}, which
drains the network on one thread while a single decoder thread (the
reader is non-reentrant) always works on each camera's freshest
frame, running the full estimator chain \emph{live}: blind decode
(both tiers) $\to$ robust Zhang $\to$ LM refinement $\to$
counter-matched extrinsics, with session records and decoded frames
persisted for the offline full-precision pass.
\code{tools/replaycam.c} replays recorded frames over the identical
protocol (the loopback tester), and \code{tools/nettest.c} is a
deterministic protocol-torture client. The first soak runs paid for
themselves immediately: TCP backpressure from multi-second decodes
throttled live senders (fixed by the thread split), block-buffered
logs vanished on kill (fixed), glibc's feature-test macros broke the
Linux build invisibly to macOS (fixed, found by the \code{deploy/}
container audit), and the fuzzer's review yielded a quadratic-resync
CPU DoS, slot-exhaustion lockout, and unchecked-allocation crashes,
all fixed and re-fuzzed. Live loopback calibrations match the
offline pass within $0.6\%$.
\label{sec:deployarch}

\begin{figure}[ht]
\centering
\resizebox{0.97\textwidth}{!}{%
\begin{tikzpicture}[
  font=\footnotesize,
  box/.style={draw, rounded corners=2pt, align=center, inner sep=4pt,
              minimum height=7mm, fill=blue!4},
  hw/.style={draw, thick, rounded corners=3pt, inner sep=7pt},
  lic/.style={draw, dashed, thick, red!60!black, rounded corners=3pt,
              inner sep=7pt},
  lbl/.style={font=\scriptsize\itshape},
  arr/.style={->, thick},
  net/.style={->, thick, dash dot}
]
  % sensor plane
  \node[box] (cam1) {camera 1\\(GS mono)};
  \node[box, right=5mm of cam1] (cam2) {camera 2\\(GS mono)};
  \node[box, right=5mm of cam2] (idcam) {identity cam\\(narrow FOV, door)};
  \node[box, right=5mm of idcam] (disp) {rotating display\\(calibration fixture)};
  \node[box, below=5mm of cam2] (trig) {trigger $\mu$C};
  \node[hw, fit=(cam1)(cam2)(idcam)(disp)(trig),
        label={[lbl]above left:sensor plane (hardware boundary)}] (sensors) {};

  % metric host
  \node[box, below=17mm of trig, xshift=-24mm] (core)
    {\textbf{C99 metric core} (\code{libmv})\\
     undistort $\cdot$ rectify $\cdot$ stereo $\cdot$ triangulate\\
     calibrate $\cdot$ track $\cdot$ background model};
  \node[box, right=8mm of core] (log)
    {storage\\PLY $\cdot$ event log $\cdot$\\recorded oracle fixtures};
  \node[hw, fit=(core)(log),
        label={[lbl]above left:metric host (trusted, MIT-clean)}] (host) {};

  % sidecar
  \node[box, below=10mm of core, xshift=14mm] (onnx)
    {inference sidecar\\ONNX Runtime: person det $\cdot$\\
     face det $\cdot$ embeddings};
  \node[box, right=14mm of onnx] (agpl)
    {optional AGPL zone\\(e.g.\ Ultralytics)\\own process or machine};
  \node[lic, fit=(onnx)(agpl),
        label={[lbl]below left:process boundary $=$ license boundary
        (promotable to a separate machine)}] (side) {};

  % arrows
  \draw[arr] (trig) -- node[lbl, right] {trigger pulses} (trig |- sensors.south) -- ++(0,-2mm);
  \draw[arr] (cam1.south) -- ++(0,-3mm) -| node[lbl, pos=0.25, above]
    {USB3 raw frames} (core.north west);
  \draw[arr] (cam2.south) -- ++(0,-4mm) -| (core.north);
  \draw[arr] (idcam.south) -- ++(0,-5mm) -| node[lbl, pos=0.25, above]
    {USB3 (door events)} (core.north east);
  \draw[arr] (core.east) -- node[lbl, above] {results} (log.west);
  \draw[<-, thick] (disp.south) -- ++(0,-3mm) -| node[lbl, pos=0.22, below]
    {HDMI: pattern $+$ frame counter} (log.north east);
  \draw[arr] (core.south west) ++(2mm,0) |- node[lbl, pos=0.75, above]
    {frames (shm / pipe)} (onnx.west);
  \draw[arr] (onnx.north) -- node[lbl, right]
    {CSV: boxes, landmarks, embeddings} (onnx |- core.south);
  \draw[net] (onnx.east) -- node[lbl, below=4mm] {TCP (data only)} (agpl.west);
\end{tikzpicture}}
\caption{Deployment architecture. Solid boxes are hardware boundaries;
the dashed red box is the license/process boundary --- all learned
components live behind it, exchanging only data (frames one way,
text rows back), so it can be promoted from ``separate process'' to
``separate machine'' (a small GPU/NPU box) without any protocol change.
The trigger microcontroller synchronizes both cameras, the identity
camera, and --- during calibration --- the rotating display's frame
counter.}
\label{fig:deploy}
\end{figure}

The communication patterns are deliberately boring: hardware trigger
pulses for simultaneity (\autoref{sec:trigger}); USB3 raw frames into
the metric host; a one-directional frame stream to the sidecar (shared
memory locally, TCP if the sidecar moves to its own machine); text rows
back. Nothing crosses the license boundary except data, every byte
crossing it is recordable --- which is exactly what makes the sidecar
replayable in tests (\autoref{sec:sidecar}) --- and the C99 core never
links against anything but \code{libm}.

\autoref{fig:machines} gives the complementary \emph{machine-centric}
view: what software and persistent data each physical box holds, and
every inter-machine data flow. Two locality rules are worth making
explicit. First, \emph{scene and identity data rest only on the metric
host}: the inference box holds model weights but stores no frames, no
tracks, and no gallery --- faces cross to it only transiently for
embedding, and the similarity decision is made back on the trusted host.
Second, \emph{sensors and fixtures are stateless}: cameras, display, and
plate hold nothing persistent, so replacing any of them is a hardware
swap plus recalibration, never a data migration.

\begin{figure}[ht]
\centering
\resizebox{0.97\textwidth}{!}{%
\begin{tikzpicture}[
  font=\scriptsize,
  mach/.style={draw, thick, rounded corners=3pt, align=left,
               inner sep=5pt, fill=blue!3},
  lbl/.style={font=\tiny\itshape},
  arr/.style={->, thick}
]
  \node[mach, text width=29mm] (cams) at (0,0)
    {\textbf{cameras ($\times3$)}\\
     2 wide GS mono $+$ identity\\
     sw: sensor firmware\\
     data: none (stateless)};
  \node[mach, text width=29mm] (mcu) at (0,-3.0)
    {\textbf{trigger $\mu$C}\\
     sw: trigger firmware\\
     data: sync schedule};
  \node[mach, text width=29mm] (disp) at (0,-5.7)
    {\textbf{display $+$ plate}\\
     sw: none (HDMI sink,\\
     motor)\\
     data: none (stateless)};
  \node[mach, text width=42mm] (host) at (6.9,-1.7)
    {\textbf{metric host} (fanless PC)\\
     sw: \code{libmv} C99 core,\\
     capture (v4l2/SDK), pattern\\
     renderer, sidecar client,\\
     report server\\
     data: calibration, background\\
     model, tracks $+$ event log,\\
     PLY models, enrollment\\
     gallery, oracle fixtures};
  \node[mach, text width=38mm] (laptop) at (6.9,-6.1)
    {\textbf{operator laptop}\\
     sw: PLY viewer, reports\\
     data: transient copies only};
  \node[mach, text width=33mm] (infer) at (13.6,-0.8)
    {\textbf{inference box}\\
     (GPU/NPU; optional ---\\
     else a process on host)\\
     sw: ONNX Runtime $+$\\
     YOLOX/SCRFD/ArcFace;\\
     AGPL tools in container\\
     data: model weights only\\
     --- no scene data at rest};
  \node[mach, text width=33mm] (nas) at (13.6,-5.0)
    {\textbf{archive NAS} (opt.)\\
     sw: none (storage)\\
     data: PLY/report/log\\
     archive; no raw video};

  \draw[arr] (mcu.north) -- node[lbl, right] {trigger pulses} (cams.south);
  \draw[arr] (mcu.south) -- node[lbl, right] {trigger (plate index)} (disp.north);
  \draw[arr] (cams.east) -- node[lbl, above, sloped, pos=0.4]
    {USB3: frames $+$ control} ($(host.west)+(0,0.9)$);
  \draw[arr] ($(host.west)+(0,-0.4)$) -- node[lbl, above, sloped, pos=0.6]
    {schedule (USB serial)} (mcu.east);
  \draw[arr] ($(host.west)+(0,-1.3)$) -- node[lbl, below, sloped, pos=0.55]
    {HDMI: pattern $+$ counter} (disp.east);
  \draw[arr] ($(host.east)+(0,0.7)$) -- node[lbl, above, sloped]
    {frames (shm / TCP)} ($(infer.west)+(0,-0.3)$);
  \draw[arr] ($(infer.west)+(0,-1.1)$) -- node[lbl, below, sloped]
    {CSV: boxes, lmk, embed} ($(host.east)+(0,-0.1)$);
  \draw[arr] ($(host.east)+(0,-1.2)$) -- node[lbl, below, sloped, pos=0.45]
    {nightly results sync} (nas.west);
  \draw[arr] ($(host.south)+(-0.5,0)$) -- node[lbl, left]
    {reports (read-only)} ($(laptop.north)+(-0.5,0)$);
  \draw[arr] ($(laptop.north)+(0.7,0)$) -- node[lbl, right]
    {enrollment photos} ($(host.south)+(0.7,0)$);
\end{tikzpicture}}
\caption{Machine-centric deployment view: per-machine software and
persistent data, and all inter-machine flows. Scene and identity data
rest only on the metric host; the inference box is compute-only;
sensors and fixtures are stateless.}
\label{fig:machines}
\end{figure}

%----------------------------------------------------------------------
\section{Roadmap}
\label{sec:roadmap}

Dependency-ordered. Each entry gives a blurb (what it is) and a pitch
(why it fits this system now); statuses reflect v0.4.0.

\begin{enumerate}
  \item \textbf{[done] Saddle-point corner detection} --- implemented
        in \code{src/reader.c}; setup and run instructions are in the
        comment atop \code{mv/reader.h} (\code{make demo\_patternsim},
        \code{MV\_READER\_DEBUG=1} for tracing). Sub-pixel template
        detection, validated blind through the rendered pipeline at
        $0.25$--$0.35$\,px (\autoref{tab:patresults}).
        \emph{Unlocks}: the image-tier calibration chain that every
        entry below assumes.
  \item \textbf{[partially done] Calibration subsystem completion} ---
        built so far: pattern generator (both tiers), synthetic
        renderer, blind readers (\code{mv/pattern.h},
        \code{mv/render.h}, \code{mv/reader.h}), the display app
        itself (\code{tools/pattern.html}, modes M0--M4, vsync
        counter, Retina-aware 1:1 pixels), the robust video estimator
        (\autoref{sec:calibrobust}), the single-frame localizer
        \code{mv\_calib\_plane\_pose}, and the rig-extrinsics tool
        \code{tools/rigcalib.c} (counter-matched simultaneous views
        $\to$ chordal-mean relative poses). All components field-proven,
        rigcalib included: the round-five capture produced the first
        real camera-to-camera extrinsics ($0.330$\,m baseline at
        $2.3^\circ$/$6$\,mm pair consistency). The record-emitting wrapper, version-block
        check, phase-patch sampling, and the robust Theil--Sen clock
        fit are now built (\code{mv/session.h}, grammar per
        \autoref{tab:records}, round-trip tested). Remaining: only
        the joint orbit fit, which waits on the rotating fixture
        hardware.
        \emph{Pitch}: this turns the fixture design into a push-button
        session --- one revolution in, geometry and clocks out --- and
        every missing piece already has its spec (\autoref{sec:calibspec})
        and its acceptance statistic waiting; nothing needs inventing,
        only building. \emph{Unlocks}: real-rig deployment of every
        scenario in \autoref{sec:scenarios} (all assume a calibrated,
        clock-modeled rig), and the M0 radiometric path that the
        lighting/diurnal scenario (\autoref{sec:lightscen}) needs on
        real hardware.
  \item \textbf{[done] Range-tiered pattern (spec v2)} --- the
        coarse layer for room-scale reading, implemented as display
        mode M4 (\autoref{sec:coarsetier}): $270$\,px cells, $90$\,px
        marks, isolated-square 8-bit counter --- $\sim$$3.4\times$ the
        range of \eqref{eq:patrange} at $10$ corners per frame instead
        of $162$. Generator \code{mv\_pattern2\_render}
        (\code{mv/pattern.h}), reader \code{mv\_read\_coarse}
        (\code{mv/reader.h}), consumed by \code{tools/calibreal.c}
        and \code{tools/rigcalib.c} as an automatic fallback;
        validated by \code{test\_pattern\_coarse} at the exact
        field-failure geometry. \emph{Pitch}: round four measured the
        need directly --- three fixed cameras watching a room could
        find the saddle lattice but not one identity dot.
        \emph{Unlocks}: rig calibration at room scale with commodity
        webcams --- the actual deployment scenario of
        \autoref{sec:overview}. The temporal multiplexing refinement is
        built: display mode M6 alternates the tiers in 64-refresh
        blocks (\code{mv\_pattern\_mux\_tier},
        \code{mv\_pattern\_mux\_render}; \code{tests/test\_mux.c}
        round-trips both tiers from one stream), so near and far
        cameras share a single capture on one counter timeline (fine
        reads give $k$ in full, coarse reads $k \bmod 256$,
        reconciled wrap-aware as \code{rigcalib} already does).
  \item \textbf{[done at sim tier] Projected structured light} ---
        \autoref{sec:slight}: display-app mode M5, cross-camera code
        matching (\code{mv\_graycode\_match}), robust 8-point +
        essential pose (\code{mv\_essential\_pose}), the
        \code{slreal} tool, and the \code{demo\_slight} integration
        sim that fabricates a full two-camera capture and recovers the
        ground-truth pose to the fourth digit. \emph{Pitch}: rooms are
        mostly blank wall, and blank wall is where our dense stereo
        honestly reports nothing; a \$60 projector paints structure
        onto exactly those surfaces, and doubles as an extrinsics
        bridge for camera pairs that cannot both see the display.
        \emph{Unlocks}: dense room geometry on textureless surfaces;
        awaiting only the projector hardware --- the runbook in
        \autoref{sec:slight} is written for solo operation.
  \item \textbf{[done] Levenberg--Marquardt refinement} ---
        implemented as \code{mv\_calib\_refine} (\code{mv/refine.h}):
        joint LM over shared intrinsics/distortion and per-view
        Rodrigues poses with a Schur complement over the view blocks;
        the pre-registered T5 acceptance passed (ensemble ratio
        $1.1087\to0.9990$, \autoref{sec:statrules}); exact at
        $\sigma=0$; never worse than the alternation initializer on
        any ensemble member. Bundle adjustment over registered views is
        also built (\code{mv/bundle.h}: multi-camera $+$ points joint
        LM, Schur complement over per-point blocks, gauge anchored by
        flagged fixed cameras): residual ratio $1.012$ against the
        predicted floor, and it tightens photo registration from
        $77$\,mm to $6$\,mm while recovering the true principal point
        that resection had to pin. A finding worth keeping: with the
        v1 single-scale features, the rig's anchor bank came out
        $100\%$ coplanar (all on the fronto-parallel wall; grazing
        planes contributed zero anchors), and free-K bundle adjustment
        over a coplanar set walks the focal--depth degeneracy to a
        spurious lower-residual solution --- the same degeneracy that
        justified pinning the principal point in
        \autoref{sec:photos}, now measured a second way; depth relief
        in the anchor set is a precondition, checked in the test.
        (Earlier framing: bundle
        adjustment \cite{Triggs2000}). Joint nonlinear minimization of
        reprojection error over intrinsics, poses, and distortion.
        \emph{Pitch}: the single most attractive item on the list ---
        the fired T5 flag of \autoref{sec:statrules} is a
        \emph{pre-registered acceptance test} (the residual ratio must
        fall from $1.17$ to $1.00\pm0.03$, else the implementation is
        wrong); it tightens every calibration number in
        \autoref{sec:results}, and the same LM core serves the orbit
        fit, the clock fit, and eventually bundle adjustment.
        \emph{Unlocks}: no new scenario --- it upgrades all of them;
        tightened $\K$ and clocks propagate into every tracking and
        model figure, and the drift monitor gains sensitivity.
  \item \textbf{[done] Feature detection and robust matching} ---
        \code{mv/feat.h}: Shi--Tomasi corners with sub-pixel
        refinement, orientation-normalized $8\times8$ patch
        descriptors, Lowe-ratio $+$ mutual matching, verified through
        the robust 8-point: 219 verified matches at $0.117$\,px median
        epipolar residual against the analytic $\F$ in sim, 176 under
        a $20^\circ$ roll; single-scale by design (v1).
        General interest
        points with descriptors, matched under the RANSAC gate of
        \autoref{sec:epipolar}. \emph{Pitch}: unlocks everything
        ``live'' --- the $\F$-drift and $\Delta t$ monitors get real
        correspondences to chew on, and movers become trackable from
        images rather than simulated detections. \emph{Unlocks}: live
        insect counting and robot tracking
        (\autoref{sec:insects}, \autoref{tab:trackresults}) on real
        footage, and the run-time $\Delta t$ estimator of
        \autoref{sec:softsync}.
  \item \textbf{[done at sim tier] Rectification warping and the
        dense path} --- \code{mv\_warp\_homography} and the
        multi-plane scene renderer (\code{mv/render.h}); run notes atop
        \code{demo/roomsim.c} (\code{make demo\_room}). Validated in
        \autoref{tab:roomresults}/\autoref{fig:room}: the pipeline of
        \autoref{sec:overview} runs on pixels end to end --- and now
        on \emph{real} frames too: \code{tools/scenecloud.c} ran the
        same path on handheld phone video to produce the 82k-point
        cloud of \autoref{fig:scenecloud}. Remaining at real tier:
        multi-camera (fixed-rig) depth rather than accidental
        baselines. \emph{Unlocks}: dense change detection and
        moving-object silhouettes; with items 6 and 9, the room model
        --- demonstrated on rendered imagery and, as a point cloud, on
        real imagery.
  \item \textbf{[done at sim tier] Temporal statistics and the
        background model} --- median-depth background, TSDF fusion, and
        change detection implemented in \code{demo/roomsim.c}
        (\autoref{tab:roomresults}: $97.9\%$ recall, $1.39\%$ false
        alarms on a moved box). Remaining: promotion from demo to
        library module, incremental background updates, and real
        frames. \emph{Unlocks}: the furniture-level room mesh and
        scene-rearrangement events --- demonstrated --- and the
        subtraction front end for the \autoref{sec:scenarios}
        trackers.
  \item \textbf{[done at sim tier] Photo-collection registration}
        --- \autoref{sec:photos}: \code{mv/photo.h} resects casual
        unknown-camera photos into the rig frame from a triangulated
        anchor bank (RANSAC $+$ trim $+$ full-LM polish, principal
        point pinned per casual-photo convention); sim: focal within
        $2.6\%$, orientation $0.25^\circ$, position $0.11$\,m.
        Formerly:
        (\autoref{sec:photos}). Resection of auxiliary photos against
        triangulated structure; $N$-view DLT already accepts the extra
        views. \emph{Pitch}: coverage where the rig cannot see
        (the topology argument of \autoref{sec:occlusion}) --- and its
        dynamic limit is the tracked mobile camera with measured poses,
        the self-tracking extension of \autoref{sec:capabilities}.
        \emph{Unlocks}: high-fidelity topology capture (the coverage
        argument of \autoref{sec:occlusion}) and the drone /
        self-tracking scenario itself.
  \item \textbf{[done] Optimal and weighted estimators} ---
        \code{mv/optimal.h}: Hartley--Sturm optimal triangulation
        (polynomial minimization, exact epipolar consistency to
        $10^{-13}$\,px; the honest sim finding is that for our
        well-conditioned rig it matches plain DLT to $0.01\%$ ---
        consistent with Hartley--Sturm's own assessment) and the
        Mahalanobis-weighted known-radius circle fit that retires the
        documented $\sigma=0.6$\,px nonlinearity ($1.4\times$ lower
        center error; an intermediate IRLS design was measured
        \emph{worse} than unweighted and rejected --- exact ML on the
        anisotropic distance was required). Hartley--Sturm
        triangulation \cite{HartleySturm1997}, Mahalanobis-weighted
        model fits, weighted DLT (\autoref{sec:heterogeneous}).
        \emph{Pitch}: retires the \emph{documented} estimator floors ---
        the circle-fit anisotropy of \autoref{tab:diagtrack} and the
        equal-weight DLT --- each with its regression already measured,
        so improvement is provable, not aspirational. \emph{Unlocks}:
        mover tracking in the regimes the current fits leave
        (\autoref{tab:diagtrack}) --- fast insects at range, robots in
        dim light --- and honest fusion of unequal cameras
        (\autoref{sec:heterogeneous}).
  \item \textbf{[done at sim tier] TSDF completion} --- implemented
        in \code{src/tsdf.c}; setup and run instructions are in the
        comment atop \code{mv/tsdf.h} (\code{make demo\_tsdf}; mesh
        lands in \code{out\_tsdf.ply}); validated in
        \autoref{tab:tsdfresults}. Remaining: the dense-depth producer
        (item 5), the voxel-major fast path, and then RANSAC
        plane/primitive extraction on the background model.
        \emph{Pitch}: every remaining piece rides on item 5 or 6;
        primitives (\autoref{sec:ladder}) then give the furniture-level
        scene description almost for free. \emph{Unlocks}: continuous
        room-model maintenance and geometric change detection as a
        standing application beside the \autoref{sec:scenarios}
        trackers.
  \item \textbf{The identity sidecar} (\autoref{sec:faces}). Person and
        face detection behind the ONNX Runtime seam, replayed oracles as
        fixtures. \emph{Pitch}: the contract is fully specified down to
        the license table and quality metrics
        (\autoref{tab:plugins}--\autoref{tab:pluginquality}); it
        upgrades occupancy from height-cohort identification to named
        persons at the door chokepoint, with its acceptance test
        (\autoref{sec:openset}) already defined. \emph{Unlocks}:
        named-person occupancy timelines and stranger detection.
\end{enumerate}

If the hardware materializes, the optional range-finder integration of
\autoref{sec:range} slots in without redesign. A suggested near-term
order: item 3 first (its acceptance test is pre-registered and it
uplifts everything already built), then 2 (completes the calibration
story), then 5 --- after which 6 and 9 are largely composition and the
system produces its first real-image room model. Each step lands as a
new module behind the same conventions (\autoref{tab:modules}) with its
own noiseless-exact tests and a synthetic experiment extending
\autoref{sec:results}.

\autoref{tab:unlocks} inverts the list: for each application scenario in
the document, which roadmap items unlock it, what can be developed and
validated in simulation alone, and what genuinely requires hardware.

\begin{table}[ht]
\centering
\scriptsize
\setlength{\tabcolsep}{3.5pt}
\begin{tabular}{l c l l}
\toprule
Scenario & Items & Sim-only development & Requires hardware \\
\midrule
robot tracking (\autoref{tab:trackresults}) & 2, 4 & \emph{done} (tracker $+$ fits) & rig $+$ live detections \\
insect counting (\autoref{sec:insects}) & 2, 4, 8 & \emph{done} (tracker, gating, stats) & rig; detection is at image tier \\
occupancy, height-ID (\autoref{sec:occupancy}) & 2, 4, 5 & \emph{done} (tracking, ID, paths) & rig $+$ live detections \\
lighting / diurnal (\autoref{sec:lightscen}) & 2 & \emph{done} (analyzer, 120-day synthetic) & calibrated camera $+$ M0; real lamps \\
calibration session (\autoref{sec:calibspec}) & 2, 3 & reader \emph{done}; orbit/clock fits simmable & display, plate, cameras \\
room model $+$ change detection (\autoref{sec:background}) & 5, 6, 9 & \emph{done} (\autoref{tab:roomresults}) & rig for real-image depth \\
topology capture (\autoref{sec:occlusion}) & 7 & fully simmable (rendered scenes) & photos of real objects \\
named-person identity (\autoref{sec:faces}) & 2, 10 & fusion logic, synthetic embeddings & identity camera $+$ sidecar; real faces \\
drone / self-tracking (\autoref{sec:capabilities}) & 4, 7, 8 & fully simmable (mover carrying a sensor) & drone $+$ rig \\
\bottomrule
\end{tabular}
\caption{Scenario unlock map. ``Items'' are the roadmap entries above;
\emph{done} marks sim-tier validation already reported in
\autoref{sec:results}--\autoref{sec:scenarios}. Every scenario's
\emph{estimation} layer is sim-developable; hardware enters for real
detections, real radiometry, and real faces.}
\label{tab:unlocks}
\end{table}

\appendix
%----------------------------------------------------------------------
\section{Camera hardware and the quality of data}
\label{sec:hardware}

The capture hardware determines which error terms enter the pipeline before
any algorithm runs. The organizing observation: cheap capture hardware
mostly adds \emph{biases} --- rolling shear, synchronization parallax,
compression structure, unmodelable distortion, focus creep --- rather than
noise. Noise we model and budget (\autoref{ex:deptherr}); biases survive
averaging, masquerade as geometry, and poison calibration silently. The
specifications below are therefore ranked by which biases they admit.

\subsection{Shutter: global vs rolling}
\label{sec:shutter}

A global-shutter sensor exposes all pixels over one common interval --- each
frame is a true snapshot, described by a single $\PP$. A rolling-shutter
sensor exposes and reads row by row: the bottom row of a frame is captured
10--30\,ms after the top row. For a static scene the difference vanishes;
for anything moving, each row samples a different instant and no single
projection matrix describes the frame --- the camera model of
\autoref{sec:camera} itself breaks.

\begin{example}[rolling-shutter shear on the rotating target]
\label{ex:rolling}
The rotating display of \autoref{sec:rotating} moves at
$\approx27$\,px/s in the image. A rolling readout of $20$\,ms samples the
top and bottom of the target $20$\,ms apart: a shear of
$27\cdot0.02\approx0.5$\,px across the corner grid --- larger than the
$0.3$\,px noise budget, and \emph{systematic}: it does not average away
over frames but feeds a consistently wrong homography into calibration.
\end{example}

\subsection{Synchronization: hardware trigger vs free-running}
\label{sec:trigger}

A trigger line starts exposure on all cameras from one electrical pulse
(coincidence to microseconds). Free-running cameras can only be matched by
software timestamps --- a few \emph{milliseconds} of jitter and drift.
Triangulation assumes both cameras saw the point at the same instant; a
time offset $\Delta t$ on a point moving at speed $v$ creates a phantom
disparity $f\,v\,\Delta t/Z$ that is mathematically indistinguishable from
depth.

\begin{example}[the price of software synchronization]
\label{ex:sync}
An object crossing the experimental rig at $1$\,m/s with $5$\,ms software
sync: phantom disparity $=800\cdot(1\cdot0.005)/4.7\approx0.85$\,px, which
the depth law \eqref{eq:deptherr} converts to
$\approx47$\,mm of depth error --- exceeding the rig's entire
$23$\,mm error budget at that range. For dynamic scenes, synchronization
error dominates every other term; a trigger wire outperforms any camera
upgrade. The trigger also permits phase-locking exposure against mains
flicker and locking capture to the display's frame counter
(\autoref{sec:rotating}).
\end{example}

\subsection{Software synchronization: measuring and correcting the clocks}
\label{sec:softsync}

This subsection is the practice counterpart of the temporal camera model
\eqref{eq:clock} of \autoref{sec:timemodel}: how the clock parameters
$(\phi_i, T_i)$ are actually measured and their effect corrected. When no
trigger line exists (the consumer tier), frames from independent,
possibly heterogeneous cameras are never simultaneous: free-running
30\,fps streams differ by up to half a frame period ($|\Delta t|$
averaging $\sim$8\,ms), and consumer crystals drift 50--100\,ppm, so the
offset slides several milliseconds per minute --- it must be
\emph{tracked}, not calibrated once. Since \autoref{ex:sync} showed the
cost lands entirely on movers (static structure is indifferent to
$\Delta t$), the right treatment is not to chase alignment but to make
time part of the measurement model. Three layers:

\paragraph{Bound.} Pair frames nearest-neighbor in time rather than by
index: free, and caps $|\Delta t|$ at half a period under any drift.

\paragraph{Measure.} Model each camera's clock as offset $+$ skew
against a common reference and estimate both continuously, two
independent ways. (i) \emph{The dynamic display pattern}
(\autoref{sec:rotating}): the Gray-coded counter gives frame-level
display timestamps (record \code{CTR} of \autoref{tab:records}); the
phase patches refine them below the refresh period (\code{PHS}); a
linear fit of (camera frame index, decoded display time) pairs per
camera yields its offset and rate --- see the precision budget in
\autoref{ex:clockfit}. (ii) \emph{Movers themselves}: a tracked point moving
across the epipolar field sits off its epipolar line by (image velocity
$\perp$ line)$\,\times\Delta t$; with $\F$ known exactly, $\Delta t$ is a
one-parameter fit to residuals already computed --- the $\F$-drift
monitor of \autoref{sec:epipolar} with one extra parameter, usable at
run time when no display is present. (A $\mu$C-driven blinking LED in a
frame corner is the minimal permanent clock beacon if continuous
display-free measurement is wanted.) A deliberate small rate offset on
one camera ($\sim$0.1\,Hz) makes the phases \emph{beat}, sweeping
$\Delta t$ through zero every $\sim$10\,s: the ``golden pairs'' at each
null (aligned to $\sim$50\,$\mu$s) are free validation samples for the
estimator.

\paragraph{Correct.} Give every measurement its own timestamp and
interpolate \emph{tracked detections} (not images) onto the common
clock before triangulating; the residual is second-order
(\autoref{ex:interp}). Dense stereo needs no correction at all --- the
dense content is the static background; movers travel the tracked
path, which is exactly the path that interpolates. The principled limit
for fast movers is trajectory triangulation: fit a short motion model to
$K$ timestamped rays rather than intersecting two rays at a fictitious
common instant --- and the same per-measurement-timestamp machinery is
what rolling-shutter correction (\autoref{sec:shutter}) consumes, so
building it once serves both. Self-verification in the
\autoref{sec:statrules} style: the slope of epipolar residual vs image
speed must equal the fitted $\Delta t$, and golden pairs must show none.

\begin{example}[clock-fit precision from the display]
\label{ex:clockfit}
Display timestamps are quantized to the refresh period
($16.7$\,ms at 60\,Hz), i.e.\ per-sample noise
$\sigma_q=16.7/\sqrt{12}\approx4.8$\,ms. One minute at 30\,fps gives
$N=1800$ samples: offset precision $\sigma_q/\sqrt N\approx0.11$\,ms,
and rate precision
$\sigma_q\sqrt{12/N}/T\approx7$\,ppm over the $T=60$\,s span ---
comfortably resolving 50--100\,ppm crystals in one minute, before the
phase patches sharpen $\sigma_q$ further.
\end{example}

\begin{example}[interpolation makes sync second-order]
\label{ex:interp}
A walker at $1$\,m/s with a full half-frame mismatch
($\Delta t=16.7$\,ms) uncorrected: phantom depth error
$\approx150$\,mm by \autoref{ex:sync}. Linearly interpolating the
tracked detection between frames leaves only the curvature term
$a\,\Delta t^2/8$: at $a=2$\,m/s$^2$,
$2\cdot0.0167^2/8\approx70\,\mu$m --- a factor of $\sim$2000. If
$\Delta t$ itself is known to $\pm1$\,ms (\autoref{ex:clockfit}), the
residual first-order term is $\sim$1\,mm at $1$\,m/s: software
synchronization turns the dominant dynamic-scene error into a minor one.
\end{example}

The verdict, stated as a rule: on the consumer tier, synchronization
mismatch is quantified and corrected, never assumed away --- one more
instance of this appendix's theme that a \emph{modeled} defect becomes a
term of the estimator rather than a bias. The trigger tier
(\autoref{sec:trigger}) remains the clean answer where the hardware
allows it.

\subsection{Radiometric and noise measurement procedures}
\label{sec:radiometry}

The cameras are the rig's own best measuring instruments; these four
procedures, run once per rig (modes M0/flat scenes,
\autoref{tab:modes}), produce the numbers everything else consumes.

\begin{enumerate}
  \item \emph{Photon-transfer curve} $\to$ the noise scale $\sigma$.
        Capture repeated frames of a static scene at several exposure
        levels; per-pixel temporal variance vs mean is linear with slope
        $1/\text{gain}$ (e$^-$/DN) and intercept the read-noise floor.
        The resulting intensity noise, propagated through the matcher,
        is the rig's \emph{measured} $\sigma$ --- the number that
        re-scales every acceptance band of \autoref{sec:statrules}
        (which this document evaluates at the synthetic $0.3$\,px).
  \item \emph{Flicker audit.} High-rate (or exposure-phase-swept)
        capture of a blank wall; the FFT of mean intensity at
        $2f_{\text{mains}}$ (100/120\,Hz) gives the modulation depth.
        Rule: keep it below $\sim$1--2\%, or set
        $t_e = n/(2f_{\text{mains}})$, which nulls it exactly.
  \item \emph{Inter-camera radiometric transfer.} A gray card (or the
        display's M0 ladder) viewed by both cameras; the joint
        histogram gives the gain/offset map between them. Flat and
        linear clears plain SAD matching; visible curvature is the
        signal to switch the matcher to a rank/census cost.
  \item \emph{Flat-field / vignetting.} The display showing uniform
        gray (M0), imaged defocused; the per-pixel gain map is stored
        and divides all subsequent frames; it also supplies the local
        white references the reader's decoding rules assume
        (\autoref{sec:calibspec}).
\end{enumerate}

A cheap lux meter anchors the ladder in absolute units so results
transfer between sites; the display's off-axis brightness falloff ---
measured for free as the plate sweeps (\autoref{sec:rotating}) ---
completes the radiometric picture of the fixture itself.

\subsection{Interface: UVC vs machine-vision stacks}
\label{sec:uvc}

UVC (USB Video Class) is the ``any webcam just works'' protocol
(\code{v4l2} on Linux, no vendor SDK). Its convenience reflects its
purpose --- streaming video to humans: at higher resolutions frames
usually arrive \emph{MJPEG-compressed}, and JPEG's $8\times8$ block
artifacts are structured noise at exactly the spatial frequencies where
sub-pixel corners and block matching live --- a \emph{correlated}
contribution to $\sigma_d$ that does not average like photon noise. UVC
also offers only coarse loosely-specified exposure/gain controls,
imprecise timestamps, and no trigger support in the class. Machine-vision
interfaces (USB3 Vision/GenICam via vendor SDKs) deliver raw pixels,
physical-unit controls, per-frame hardware timestamps and trigger I/O.
Rule of thumb: UVC is acceptable exactly where rolling shutter is
acceptable --- static bring-up.

\subsection{Self-description: what a camera will tell you about itself}
\label{sec:selfdesc}

A natural hope is that the camera can be \emph{asked} for its physical
characteristics --- pixel size, sensor dimensions, focal length. It
mostly cannot. What a UVC device advertises in its descriptors is
operational, not physical: the list of frame sizes and rates it will
stream, and the ranges of its exposure/gain/white-balance controls.
There is no field for pixel pitch, sensor format, lens focal length, or
even the sensor part number. Phone cameras do embed focal length and a
35\,mm-equivalent in the EXIF of still photos, but a UVC video stream
carries no EXIF at all; machine-vision cameras (\autoref{sec:uvc}) are
the honorable exception, exposing sensor model and physical-unit
controls through GenICam, with a real sensor datasheet behind them.
For webcam-class hardware the physical numbers must come from the
\emph{datasheet culture} instead --- and consumer datasheets are
marketing documents, reliable only after cross-examination.

\begin{example}[how big is a pixel on a Logitech C270?]
\label{ex:c270}
Logitech's specification page for the C270 gives: optical resolution
$1280\times960$ (1.2\,MP), field of view $60^\circ$, focal length
$4.0$\,mm, focus fixed, and --- for sensor type --- the single word
``plastic.'' Retail listings advertise ``3\,MP,'' which is software
interpolation of stills. Pixel size appears nowhere. The number
exists, but comes from the community: the OpenFlexure microscope
project, which re-lenses this exact webcam, reports $2.8\,\mu$m pixels
and a $3.58\times2.02$\,mm active area in 720p
mode.\footnote{Logitech, ``HD Webcam C270 Technical Specifications,''
\url{https://support.logi.com/hc/en-us/articles/360023462093}; OpenFlexure
Microscope project wiki, ``Camera Options,''
\url{https://github.com/rwb27/openflexure_microscope/wiki/Camera-Options}.
Both retrieved 2026-08-11.} The numbers cross-check into a consistent
story: $1280\times2.8\,\mu\text{m}=3.58$\,mm and
$960\times2.8\,\mu\text{m}=2.69$\,mm, so the native chip is
$3.58\times2.69$\,mm --- the standard $1/4$-inch optical format --- and
720p is a row crop of it ($720\times2.8=2.02$\,mm~\checkmark). The
advertised $60^\circ$ is then the \emph{diagonal} of the native $4{:}3$
frame ($2\arctan(2.24/4.0)=58.5^\circ\approx60^\circ$); the horizontal
field is only $2\arctan(1.79/4.0)=48^\circ$ --- planning rig coverage
(\autoref{sec:scaling}) with the headline $60^\circ$ would overestimate
width by a quarter.
\end{example}

What does the pixel size buy, given that calibration
(\autoref{sec:calib}) never asks for it? Calibration measures
$f_x = f_{\text{mm}}/\text{pitch}$ --- for the C270,
$4.0\,\text{mm}/2.8\,\mu\text{m} \approx 1430$\,px --- and every
geometric quantity in this document (rays, triangulation, the
ground-sampling floor $Z/f_x$ of \autoref{sec:resolution}) consumes
only that ratio. The physical factorization is still worth having,
four ways. \emph{Prediction}: before the hardware arrives, pitch and
focal length forecast the field of view and the sampling density ---
at our $4.7$\,m working depth the C270 samples the scene at
$Z\cdot\text{pitch}/f_{\text{mm}}\approx3.3$\,mm per pixel.
\emph{Cross-examination}: measured $f_x$ times claimed pitch must
reproduce the claimed focal length; a mode that secretly crops, bins,
or interpolates (the ``3\,MP'' stills) breaks the identity and is
caught --- as is a non-square pixel, since calibration separately
reports $f_x$ and $f_y$. \emph{Radiometry}: photon capture scales with
pixel \emph{area}, so the C270's $(2.8\,\mu\text{m})^2=7.8\,\mu\text{m}^2$
cell collects $\sim$$2/3$ of the photons of a
$3.45\,\mu$m machine-vision pixel at equal exposure --- a shot-noise
penalty the photon-transfer procedure of \autoref{sec:radiometry}
measures directly. \emph{Diffraction sanity}: at f/2.8 the Airy-disk
diameter $2.44\lambda N\approx3.8\,\mu$m at $\lambda=0.55\,\mu$m
already exceeds the pitch, so the plastic lens and diffraction, not
the pixel grid, set the resolution floor (cf.\ the MTF discussion of
\autoref{sec:lenses}). The general rule: the camera's trustworthy
self-description is its \emph{data} --- calibration and the
measurement procedures of \autoref{sec:radiometry} extract everything
the pipeline consumes, in the units it consumes; the datasheet's role
shrinks to choosing hardware and to factorizing measured ratios into
physically meaningful parts.

\subsection{Optics: M12 vs C/CS mounts}
\label{sec:lenses}

M12 (``S-mount'') lenses are the small threaded board lenses of
webcam-class hardware: cheap, fixed aperture, focused by rotating the
barrel, no standardized flange distance. C-mount and CS-mount are the
machine-vision standards: metal barrels, multi-element glass, iris and
focus rings with lock screws, standardized flange distances (C:
$17.53$\,mm; CS: $12.53$\,mm; a C lens fits a CS body via a 5\,mm spacer,
not conversely). Three separate data-quality channels:

\begin{enumerate}
  \item \emph{Sharpness (MTF) is the true floor under $\sigma_d$}:
        sub-pixel localization precision scales with the optical blur
        radius; a soft lens can double $\sigma_d$, and via
        \eqref{eq:deptherr} double $\sigma_Z$ --- equivalent to discarding
        half the sensor resolution (cf.\ the weakest-link law of
        \autoref{sec:heterogeneous}: the lens is a full member of that
        chain).
  \item \emph{Distortion modelability}: the Brown model
        \eqref{eq:distortion} absorbs the smooth distortion of decent
        lenses; cheap wide M12s exhibit higher-order or non-radial terms
        that leave \emph{systematic} calibration residuals.
  \item \emph{Stability of $\K$}: thread-focused plastic barrels creep
        with vibration and temperature --- silent recalibration events,
        precisely what the $\F$-drift monitor of \autoref{sec:epipolar}
        exists to catch. Locked C/CS rings hold $\K$ for months.
        Autofocus is this failure mode as a feature: \emph{a calibrated
        camera must never refocus}.
\end{enumerate}

An iris adds one more useful knob: most lenses peak in sharpness around
f/4--f/8; beyond that diffraction itself blurs (spot
$\approx1.22\,\lambda N$: at f/8 about $5.4\,\mu$m, already exceeding a
$3\,\mu$m pixel).

\subsection{Monochrome vs color}
\label{sec:mono}

A Bayer color sensor samples each color at $\tfrac14$--$\tfrac12$ of the
pixel grid and interpolates the rest; demosaicing smears exactly the
high-contrast edges that corners and matching depend on, and costs about
one stop of sensitivity. This pipeline processes grayscale throughout, so
monochrome sensors deliver strictly better data; color remains at most a
redundancy layer in the calibration pattern (\autoref{sec:rotating}).

\subsection{Summary and candidate hardware}
\label{sec:cameratiers}

\begin{table}[ht]
\centering
\begin{tabular}{lll}
\toprule
Characteristic & Data-quality consequence & When it matters \\
\midrule
rolling shutter & $\sim$0.5\,px systematic shear (\autoref{ex:rolling}) & any motion \\
no trigger      & phantom disparity, $\sim$47\,mm (\autoref{ex:sync})   & dynamic scenes \\
UVC / MJPEG     & structured artifacts in $\sigma_d$; no trigger        & beyond bring-up \\
M12 optics      & MTF floor, distortion residuals, $\K$ creep           & always, silently \\
C/CS optics     & sharp, modelable, mechanically stable $\K$            & the metric rig \\
color (Bayer)   & demosaic edge artifacts, $-1$ stop sensitivity        & mono preferred \\
\bottomrule
\end{tabular}
\caption{Capture-hardware characteristics and their consequences.}
\label{tab:hardware}
\end{table}

Three procurement tiers follow (indicative 2026 prices):
\begin{enumerate}
  \item \emph{Bring-up} ($\sim$\$150/pair): two Logitech C920/C930-class
        UVC webcams. Rolling shutter, no trigger, but focus and exposure
        lockable via \code{v4l2-ctl}; teaches the pipeline every
        real-world lesson cheaply. Algorithms yes, metric claims no.
  \item \emph{Budget metric rig} ($\sim$\$150--250/pair): OV9281-class
        modules (e.g.\ Arducam) --- $1280\times800$ \emph{monochrome
        global shutter}, external-trigger variants, interchangeable M12
        optics (choose the best lens, lock it with thread-lock). With a
        2.8--4\,mm lens, $f\approx950$--$1300$\,px --- the regime of all
        worked examples here. Same idea on CSI: the Raspberry~Pi Global
        Shutter Camera (IMX296, trigger pin, $\sim$\$50).
  \item \emph{Solid metric rig} ($\sim$\$500--900/pair): entry
        machine-vision USB3 (FLIR Blackfly~S, Basler ace/dart, or
        Daheng/HIK equivalents): 1.6--2.3\,MP mono global shutter, real
        trigger I/O, Linux SDKs, C/CS lenses. The tier where the
        resolution mathematics of \autoref{sec:resolution} becomes
        purchasable.
\end{enumerate}
A packaged shortcut: the Luxonis OAK-D class devices ($\sim$\$250)
contain two factory-synchronized global-shutter mono cameras in one USB3
housing --- the whole hardware problem in one purchase, at the cost of a
fixed $\sim$7.5\,cm baseline, short for a 4--6\,m working volume
($\sigma_Z\propto1/B$, \eqref{eq:deptherr}).

\autoref{tab:models} lists concrete representatives, sorted by price;
prices are indicative mid-2026 street prices per camera and drift, but
the \emph{characteristics} are fixed properties of each product.

\begin{table}[ht]
\centering
\footnotesize
\newcommand{\cm}{$\checkmark$}
\begin{tabular}{l r l c c c c l}
\toprule
Model & $\approx$\$ & Sensor / resolution & Mono & Global & HW & Raw & Lens \\
      &             &                     &      & shutter & trigger & frames & \\
\midrule
Raspberry Pi GS Camera$^{a}$ & 50 & IMX296, $1456\times1088$ & --$^{a}$ & \cm & \cm & \cm & CS \\
Logitech C920s$^{b}$ & 70 & $1920\times1080$ & -- & -- & -- & --$^{b}$ & fixed \\
Arducam OV9281 USB & 90 & OV9281, $1280\times800$ & \cm & \cm & \cm & \cm & M12 \\
HIKrobot MV-CS016-10UM & 230 & IMX273, $1440\times1080$ & \cm & \cm & \cm & \cm & C \\
Luxonis OAK-D$^{c}$ & 250 & $2\times$OV9282, $1280\times800$ & \cm & \cm & --$^{c}$ & \cm & fixed \\
Basler dart daA1280-54um & 280 & $1280\times960$ & \cm & \cm & \cm & \cm & S/CS \\
Intel RealSense D435$^{d}$ & 330 & $2\times$OV9282 IR & \cm & \cm & --$^{d}$ & \cm & fixed \\
FLIR Blackfly S 16S2M-CS & 465 & IMX273, $1440\times1080$ & \cm & \cm & \cm & \cm & CS \\
\bottomrule
\end{tabular}
\caption{Representative commercial cameras, sorted by indicative 2026
price per unit. $^{a}$CSI interface (needs a Raspberry~Pi, not USB);
color filter --- monochrome variants from third parties.
$^{b}$UVC; uncompressed only at reduced rates, MJPEG otherwise; exposure
and focus lockable via \code{v4l2-ctl}. $^{c}$No external trigger input,
but the internal stereo pair is factory-synchronized --- the trigger
problem is solved \emph{inside} the box; baseline fixed at 7.5\,cm.
$^{d}$Depth device; the raw synchronized IR pair is accessible and
usable as a stereo source; projector should be disabled for our
matcher.}
\label{tab:models}
\end{table}

\subsection{Blending heterogeneous cameras}
\label{sec:blend}

Knowing each camera's characteristics is itself an asset: a
\emph{modeled} defect is a correctable or at least gateable one. A
rolling-shutter camera whose row-readout time is known can have its rows
time-stamped individually and its observations gated out (or motion-
compensated) whenever the scene moves; a camera with a measured noise
curve enters the triangulation with an honest weight
(\autoref{sec:heterogeneous}); a lens with well-fit distortion
contributes unbiased rays while one with known residuals can be trusted
only in its image center. The measurement model becomes per-camera, and
every characteristic in \autoref{tab:hardware} turns from a hidden bias
into a term of that model.

A deliberate \emph{blend} of camera types then does more than average ---
it makes artifacts detectable, because most artifacts are
\emph{camera-specific} while the scene is common. MJPEG block edges,
rolling shear, demosaic fringes, and specular highlights each live in
some cameras and not others (highlights move with viewpoint; compression
artifacts sit on one camera's grid); true structure must be consistent
across all views. With identical cameras, a shared systematic error is
invisible --- every unit votes for the same lie; with heterogeneous
hardware the correlation is broken and disagreement \emph{localizes} the
artifact, in the same spirit as the $\F$-drift and laser-audit health
monitors (\autoref{sec:epipolar}, \autoref{sec:range}). Complementary
pairings fall out naturally: a synchronized global-shutter mono pair as
the metric backbone; a high-resolution color camera (rolling shutter
tolerated, gated by motion) contributing fine texture and appearance to
the model; a wide-FOV unit for coverage and moving-object flagging, its
geometry weighted down per \autoref{sec:heterogeneous}. Geometry from
the cameras built for geometry, appearance from the cameras built for
appearance, and cross-checks from their disagreement --- the
$N$-camera analysis of \autoref{sec:scaling} applies unchanged, since
every camera carries its own $\K$, distortion, and noise model.

%----------------------------------------------------------------------
\section{Root-mean-square (RMS) error: definition and use}
\label{sec:rms}

Nearly every quantitative claim in this document is an RMS figure. This
appendix defines it, explains why it is the right summary here, and maps
every way the document uses it.

\subsection{Definition}
For scalar errors $e_1,\dots,e_N$,
\begin{equation}
\mathrm{RMS} \;=\; \sqrt{\tfrac1N\textstyle\sum_{i=1}^N e_i^2},
\label{eq:rmsdef}
\end{equation}
the square root of the mean of the squares. For vector errors (a 2D
reprojection offset, a 3D position error) the same formula is applied to
the \emph{norms}: $\mathrm{RMS}=\sqrt{\frac1N\sum\|\vect e_i\|^2}$,
which is also $\sqrt{\sum_k \mathrm{RMS}_k^2}$ over the components ---
components add \emph{in quadrature}.

\begin{example}[RMS vs mean vs max]
\label{ex:rmsbasic}
Errors $\{1,2,2,5\}$: mean $=2.5$; RMS
$=\sqrt{(1+4+4+25)/4}$ $=\sqrt{8.5}\approx2.92$; max $=5$. RMS always lies
between mean and max, weighting large errors more than the mean does
(squaring makes the $5$ contribute $25/34$ of the total). This document
reports RMS as the headline and max separately when tails matter
(e.g.\ \autoref{tab:trackresults}).
\end{example}

\subsection{Why squares}
Three reasons, in increasing depth. (i) \emph{Calculus}: squared error is
smooth, so least-squares problems have closed forms --- every estimator in
this system (\autoref{ex:nullvec} onward) minimizes a sum of squares, and
RMS is exactly the quantity those estimators optimize, expressed per
sample. (ii) \emph{Probability}: for zero-mean Gaussian noise the RMS of
the errors converges to the distribution's $\sigma$; reporting RMS is
reporting the fitted noise scale, directly comparable to the injected
$\sigma$ in every experiment. Least squares \emph{is} maximum likelihood
under Gaussian noise, closing the loop. (iii) \emph{Composition}:
independent error sources add in quadrature,
$\sigma_{\text{tot}}^2=\sum_j\sigma_j^2$ --- the working rule behind the
two-coordinate pixel distance, the two-camera angular noise
\eqref{eq:hetero}, and the disparity noise $\sigma_d=\sqrt2\,\sigma$ used
throughout.

\begin{example}[quadrature]
\label{ex:rmsquad}
Per-coordinate pixel noise $\sigma=0.3$\,px: the Euclidean reprojection
\emph{distance} has RMS $\sqrt{0.3^2+0.3^2}=0.424$\,px --- the baseline
against which the residuals of \autoref{sec:statrules} are judged.
Disparity, a difference of two independent $0.3$\,px coordinates,
likewise has $\sigma_d=\sqrt2\cdot0.3\approx0.42$\,px
(\autoref{ex:deptherr}).
\end{example}

\subsection{Bias, variance, and what RMS cannot tell you}
RMS mixes systematic and random error:
\begin{equation}
\mathrm{RMS}^2 \;=\; \underbrace{\bar e^{\,2}}_{\text{bias}^2}
\;+\; \underbrace{\tfrac1N\sum (e_i-\bar e)^2}_{\text{variance}} .
\label{eq:biasvar}
\end{equation}

\begin{example}[decomposition]
\label{ex:rmsbias}
A constant bias of $3$ plus zero-mean noise of standard deviation $4$
gives $\mathrm{RMS}=\sqrt{3^2+4^2}=5$. Two very different failure modes,
one number. This is precisely why the diagnostics of \autoref{sec:budget}
never rely on a single RMS: the $\sigma$-sweep separates the terms ---
variance scales with $\sigma$, bias does not --- and the $\sigma=0$ run
isolates bias exactly.
\end{example}

\subsection{Statistical handling}
Two facts about RMS as an \emph{estimate}, both load-bearing in
\autoref{sec:statrules}. First, a sample RMS over $N$ Gaussian errors
fluctuates with relative standard deviation $\approx1/\sqrt{2N}$
($\chi^2$ with $N$ degrees of freedom): with $N=250$ that is $4.5\%$,
which sets the $\pm3/\sqrt{2N}$ null bands of the T2/T5 tripwires.
Second, residuals of a least-squares fit are \emph{smaller} than the
noise, because the fit absorbs $p$ degrees of freedom:
$\mathrm{RMS}_{\text{resid}}=\sigma\sqrt{1-p/n}$ for $p$ parameters and
$n$ residual coordinates. Triangulation ($p=3$, $n=4$) leaves
$\sigma\sqrt{1/4}\cdot\sqrt2=\sigma/\sqrt2$ per-view distance ---
$0.212$\,px at $\sigma=0.3$, matching the measured $0.218$
(\autoref{tab:results}); calibration ($p=42$, $n=756$) should leave
$0.412$\,px, and the measured $0.484$ is the fired T5 flag of
\autoref{sec:statrules}. An RMS \emph{below} the corrected prediction
signals overfitting just as surely as one above signals underfitting.

\subsection{Where this document uses RMS}
\begin{itemize}
  \item \emph{Reprojection RMS} (px): residual image distance after
        triangulation or calibration --- the fitted noise scale;
        compare to $\sqrt2\,\sigma\sqrt{1-p/n}$
        (\autoref{tab:results}, \autoref{tab:calibresults}).
  \item \emph{3D / position RMS} (m or mm): Euclidean reconstruction or
        tracking error against ground truth; components reported
        separately when anisotropy matters (depth vs lateral,
        \autoref{tab:diagtrack}).
  \item \emph{Timing RMS} (s or min): event and sunrise timing in the
        scenario studies (\autoref{sec:scenarios}); same formula, time
        errors instead of distances.
  \item \emph{Jitter RMS about an offset}: variance-only RMS after
        removing a known constant (the sunrise estimator's offset,
        \autoref{sec:lightscen}) --- i.e.\ the second term of
        \eqref{eq:biasvar} alone, used deliberately when the bias is
        understood and harmless.
  \item \emph{Predicted RMS}: the error laws \eqref{eq:deptherr},
        \eqref{eq:hetero} are statements about the RMS of infinite
        repetitions; measured-over-predicted ratios are the consistency
        statistics of \autoref{sec:statrules}.
\end{itemize}

%----------------------------------------------------------------------
\section{Where to read: the code--paper map}
\label{sec:codemap}

Two conventions keep the code and this document navigable as one work.
\emph{Code $\to$ paper}: every public header opens with a
\code{Paper:} comment naming the section (by title, stable under
renumbering) whose theory it implements. \emph{Paper $\to$ code}:
\autoref{tab:codemap} maps each section to its implementation and to
the test or experiment that validates it --- so to pick up any thread,
read the section, open the module, run the validator.

\begin{table}[ht]
\centering
\scriptsize
\setlength{\tabcolsep}{3pt}
\begin{tabular}{l l l}
\toprule
Paper & Implementation & Validated by \\
\midrule
\autoref{sec:toolkit}, \autoref{sec:numerics} & \code{mv/mat.h}, \code{src/mat.c} & SVD/inverse tests \\
\autoref{sec:camera} & \code{mv/cam.h}, \code{src/cam.c} & center/ray/projection tests \\
\autoref{sec:timemodel} (clock model) & --- (planned state on \code{mv\_camera}) & \autoref{ex:clockfit} budgets \\
\autoref{sec:homography}--\autoref{sec:radial} & \code{mv/calib.h}, \code{src/calib.c} & Zhang exactness tests; \code{demo\_calibrate} \\
\autoref{sec:printed} & \code{mv/target.h}, \code{src/target.c} & calibration tests \\
\autoref{sec:display} & \code{mv/graycode.h}, \code{src/graycode.c} & round-trip tests \\
\autoref{sec:calibspec} (pattern, spec v1) & \code{mv/pattern.h}, \code{src/pattern.c} & M-array selftest; \code{demo\_patternsim} \\
\autoref{sec:calibspec} (reader) & \code{mv/reader.h}, \code{src/reader.c} & blind-read test; \autoref{tab:patresults} \\
\autoref{sec:calibspec} (validation tier) & \code{mv/render.h}, \code{src/render.c} & \code{demo\_patternsim} \\
\autoref{sec:epipolar} & \code{mv/epipolar.h}, \code{src/epipolar.c} & F/8-point tests; \code{demo\_synthetic} \\
\autoref{sec:triangulation}, \autoref{sec:scaling} & \code{mv/triangulate.h}, \code{src/triangulate.c} & exactness tests; \autoref{tab:diagtri} \\
\autoref{sec:stereo} & \code{mv/rectify.h}, \code{mv/stereo.h} & row-alignment tests \\
\autoref{sec:models} & \code{mv/cloud.h}, \code{mv/img.h} & PLY/PGM outputs of all demos \\
\autoref{sec:tsdf} & \code{mv/tsdf.h}, \code{src/tsdf.c} & sign/mesh tests; \autoref{tab:tsdfresults} \\
\autoref{sec:background} (dense path) & \code{demo/roomsim.c}, \code{mv/render.h} & warp/scene tests; \autoref{tab:roomresults} \\
\autoref{sec:results} diagnostics & \code{demo/diagnose.c} & the tripwires of \autoref{sec:statrules} \\
\autoref{sec:scenarios} & \code{demo/track\_robot,insects,people}; \code{lightlog} & their ground-truth scoring \\
\autoref{sec:statrules} T1 & \code{tests/test\_mv.c} (47 assertions) & \code{make check} \\
\bottomrule
\end{tabular}
\caption{Code--paper cross-reference. Demos print exactly the numbers
quoted in \autoref{sec:results}--\autoref{sec:scenarios}
(\code{make demo}); every experiment is deterministic and seeded.}
\label{tab:codemap}
\end{table}

\bibliographystyle{alpha}
\bibliography{refs}

\end{document}
