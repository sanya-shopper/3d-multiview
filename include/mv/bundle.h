#ifndef MV_BUNDLE_H
#define MV_BUNDLE_H

/* Paper: doc/multiview.tex -- bundle adjustment over registered views.
 * OWNERSHIP (parallel build): this header, src/bundle.c, and
 * tests/test_bundle.c belong to the bundle work item ONLY. */

#include "mv/cam.h"

/* work item under construction */

#endif /* MV_BUNDLE_H */
