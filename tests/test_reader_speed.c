/* Reader throughput + full-HD decode parity tests.
 * OWNERSHIP (parallel build): reader-speed work item ONLY.
 * Scaffold stub: real assertions land with the speedup. */
#include <stdio.h>

int main(void)
{
    printf("ok:   reader speed: scaffold stub (speedup item pending)\n");
    printf("all reader-speed tests passed\n");
    return 0;
}
