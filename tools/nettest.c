/* Protocol torture tool for livehub.
 * OWNERSHIP (parallel build): this file belongs to the nettest item. */
#include <stdio.h>
int main(void)
{
    fprintf(stderr, "work item under construction\n");
    return 1;
}
